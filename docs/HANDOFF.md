# HANDOFF — pyrrho project current status

> Fresh-session entry point. Read this first.
> Current north-star goal: [GOAL.md](GOAL.md).
> For the *history* of how we got here, see [LOG.md](LOG.md).
> For the full plan and roadmap, see [PROJECT.md](PROJECT.md).

This file is **overwritten** as the state changes. It always reflects only the current state.

---

## What pyrrho is, in 30 seconds

Fine-tuned classification models for RAG governance. Given a (query, retrieved contexts) pair, predicts `ABSTAIN` / `DISPUTED` / `TRUSTWORTHY` plus planning metadata for fitz-sage. Encoders are the CPU production track; generative SLM/MoE work is a separate portfolio track. Published public nano release is still `pyrrho-nano-g5.5` on official fitz-gov V11.0.0, but it is not a downstream fitz-sage upgrade. Local downstream controls: `pyrrho-nano-g5.6` remains the strongest strict-owner fitz-sage candidate at **97/120**; `g5.7` moved sideways at **96/120**; `g5.8` is rejected as another negative control (**92/120** for seed 7, **95/120** for seed 1337). Fresh dataset line: `fitz-gov-v2` is the new from-scratch family name. The 20k v2 alpha scored **20/120** downstream and is a negative baseline. The 50k v2 alpha, `pyrrho-v2-nano-g1.1-alpha`, improves held-out governance to **87.53%** / **2.97%** FT and fitz-sage to **41/120**, but it still fails package smoke and is not usable. Do not publish or wire either v2 alpha into fitz-sage.

The brand name is from Pyrrho of Elis — the Greek philosopher whose school practiced suspension of judgment when evidence was insufficient.

## Current data reset

Active classic base: `data/multitask_g5_6_v12_focused_medium_candidate` (**67,944** rows total, including **7,061** V12 focused classic rows). This corpus is now frozen on Hugging Face as [`yafitzdev/fitz-gov-v1`](https://huggingface.co/datasets/yafitzdev/fitz-gov-v1), tag `v1-final`, with `v1-final/train.parquet` (**54,416**), `v1-final/validation.parquet` (**6,778**), and `v1-final/test.parquet` (**6,750**). The old [`yafitzdev/fitz-gov`](https://huggingface.co/datasets/yafitzdev/fitz-gov) URL redirects to `fitz-gov-v1`; old V7-V11 tags/files remain in the renamed repo for continuity. Rejected sage-shaped, g5.7/g5.8, and failed repair artifacts were archived on 2026-06-18 under `data/_archive/20260618_classic_reset_archive/`, `models/_archive/20260618_classic_reset_archive/`, and `outputs/_archive/20260618_classic_reset_archive/`; the discarded-row/data archive is also on Hugging Face as private dataset `yafitzdev/fitz-gov-v1-discarded-archive`, tag `20260618-reset-archive`. The private archive has **5,126** raw small files plus `compressed_large_jsonl/missing_large_jsonl_raw.tar.zst` (**873,676,053** bytes, SHA256 `98dc1f88c810665bfa555ce72e05b64abac6cc70d985b92c3a83708a75d1c103`) covering the 49 large JSONL files from the interrupted raw upload. See `data/_archive/20260618_classic_reset_archive/archive_manifest.json`.

V13 classic generation probe: Round 1 in `C:/Users/yanfi/PycharmProjects/fitz-gov/data/_workspaces/v13_classic_probe_100_20260618/` is rejected despite schema validity because it did not match the V12 focused distribution. Round 2 in `C:/Users/yanfi/PycharmProjects/fitz-gov/data/_workspaces/v13_classic_probe_100_round2_20260619/` is the usable template: six `gpt-5.4` subagents generated **100** classic rows; `validate_v13_probe.py` passed **100/100** with **0** errors/warnings, and `strict_golden_compare.py` passed with **0** errors. For any next classic V13 generation, use `generation_contract_round2.md` and keep the strict golden-distribution check as the batch gate.

Current V13 gap detector pass: reports are under `C:/Users/yanfi/PycharmProjects/fitz-gov/data/_workspaces/reports/v13_current_gap_detector_20260619/`. The V11 practical planner over active g5.6/V12 plus accepted V13 Round 2 (**68,044** rows) reports **420** practical rows still needed: all `TRUSTWORTHY`, all `failure_focus=stale_forbidden_decoy`, only `unstructured_text` (**210**) and `log_trace` (**210**), evenly split across 7 domains and 3 difficulties, with pack shapes `retrieval_pack_6_10` (**210**) and `long_pack_11_plus` (**210**). `class_obligation` is fully covered. Use `v11_row_targets_current_plus_v13_probe.jsonl` for the next generation queue; do not use the raw V10 detector on flattened pyrrho data without adapting it.

V13 expansion levels are calculated in `C:/Users/yanfi/PycharmProjects/fitz-gov/data/_workspaces/reports/v13_current_gap_detector_20260619/v13_gap_level_options_20260619.json`. Level 1 is **420** rows. Level 2 is **836** rows total from current state, Level 3 **1,275**, Level 4 **1,767**, Level 5 **2,895**, and Level 6 **7,416**. These are practical row counts from the current base, not additive counts after regeneration.

fitz-gov-v2 fresh-start status: the old working name `fitz-gov-modern` is superseded by `fitz-gov-v2` (`fitz-gov-v1` = classic). The generator repo is `C:/Users/yanfi/PycharmProjects/fitz-gov-modern_generator`. Round 1 produced **20,000** v2 rows under `outputs/bulk_20000`; Round 2 produced **30,000/30,000** rows under `outputs/bulk_30000_round2`. Combined import passed via `scripts/prepare_v2_bulk_data.py` with **50,000** rows, **0** duplicate IDs, **0** duplicate full payloads, and **1** repeated query string. Prepared 50k data is `data/multitask_v2_g1_1_alpha_50k` with train **40,032** / eval **5,020** / test **4,948**. Source counts are **20,000** round-1 + **30,000** round-2; labels are balanced around **16.6k** each; modalities are balanced around **7.1k** each.

`pyrrho-v2-nano-g1-alpha` status: config `configs/encoder/modernbert_base_v2_nano_g1_alpha.yaml`, output `outputs/pyrrho-v2-nano-g1-alpha/seed_42`, package `models/pyrrho-v2-nano-g1-alpha`. Single seed **42**, base ModernBERT initialization, no g5.6 warm start. Package verify passed. Held-out test: governance **79.81%** / FT **5.01%**, query-contract F1 **82.25%**, route accuracy **93.00%**, taxonomy accuracy **17.86%**, retrieval-action F1 **39.84%**, gap-type F1 **24.40%**, answerability F1 **55.55%**, retrieval-modality F1 **77.68%**, retrieval-obligation F1 **51.53%**. fitz-sage benchmark: core **7/20**, holdout **8/50**, holdout2 **5/50**, total **20/120**, forbidden evidence **9**. Reports: `C:/Users/yanfi/PycharmProjects/fitz-sage/benchmarks/results/*_v2_g1_alpha.{json,md}`.

`pyrrho-v2-nano-g1.1-alpha` status: config `configs/encoder/modernbert_base_v2_nano_g1_1_alpha.yaml`, output `outputs/pyrrho-v2-nano-g1.1-alpha/seed_42`, package attempt at `models/pyrrho-v2-nano-g1.1-alpha`. Single seed **42**, base ModernBERT initialization, no g5.6 warm start. Package creation copied the model, but built-in CPU smoke returned `ok=false` because the handcrafted speed-of-light case was classified `DISPUTED` instead of `TRUSTWORTHY`; treat the package as benchmark-only, not a valid release artifact. Held-out test: governance **87.53%** / FT **2.97%**, query-contract F1 **81.51%**, route accuracy **95.70%**, taxonomy accuracy **23.57%**, retrieval-action F1 **45.41%**, gap-type F1 **40.17%**, answerability F1 **56.86%**, retrieval-modality F1 **82.54%**, retrieval-obligation F1 **62.89%**. fitz-sage benchmark: core **7/20**, holdout **19/50**, holdout2 **15/50**, total **41/120**, forbidden evidence **9**. Domain totals: code **0/28**, mixed **10/23**, structured **25/32**, unstructured **6/37**. Reports: `C:/Users/yanfi/PycharmProjects/fitz-sage/benchmarks/results/*_v2_g1_1_alpha.{json,md}`.

V2 blind-label QA / adjudication status: work-laptop Sonnet 4.6 QA covered **49,838 / 50,000** unique rows across `C:/Users/yanfi/PycharmProjects/fitz-gov-modern_generator/outputs/blind_qa_sonnet_5k_20260620/` and `.../blind_qa_sonnet_remaining_45015_20260620/`. The v2.1 overlay exists at `C:/Users/yanfi/PycharmProjects/fitz-gov-modern_generator/outputs/v2_1_label_adjudication_20260620/labels_v2_1_overlay.jsonl` and passes strict structural validation, but it is **semantically rejected**. Follow-up **1,000**-row Codex-subagent blind QA failed: label agreement **56.8%**, retrieval-action **50.9%**, gap-type **49.6%**, taxonomy **32.8%**, adjudication flags **18**. Reports: `C:/Users/yanfi/PycharmProjects/fitz-gov-modern_generator/outputs/blind_qa_v2_1_followup_1k_20260621/{drift_report.md,drift_report.json}`. Failure shape: overlay TRUSTWORTHY rows agreed **327/327**, but overlay ABSTAIN/DISPUTED rows were often relabeled TRUSTWORTHY by blind subagents. Decision: freeze v2.1 as a failed biased adjudication overlay; do not train from raw worker labels or v2.1. Active next path is **v2.2 neutral blind relabel** over the same raw 50k evidence packs using only `query + contexts`. Generator repo files: `prompt/NEUTRAL_RELABEL_PROMPT.md`, `build_neutral_relabel_queue.py`, `merge_neutral_relabel.py`, and `outputs/v2_2_neutral_relabel_20260621/HANDOFF.md`.

## Active goal — pyrrho-MoE-g4-real

The active goal is now explicitly defined in [GOAL.md](GOAL.md): build `pyrrho-MoE-g4-real`, a clean real sparse MoE with stock runtime compatibility as gate zero. `pyrrho-MoE-g3-mvp` is frozen as an experimental proof-of-life artifact, not the active target.

This supersedes both the prior "beta-hardening the alpha quorum package" direction and the patched-runtime `g3-mvp` path. The new rule: no patched llama.cpp, no Qwen3MoE `mlp_only_layers`, no public Mixtral/Qwen architecture metadata, no label-scoring-only release, and no long training before a stock runtime shape is proven.

## Current state of the family

| Release | Status |
|---|---|
| `pyrrho-nano-g1` (ModernBERT 149M encoder) | Trained + 3-seed validated. On HuggingFace. **In production — it is the governance backend of fitz-sage v0.13.0.** |
| `pyrrho-small-g1` (Qwen3.5-0.8B + LoRA, V5.1 plain SFT) | Trained + 3-seed validated locally. **Not on HF — fails the FT gate (12.13% vs 5.7% bar).** Release dir staged at `models/pyrrho-small-g1/`. See LOG 2026-05-20 evening. |
| `pyrrho-small-g1.1` (Qwen3.5-0.8B + LoRA, V5.1 SFT with class weights + label smoothing) | Trained + 3-seed validated locally. **Still not on HF — FT improved 12.13 → 9.31% but still misses 5.7% gate by ~3.6 pts.** Release dir staged at `models/pyrrho-small-g1.1/`. See LOG 2026-05-20 night. |
| `pyrrho-nano-g1.1` (ModernBERT V6 retrain) | Attempted locally on V6; **not released**. 3-seed result was 81.54 ± 5.97% accuracy / 5.31 ± 0.21% false-trustworthy, with high variance after toolchain drift. Superseded by direct V7 `g2` work. See LOG 2026-05-21. |
| `pyrrho-nano-g2` (ModernBERT V7 retrain) | **Trained + 3-seed validated on fitz-gov V7.0.1 schema-clean contract (same rows/splits/labels as V7.0.0). On Hugging Face.** Held-out test result: **95.24 ± 0.48% accuracy / 3.48 ± 0.40% false-trustworthy**. Passes gates by a wide margin. |
| `pyrrho-nano-g3` (ModernBERT V8 retrain) | **Trained + 3-seed validated on the fitz-gov V8 row set, now published as V8.0.1 with explicit `meta.modality: "unstructured"`. On Hugging Face at [`yafitzdev/pyrrho-nano-g3`](https://huggingface.co/yafitzdev/pyrrho-nano-g3).** Held-out V8 test result: **97.52 ± 0.43% accuracy / 1.42 ± 0.16% false-trustworthy** on 2,459 cases. Release artifact uses seed **1337** because it passed the packaged smoke check while also clearing held-out gates (**97.68% / 1.54% FT**). |
| `pyrrho-nano-g3.1` (ModernBERT V8 multitask encoder) | **Published custom package on Hugging Face at [`yafitzdev/pyrrho-nano-g3.1`](https://huggingface.co/yafitzdev/pyrrho-nano-g3.1); downloaded-snapshot verified; model card refreshed from the g3-style public template.** Trained on fitz-gov V8.1-style rows prepared from V8.0.1 plus mandatory `routing.query_contract`, with heads for governance, pre-retrieval query contract, semantic route/domain, taxonomy pattern, and six scalar governance signals. Held-out test result: **97.84 ± 0.15%** calibrated governance accuracy / **0.85 ± 0.07%** false-trustworthy, **94.24 ± 0.28%** query-contract macro F1, **93.41 ± 0.32%** route accuracy, **89.26 ± 0.23%** taxonomy accuracy, and **0.0592 ± 0.0005** scalar MAE. Release seed **7**, threshold **0.39**. Latest checked HF commit is README-only refresh `811bc4940d356ddf7b5bcf9102f211622d326cf1`; full package content was verified at commit `211d3131b2b1b0e74302b4213b16eb242b5b1e31`. |
| `pyrrho-nano-g3.2` (ModernBERT V8.2 retrieval-control multitask encoder) | **Published custom package on Hugging Face at [`yafitzdev/pyrrho-nano-g3.2`](https://huggingface.co/yafitzdev/pyrrho-nano-g3.2); downloaded-snapshot verified.** fitz-gov V8.2.0 is live at `yafitzdev/fitz-gov`, tag `v8.2.0`, commit `8a754bc97167bc928e6e950f4c4ec0ed71df4fa3`, with `routing.retrieval_control` on all **24,592** rows. Prepared data is `data/multitask_v8_2_retrieval_control` with train **19,674** / eval **2,459** / test **2,459**. Config is `configs/encoder/modernbert_base_g3_2_retrieval_control.yaml`. Held-out test result: **97.29 ± 0.22%** calibrated governance accuracy / **1.11 ± 0.17%** false-trustworthy, **93.92 ± 0.22%** query-contract macro F1, **92.59 ± 0.54%** route accuracy, **88.80 ± 0.19%** taxonomy accuracy, and **0.0640 ± 0.0005** scalar MAE. New-head held-out macro F1: retrieval action **86.69 ± 0.23%**, gap type **76.75 ± 1.56%**, answerability shape **75.53 ± 0.54%**, retrieval modality **55.75 ± 1.75%**. Release seed **1337**, threshold **0.34**; Hub commit `36a5940eb04bbfa0638948ee41953088bafdb0a3`. |
| `pyrrho-nano-g4` (ModernBERT official V9.0.0 multitask encoder) | **Published custom package on Hugging Face at [`yafitzdev/pyrrho-nano-g4`](https://huggingface.co/yafitzdev/pyrrho-nano-g4); downloaded-snapshot CPU verified.** Data prep is `data/multitask_g4_v9_official`, built directly from `yafitzdev/fitz-gov`, config/tag `v9.0.0`, commit `874fd18d4952eec0e72b6df2264f8281615fd350`, with train **32,625** / eval **4,104** / test **4,026**. Config: `configs/encoder/modernbert_base_g4_v9_official.yaml`. Held-out test result: **97.46 ± 0.09%** calibrated governance accuracy / **1.21 ± 0.06%** false-trustworthy, **86.00 ± 0.23%** query-contract macro F1, **93.53 ± 0.09%** route accuracy, **77.95 ± 0.25%** taxonomy accuracy, and **0.0690 ± 0.0003** scalar MAE. New-head held-out macro F1: retrieval action **85.92 ± 0.43%**, gap type **69.85 ± 1.00%**, collapsed answerability **94.92 ± 0.19%**, retrieval modality **51.94 ± 0.59%**. Release package seed **1337**, threshold **0.48**. Hub commit `43789ecdda918c9382e3f42aebf72f57bb82944d`; Hub reports **13** sibling files and safetensors parameter count **149,071,947**. |
| `pyrrho-nano-g5` (ModernBERT V10 target-10 retrieval-obligation multitask encoder) | **Published custom package on Hugging Face at [`yafitzdev/pyrrho-nano-g5`](https://huggingface.co/yafitzdev/pyrrho-nano-g5); downloaded-snapshot CPU verified, but not a clean fitz-sage production upgrade.** Data corpus is the official fitz-gov V10.0.0 row set: **53,503** rows = V6 **2,980** + V7 **7,520** + V8 **14,092** + V9 **16,163** + V10 **12,748**. The g5 training prep used the same rows with local splits train **42,826** / eval **5,372** / test **5,305** and **31** retrieval-obligation labels; the published V10 split manifest is train **42,814** / validation **5,346** / test **5,343**. Post-publish fitz-sage audit found the g5 prep path mishandled V10 retrieval-action aliases: **1,830** local V10 TRUSTWORTHY rows were trained as `retrieval_action=retrieve_more` despite `gap_type=none`; the public V10 upload normalizer reduced this to **40** remaining contradictions. V10 QA is clean after repair: **12,748/12,748** agreement, **0** triage, **0** missing/invalid/error at `C:/Users/yanfi/PycharmProjects/fitz-gov/data/_workspaces/qa/v10_target10_candidate_20260613/score_after_blind_triage_repair_20260613/`. Config: `configs/encoder/modernbert_base_g5_v10_target10_candidate.yaml`; training outputs: `outputs/pyrrho-nano-g5_v10_target10_candidate/seed_{42,1337,7}/`; summary: `outputs/pyrrho-nano-g5_v10_target10_candidate/summary.json`. Release package seed **7**, threshold **0.34**, selected for strongest held-out retrieval-obligation macro F1 while staying inside governance gates. Three-seed held-out test: **97.39 ± 0.13%** governance accuracy / **1.09 ± 0.25%** false-trustworthy, query-contract macro F1 **86.27 ± 0.14%**, route accuracy **93.65 ± 0.14%**, taxonomy accuracy **78.42 ± 0.22%**, scalar MAE **0.0689 ± 0.0002**, retrieval-action macro F1 **86.67 ± 0.18%**, gap-type macro F1 **83.47 ± 0.18%**, collapsed answerability macro F1 **93.27 ± 0.32%**, retrieval-modality macro F1 **85.98 ± 0.10%**, retrieval-obligation macro F1 **82.48 ± 0.47%**. Local CPU package verification passed via `python scripts/package_multitask_encoder.py verify --package-dir models\pyrrho-nano-g5 --device cpu`; downloaded snapshot verification passed at `outputs/hf_download_pyrrho_nano_g5_verify/release_verify_report.json`; repo smoke passed via `python -m pytest tests\test_smoke.py -v` (**11 passed**, **2 warnings**). |
| `pyrrho-nano-g5.1` (local V10 repaired retrieval-action checkpoint) | **Local-only repair checkpoint; not published.** Patch landed in `scripts/prepare_g4_alpha_data.py`: answer-style retrieval-action aliases now normalize to `answer_now`, and any `TRUSTWORTHY` row with `gap_type=none` is forced to `answer_now` during flattening. Rebuilt data: `data/multitask_g5_1_v10_repaired` with train **42,826** / eval **5,372** / test **5,305**; bad `TRUSTWORTHY + gap_type=none + retrieval_action!=answer_now` rows are **0**, down from **1,830** in g5 prep. Config: `configs/encoder/modernbert_base_g5_1_v10_repaired.yaml`; outputs: `outputs/pyrrho-nano-g5_1_v10_repaired/seed_{42,1337,7}/`; package: `models/pyrrho-nano-g5.1/`, seed **7**, threshold **0.34**. Three-seed held-out test: **97.57 ± 0.12%** governance accuracy / **1.11 ± 0.06%** false-trustworthy, retrieval-action macro F1 **87.03 ± 0.19%**, gap-type macro F1 **83.53 ± 0.37%**, retrieval-modality macro F1 **85.79 ± 0.28%**, retrieval-obligation macro F1 **82.34 ± 0.38%**. Historical fitz-sage runs: g5 **57/120**, g5.1 baseline **64/120**, obligation-aware **73/120**, compactbridge **94/120** with mixed **12/23** and forbidden evidence **0**. The compactbridge run is now historical, not the current architecture score, because the 2026-06-15 strict role-boundary fix removed hidden fitz-sage semantic planning and made Pyrrho the owner of pre-retrieval route/modality/temporal/comparison/broadness decisions. Current strict-owner fitz-sage benchmark is **92/120**: core **16/20**, holdout **41/50**, holdout2 **35/50**, forbidden evidence **5**, mixed **14/23**, code **22/28**, structured **29/32**, unstructured **27/37**. The weak slices are temporal **2/11**, final_fact **0/4**, conflict/governance/stale-doc dispute cases **0 passed**. Reports are `C:/Users/yanfi/PycharmProjects/fitz-sage/benchmarks/results/*_g5_1_strict_owner.{json,md}`. The strict path also passed fitz-sage full unit tests (**1209 passed**, **14 warnings**) and `python -m tools.contract_map`. CPU package verification passed via `python scripts/package_multitask_encoder.py verify --package-dir models\pyrrho-nano-g5.1 --device cpu`; prior audit reports are `C:/Users/yanfi/PycharmProjects/fitz-sage/benchmarks/results/g5_1_obligation_failure_audit.{md,json}`. |
| `pyrrho-nano-g5.5` (ModernBERT official V11.0.0 repair multitask encoder) | **Published custom package on Hugging Face at [`yafitzdev/pyrrho-nano-g5.5`](https://huggingface.co/yafitzdev/pyrrho-nano-g5.5); downloaded-snapshot CPU verified, but not a clean fitz-sage production upgrade over local g5.1 yet.** fitz-gov V11.0.0 is live at `yafitzdev/fitz-gov`, tag `v11.0.0`, commit `580809e42376d84284043689c702de4c500bca85`, with **60,883** rows = V6 **2,980** + V7 **7,520** + V8 **14,092** + V9 **16,163** + V10 **12,748** + V11 **7,380**. Public V11 splits are train **48,800** / validation **6,028** / test **6,055**, exact-query leakage **0**. Prepared data is `data/multitask_g5_5_v11_official` with the same local split counts and **18,868** labeled retrieval-obligation rows / **42,015** masked rows. Config: `configs/encoder/modernbert_base_g5_5_v11_official.yaml`; outputs: `outputs/pyrrho-nano-g5_5_v11_official/seed_{42,1337,7}/`; summary: `outputs/pyrrho-nano-g5_5_v11_official/summary.json`. Release package: `models/pyrrho-nano-g5.5/`, seed **1337**, threshold **0.43**, selected for lowest held-out false-TRUSTWORTHY and strongest retrieval-obligation macro F1. Three-seed held-out test: **97.98 ± 0.04%** governance accuracy / **0.92 ± 0.05%** false-trustworthy, query-contract macro F1 **87.68 ± 0.07%**, route accuracy **94.63 ± 0.06%**, taxonomy accuracy **82.51 ± 0.25%**, scalar MAE **0.0637 ± 0.0001**, retrieval-action macro F1 **88.44 ± 0.10%**, gap-type macro F1 **86.27 ± 0.10%**, answerability macro F1 **94.97 ± 0.14%**, retrieval-modality macro F1 **89.18 ± 0.17%**, retrieval-obligation macro F1 **86.38 ± 0.44%**. Strict-owner fitz-sage benchmark against the downloaded snapshot scored **90/120**: core **16/20**, holdout **39/50**, holdout2 **35/50**, forbidden evidence **4**. This is **-2** versus local g5.1 strict-owner (**92/120**) despite stronger held-out fitz-gov heads; reports are `C:/Users/yanfi/PycharmProjects/fitz-sage/benchmarks/results/*_g5_5_strict_owner.{json,md}`. Hub commit `68abae6e9c979b429bd779cfa8242faa88656116`; Hub reports **13** sibling files and safetensors parameter count **149,095,786**. Downloaded snapshot verification passed at `outputs/hf_download_pyrrho_nano_g5_5_verify/release_verify_report.json`; repo smoke passed via `python -m pytest tests\test_smoke.py -v` (**11 passed**, **2 warnings**). |
| `pyrrho-nano-g5.6` (ModernBERT local V12 focused-medium candidate) | **Local-only candidate; not published and not release-quality until the V12 rows get blind-label QA or become an official fitz-gov release.** Added **7,061** V12 focused-medium rows on top of official V11.0.0: total prepared rows **67,944** = V6 **2,980** + V7 **7,520** + V8 **14,092** + V9 **16,163** + V10 **12,748** + V11 **7,380** + V12 candidate **7,061**. Prepared data: `data/multitask_g5_6_v12_focused_medium_candidate` with train **54,416** / eval **6,778** / test **6,750**; V12 split is train **5,616** / eval **750** / test **695**. Candidate data is structurally clean, duplicate-clean, and gap-closed against `C:/Users/yanfi/PycharmProjects/fitz-gov/data/_workspaces/reports/v12_g5_6_focused_medium_20260615_plan.json`, but blind-label QA is **not run**. Config: `configs/encoder/modernbert_base_g5_6_v12_focused_medium_candidate.yaml`; outputs: `outputs/pyrrho-nano-g5_6_v12_focused_medium_candidate/seed_{42,1337,7}/`; summary: `outputs/pyrrho-nano-g5_6_v12_focused_medium_candidate/summary.json`; package: `models/pyrrho-nano-g5.6/`, seed **42**, threshold **0.34**. Three-seed held-out test: **98.28 ± 0.03%** governance accuracy / **0.81 ± 0.03%** false-trustworthy, query-contract macro F1 **88.74 ± 0.17%**, route accuracy **94.63 ± 0.21%**, taxonomy accuracy **84.03 ± 0.29%**, scalar MAE **0.0586 ± 0.0002**, retrieval-action macro F1 **89.06 ± 0.04%**, gap-type macro F1 **86.60 ± 0.22%**, answerability macro F1 **95.43 ± 0.14%**, retrieval-modality macro F1 **90.44 ± 0.11%**, retrieval-obligation macro F1 **87.30 ± 0.23%**. Strict-owner fitz-sage benchmark scored **97/120**: core **15/20**, holdout **42/50**, holdout2 **40/50**, forbidden evidence **3**, mixed **15/23**, code **26/34**, structured **31/32**, table **45/52**, temporal **6/11**, final_fact **2/4**, conflict **1/11**, governance **0/4**. This is **+7** versus g5.5 (**90/120**) and **+5** versus g5.1 (**92/120**). Reports are `C:/Users/yanfi/PycharmProjects/fitz-sage/benchmarks/results/*_g5_6_strict_owner.{json,md}`. CPU package verification passed at `models/pyrrho-nano-g5.6/release_verify_report.json`; repo smoke passed via `python -m pytest tests\test_smoke.py -v` (**11 passed**, **2 warnings**). |
| `pyrrho-nano-g5.7` (ModernBERT local V12.1 failure-repair candidate) | **Local-only candidate; not published and not a downstream upgrade over g5.6.** Added **1,200** V12.1 failure-repair rows on top of g5.6 after auditing the remaining strict-owner failures. Prepared data: `data/multitask_g5_7_v12_1_failure_repair_candidate` with **69,144** rows: train **55,380** / eval **6,871** / test **6,893**; V12.1 split is train **964** / eval **93** / test **143**. Config: `configs/encoder/modernbert_base_g5_7_v12_1_failure_repair_candidate.yaml`; outputs: `outputs/pyrrho-nano-g5_7_v12_1_failure_repair_candidate/seed_{42,1337,7}/`; summary: `outputs/pyrrho-nano-g5_7_v12_1_failure_repair_candidate/summary.json`; package: `models/pyrrho-nano-g5.7/`, seed **1337**, threshold **0.34**. Three-seed held-out test: **98.33 ± 0.05%** governance accuracy / **0.85 ± 0.04%** false-trustworthy, query-contract macro F1 **89.31 ± 0.22%**, taxonomy accuracy **84.36 ± 0.13%**, scalar MAE **0.0561 ± 0.0002**, retrieval-action macro F1 **89.15 ± 0.25%**, gap-type macro F1 **86.61 ± 0.29%**, answerability macro F1 **95.60 ± 0.12%**, retrieval-modality macro F1 **90.68 ± 0.11%**, retrieval-obligation macro F1 **88.41 ± 0.51%**. Strict-owner fitz-sage benchmark scored **96/120**: core **15/20**, holdout **43/50**, holdout2 **38/50**, forbidden evidence **3**. It fixed **3** g5.6 failures but introduced **4** new failures; audit reports are `C:/Users/yanfi/PycharmProjects/fitz-sage/benchmarks/results/g5_7_strict_owner_failure_audit.{json,md}`. CPU package verification passed at `models/pyrrho-nano-g5.7/release_verify_report.json`; repo smoke passed via `python -m pytest tests\test_smoke.py -v` (**11 passed**, **2 warnings**). |
| `pyrrho-nano-g5.6-evidence-replay-alpha` (role-correct base-only isolation control) | **Local research control; not published.** This retrains from `models/pyrrho-nano-g5.6` for one epoch on the original g5.6 data only, after fixing the architecture so `retrieval_action` and `gap_type` are evidence-conditioned heads while `answerability_shape`, `retrieval_modality`, and `retrieval_obligation` stay query-conditioned. Config: `configs/encoder/modernbert_base_g5_6_evidence_action_replay_alpha.yaml`; data: `data/multitask_g5_6_v12_focused_medium_candidate`; output: `outputs/pyrrho-nano-g5_6_evidence_action_replay_alpha/`; package: `models/pyrrho-nano-g5.6-evidence-replay-alpha`. Held-out test: **98.19%** governance accuracy / **0.87%** false-trustworthy, retrieval-action macro F1 **88.91%**, gap-type macro F1 **86.74%**, retrieval-modality macro F1 **90.36%**, retrieval-obligation macro F1 **87.79%**. Strict-owner fitz-sage scored **94/120**: core **15/20**, holdout **42/50**, holdout2 **37/50**, forbidden evidence **3**. This isolates the architecture/head-routing effect at about **-3** versus g5.6, not enough to explain the larger sage regressions. |
| `pyrrho-sage-nano-g5.6-evidence-alpha` (corrected query-planning sage control) | **Local research control; downstream-negative; do not publish or wire into fitz-sage.** Data is clean g5.6 plus the corrected query-planning-only sage view `data/fitz_gov_sage_combined_laptop30000_local900_rofilled_query_planning_only_masked_action_gap_20260618` (**98,844** rows total). Config: `configs/encoder/modernbert_base_sage_g5_6_evidence_action_alpha_query_planning_rofilled_lowlr.yaml`; output: `outputs/pyrrho-sage-nano-g5_6_evidence_action_alpha_query_planning_rofilled_lowlr/`; package: `models/pyrrho-sage-nano-g5.6-evidence-alpha`. Held-out test stayed governance-safe but obligation weakened: **98.13%** governance accuracy / **0.82%** false-trustworthy, retrieval-action macro F1 **88.70%**, gap-type macro F1 **86.67%**, retrieval-modality macro F1 **89.29%**, retrieval-obligation macro F1 **80.32%**. Strict-owner fitz-sage scored **90/120**: core **15/20**, holdout **41/50**, holdout2 **34/50**, forbidden evidence **5**. It changed `retrieval_obligation` on **58/120** benchmark cases, which is the main observed downstream damage. |
| `pyrrho-sage-nano-g5.6-fullstage-alpha` (clean+full-sage stacking control) | **Local research control; downstream-negative; do not publish or wire into fitz-sage.** This is the direct test of naive stacking: clean g5.6 base plus the corrected full sage stage corpus `data/fitz_gov_sage_combined_laptop30000_local900_rofilled_materialized_masked_action_gap_20260618`, producing `data/multitask_sage_g5_6_full_stage_alpha_v12_plus_rofilled_masked_action_gap` with **129,744** rows: **67,944** clean + **30,900** query-planning + **30,900** evidence-governance. Config: `configs/encoder/modernbert_base_sage_g5_6_full_stage_alpha_lowlr.yaml`; output: `outputs/pyrrho-sage-nano-g5_6_full_stage_alpha_lowlr/`; package: `models/pyrrho-sage-nano-g5.6-fullstage-alpha`. Held-out test: **97.60%** governance accuracy / **1.19%** false-trustworthy, retrieval-action macro F1 **89.00%**, gap-type macro F1 **86.86%**, retrieval-modality macro F1 **89.51%**, retrieval-obligation macro F1 **80.75%**. Strict-owner fitz-sage scored **88/120**: core **17/20**, holdout **37/50**, holdout2 **34/50**, forbidden evidence **4**. It gained **4** cases and lost **13** versus g5.6; naive clean+sage stacking is rejected. |
| `pyrrho-sage-nano-g1` (ModernBERT fitz-sage-shaped V10 packview encoder) | **Local first seed trained and CPU package verified, but downstream-negative; do not publish or wire into fitz-sage.** Data is the completed `fitz-gov-sage v1` derived view from clean V10.0.0 / `data/multitask_g5_1_v10_repaired`, excluding V11/V12 repair rows: **20,000** stage rows = **10,000** query-planning + **10,000** evidence-governance, split train **16,014** / eval **1,994** / test **1,992**. Config: `configs/encoder/modernbert_base_sage_g1_v10_packview.yaml`; output: `outputs/pyrrho-sage-nano-g1_v10_packview/seed_1337/`; package: `models/pyrrho-sage-nano-g1/`; initialized from `models/pyrrho-nano-g5.1` with **154/154** tensors copied. Single-seed held-out test: **96.29%** governance accuracy / **1.32%** false-trustworthy at threshold **0.34**, query-contract macro F1 **77.21%**, route accuracy **92.87%**, taxonomy accuracy **72.49%**, scalar MAE **0.0732**, retrieval-action macro F1 **58.69%**, gap-type macro F1 **59.48%**, answerability macro F1 **89.33%**, retrieval-modality macro F1 **82.55%**, retrieval-obligation macro F1 **83.74%**. Package gates passed: CPU verify returned `ok=True`; repo smoke passed **11 passed**, **2 warnings**. Strict-owner fitz-sage benchmark scored **78/120**: core **13/20**, holdout **34/50**, holdout2 **31/50**, forbidden evidence **5**. Failure breakdown: **29** mode failures, **11** required-evidence misses, **5** forbidden hits; mixed holdout2 is especially bad (**1/10**). |
| `pyrrho-sage-nano-g1.1` (ModernBERT fitz-sage-shaped V10 clean+stage repair) | **Historical local control; downstream-negative and now known to use the wrong action/gap routing assumption. Do not publish or wire into fitz-sage.** It routed `retrieval_action` and `gap_type` from query-only state; 2026-06-18 research supersedes that assumption because fitz-sage consumes both as evidence-conditioned heads. Data fix: `data/fitz_gov_sage_v1_1` masks pre-retrieval labels on evidence-governance rows; mixed training data is `data/multitask_sage_g1_1_v10_clean_plus_stage` = **53,503** clean V10 rows + **20,000** stage rows, split train **58,840** / eval **7,366** / test **7,297**. Config: `configs/encoder/modernbert_base_sage_g1_1_v10_clean_plus_stage.yaml`; output: `outputs/pyrrho-sage-nano-g1_1_v10_clean_plus_stage/seed_1337/`; package: `models/pyrrho-sage-nano-g1.1/`; initialized from `models/pyrrho-nano-g5.1` with **154/154** tensors copied. Single-seed held-out test: **97.27%** governance accuracy / **1.27%** false-trustworthy at threshold **0.34**, query-contract macro F1 **85.20%**, taxonomy accuracy **77.32%**, scalar MAE **0.0680**, retrieval-action macro F1 **72.63%**, gap-type macro F1 **70.81%**, answerability macro F1 **92.43%**, retrieval-modality macro F1 **85.51%**, retrieval-obligation macro F1 **83.44%**. Package verify returned `ok=True`; repo smoke plus multitask tests passed **20 passed**, **2 warnings**. Strict-owner fitz-sage benchmark scored **89/120**: core **15/20**, holdout **40/50**, holdout2 **34/50**, forbidden evidence **5**. Failure breakdown: **18** mode failures, **8** required-evidence misses, **5** forbidden hits. |
| `pyrrho-sage-nano-g2-alpha` (ModernBERT fitz-sage-shaped V10 clean+large-stage alpha) | **Local single-seed alpha; downstream-negative; do not publish or wire into fitz-sage.** Repaired the laptop 30k block by adding **35** missing/incomplete source pairs, producing `data/fitz_gov_sage_opus30k_laptop_repaired_full_20260617` with **60,000** stage rows and **0** structural/shape/label violations. Combined stage corpus is `data/fitz_gov_sage_combined_laptop30000_local900_materialized_20260617`: **30,900** source pairs / **61,800** stage rows. Mixed training data is `data/multitask_sage_g2_alpha_v10_clean_plus_laptop30k_local900_stage` = **53,503** clean V10 rows + **61,800** sage stage rows = **115,303** rows, split train **92,166** / eval **11,604** / test **11,533**. Config: `configs/encoder/modernbert_base_sage_g2_alpha_v10_clean_plus_laptop30k_local900_stage.yaml`; output: `outputs/pyrrho-sage-nano-g2_alpha_v10_clean_plus_laptop30k_local900_stage/seed_1337/`; package: `models/pyrrho-sage-nano-g2-alpha/`; initialized from `models/pyrrho-nano-g5.1` with **154/154** tensors copied. Held-out test: **97.43%** governance accuracy / **1.18%** false-trustworthy at threshold **0.34**, query-contract macro F1 **86.06%**, route accuracy **93.51%**, taxonomy accuracy **77.53%**, scalar MAE **0.0685**, retrieval-action macro F1 **73.27%**, gap-type macro F1 **71.68%**, answerability macro F1 **94.00%**, retrieval-modality macro F1 **86.06%**, retrieval-obligation macro F1 **84.35%**. CPU package verify passed. Strict-owner fitz-sage scored only **86/120**: core **13/20**, holdout **37/50**, holdout2 **36/50**, forbidden evidence **5**. This is **-3** versus g1.1 and **-11** versus g5.6 despite more stage data, so the current sage-shaped data mix is not a downstream improvement path without deeper diagnosis. |
| `pyrrho-sage-nano-g2.1-alpha` (ModernBERT ro-filled fitz-sage-shaped alpha) | **Local single-seed alpha; downstream-negative and rejected; do not publish or wire into fitz-sage.** Filled the missing V6-V9 `retrieval_obligation` labels for the current 30,900-pair sage corpus: laptop patch `done/done2/labels.all.jsonl` added **23,961** labels, and local six-subagent patch `data/_workspaces/sage_missing_ro_obligation_local294_20260618/labels_local294.all.jsonl` added the remaining **294** labels. Ro-filled stage corpus: `data/fitz_gov_sage_combined_laptop30000_local900_rofilled_materialized_20260618`, with **61,800** rows, **30,900** query-planning obligation labels, **0** remaining V6-V9 query-planning gaps, and **0** duplicate row IDs. Mixed training data: `data/multitask_sage_g2_1_alpha_v10_clean_plus_laptop30k_local900_rofilled` = **115,303** rows, split train **92,166** / eval **11,604** / test **11,533**, with obligation-labeled rows rising **19,393 -> 43,648** after fixing `scripts/prepare_sage_mixed_data.py` metadata counts. Config: `configs/encoder/modernbert_base_sage_g2_1_alpha_v10_clean_plus_laptop30k_local900_rofilled.yaml`; output: `outputs/pyrrho-sage-nano-g2_1_alpha_v10_clean_plus_laptop30k_local900_rofilled/seed_1337/`; package: `models/pyrrho-sage-nano-g2.1-alpha/`; initialized from `models/pyrrho-nano-g5.1` with **154/154** tensors copied. Held-out test: **97.29%** governance accuracy / **1.25%** false-trustworthy, query-contract macro F1 **86.76%**, retrieval-action macro F1 **73.54%**, gap-type macro F1 **72.15%**, retrieval-modality macro F1 **86.21%**, and expanded retrieval-obligation macro F1 **76.08%**. CPU package verify passed. Strict-owner fitz-sage regressed to **81/120**: core **14/20**, holdout **36/50**, holdout2 **31/50**, forbidden evidence **6**. Versus g2-alpha it fixed **4** cases but introduced **9** new failures; this proves missing obligation labels alone were not the bottleneck and the ro-filled label distribution/training objective currently hurts downstream behavior. |
| `pyrrho-nano-g5-alpha` (ModernBERT local V10 5/5/5 retrieval-obligation multitask encoder) | **Superseded local alpha; keep only as the first V10 obligation-head integration checkpoint. Do not publish or wire into fitz-sage.** Data: `data/multitask_g5_alpha_v10_5x5_candidate` = official public V9 **40,755** rows plus **6,058** local V10 5/5/5 candidates. Package: `models/pyrrho-nano-g5-alpha/`; checkpoint: `outputs/pyrrho-nano-g5_alpha_v10_5x5_candidate/seed_1337/best_model`. Single-seed held-out test: **96.98%** governance accuracy / **1.22%** false-trustworthy at threshold **0.34**, retrieval-modality macro F1 **78.36%**, retrieval-obligation macro F1 **67.15%**. Superseded by `pyrrho-nano-g5`, which raises 3-seed mean obligation F1 to **82.48%**. |
| `pyrrho-nano-g4-target100` (ModernBERT local V9 target-100 multitask encoder) | **Local-only pre-public 3-seed candidate; do not publish directly as `pyrrho-nano-g4`. Superseded by the official-split `pyrrho-nano-g4` run.** It was prepared from the QA-gated local fitz-gov V9 target-100 vault before the public V9 split was finalized: `data/multitask_g4_v9_target100` has **40,755** rows with train **32,617** / eval **4,051** / test **4,087**. Public fitz-gov V9.0.0 uses official query-grouped splits train **32,625** / validation **4,104** / test **4,026**. Existing local candidate metrics remain useful as a sanity check: **97.05 ± 0.11%** governance accuracy / **1.36 ± 0.13%** false-trustworthy at threshold **0.34**, collapsed answerability macro F1 **94.69 ± 0.11%**. Local package `models/pyrrho-nano-g4-target100/` is CPU verified for fitz-sage integration only. |
| `pyrrho-nano-g4-alpha` (ModernBERT local V9-candidate multitask encoder) | **Local-only alpha package for fitz-sage integration; do not publish as a release-quality V9 model.** Trained seed **1337** from the published `g3.2` checkpoint with the old 11-way answerability head replaced by the new **4-way collapsed** head: `direct_answer`, `synthesis_answer`, `set_answer`, `structured_reasoning`. Data: `data/multitask_g4_alpha_v9_candidate` = **35,362** rows (**24,592** V8.2 + **750** local merged V9 + **10,020** structurally clean V9 candidates from batches `100`-`433`, not blind-QAed). Config: `configs/encoder/modernbert_base_g4_alpha_v9_candidate.yaml`; package: `models/pyrrho-nano-g4-alpha/`; CPU package smoke passed. Mixed held-out alpha test: **97.38%** governance accuracy / **1.35%** false-trustworthy at threshold **0.44**, query-contract macro F1 **86.71%**, route accuracy **93.47%**, taxonomy accuracy **79.58%**, scalar MAE **0.0703**, retrieval-action macro F1 **85.97%**, gap-type macro F1 **70.61%**, collapsed answerability macro F1 **93.81%**, retrieval-modality macro F1 **52.97%**. V9 enum drift was canonicalized into the V8.2 head contract; treat metrics as development signals only. |
| `pyrrho-nano-g3-modality-joint-3seed probe` (ModernBERT local structured/code control) | **Local-only 3-seed joint retraining control; do not publish.** Trained on published V8 plus candidate structured+code rows from `data/processed_v8_plus_structured_code_candidate`. Mixed held-out test passes aggregate gates at **98.34 ± 0.23% accuracy / 1.14 ± 0.33% FT**. Candidate modality rows scored **100.00 ± 0.00% / 0.00 ± 0.00% FT** for both structured and code across all 3 seeds. The blocker is generalization/release evidence: unstructured held-out slice is **96.99 ± 0.41% / 2.03 ± 0.60% FT**, worse on FT than published g3's V8 baseline (**97.52 ± 0.43% / 1.42 ± 0.16% FT**), hand-authored tabular OOD is improved but not stable (**83.33 ± 4.81% / 4.17 ± 7.22% FT**), and hand-authored code OOD remains unsafe (**77.78 ± 14.70% / 29.17 ± 27.32% FT**). |
| `pyrrho-nano-g3-modality-joint+patch-3seed probe` (ModernBERT local structured/code patch control) | **Local-only label-trusted 3-seed control; do not publish.** Trained on published V8.0.1 plus 20,000 structured/code candidate rows and the 720-row targeted code patch from `data/processed_v8_plus_structured_code_patch_candidate`. Mixed held-out test passes gates at **98.52 ± 0.13% accuracy / 1.06 ± 0.22% FT**. Candidate code and structured slices are both **100.00 ± 0.00% / 0.00 ± 0.00% FT**; unstructured slice is **97.28 ± 0.25% / 1.92 ± 0.40% FT**. Hand-authored code OOD improved strongly vs the unpatched joint branch (**77.78 ± 14.70% / 29.17 ± 27.32% FT -> 95.37 ± 3.21% / 6.94 ± 4.81% FT**), but the remaining false-TRUSTWORTHY failures all collapse to retry-limit code/config numerical conflict. Patch blind-label QA is still deferred. |
| `pyrrho-nano-g3-modality-joint+retry-patch-3seed probe` (ModernBERT local retry-conflict control) | **Current best local modality baseline; label-trusted only; do not publish.** Added a second 360-row retry-limit code/config patch and prepared `data/processed_v8_plus_structured_code_retry_patch_candidate` offline from the local V8 split manifest. Seeds **42/1337/7** trained at `outputs/modality_retraining/structured_code_retry_patch_seed{42,1337,7}/`. Aggregate held-out test: **98.62 ± 0.15% accuracy / 1.06 ± 0.05% FT** overall; code **100.00 ± 0.00% / 0.00 ± 0.00% FT**, structured **100.00 ± 0.00% / 0.00 ± 0.00% FT**, unstructured **97.47 ± 0.28% / 1.94 ± 0.09% FT**. The 36-row code OOD probe is **99.07 ± 1.60% accuracy / 1.39 ± 2.41% FT** and the 36-row tabular OOD probe is **93.52 ± 8.93% / 1.39 ± 2.41% FT**. The OOD-only policy sweep's best fixed retry threshold is tau **0.73**, which removes probe FT on these small OOD sets. Full blind-label QA is still required before any merge/publish decision. |
| `pyrrho-nano-g3-modality-joint+missing-evidence-patch exposed-seed probe` (ModernBERT local missing-evidence control) | **Local-only label-trusted exposed-seed control; do not publish.** Added a third 360-row candidate patch for code `missing_specific_field` diff-context plus structured missing-result/exact-row/conflict controls and prepared `data/processed_v8_plus_structured_code_missing_evidence_patch_candidate`. Seeds **42/7** trained at `outputs/modality_retraining/structured_code_missing_evidence_patch_seed{42,7}/`. Two-seed held-out test: **98.70 ± 0.18% accuracy / 0.75 ± 0.25% FT** overall; code **100.00 ± 0.00% / 0.00 ± 0.00% FT**, structured **100.00 ± 0.00% / 0.00 ± 0.00% FT**, unstructured **97.56 ± 0.35% / 1.39 ± 0.46% FT**. Code OOD is **93.06 ± 5.89% / 2.08 ± 2.95% FT** and tabular OOD is **87.50 ± 13.75% / 0.00 ± 0.00% FT**. This fixes the seed-42 missing-field false-TRUSTWORTHY but is not a clear domination of the retry patch: seed 42 becomes over-conservative on TRUSTWORTHY OOD examples, while seed 7 still has one retry-limit FT. The threshold/policy sweep confirms this is not just calibration. Full blind-label QA remains required before any merge/publish decision. |
| `pyrrho-nano-g3-modality-specialist-routing diagnostic` (ModernBERT local routing/specialist comparison) | **Complete local-only diagnostic; do not publish.** Added `scripts/modality_specialist_compare.py` and compared the retry-patch seed-42 generalist with the existing seed-42 code-only and structured-only separate specialist encoders. Artifacts: `outputs/modality_specialist_compare/retry_patch_seed42_router/`, `outputs/code_ood_probe/code_specialist_seed42/`, and `outputs/tabular_ood_probe/structured_specialist_seed42/`. On the retry-patch mixed held-out test, seed-42 retry generalist scored **98.74% / 1.07% FT**; routing code rows to the old code-only specialist regressed to **98.26% / 1.46% FT** because code-slice quality fell to **97.97% / 1.66% FT**; routing structured rows tied the generalist on in-distribution structured candidate rows. On OOD, the code-only specialist was safer but too conservative (**86.11% / 0.00% FT**, TRUSTWORTHY recall **58.33%**) and the structured-only specialist was unsafe (**91.67% / 8.33% FT**). This does **not** justify replacing or augmenting retry-patch with the existing one-seed specialists; any future specialist needs to be patch-aware and evaluated as a new local control. |
| `pyrrho-nano-g3-modality-patch-aware-code-specialist diagnostic` (ModernBERT local code specialist comparison) | **Complete local-only seed-42 diagnostic; do not publish.** Added `scripts/filter_processed_modalities.py`, prepared `data/processed_v8_plus_code_retry_patch_candidate` by keeping `unstructured` + `code` rows from the retry-patch processed set, and trained `outputs/modality_retraining/code_retry_patch_seed42/`. Held-out code+unstructured test is **98.25% / 1.49% FT**; code slice is **100.00% / 0.00% FT**, but unstructured is **97.48% / 2.13% FT**. Code OOD is **97.22% / 4.17% FT**, tying the retry-patch seed-42 generalist but shifting the miss from missing-field to retry-limit `constant_config_conflict` on `code_excerpt`. Routing code rows to this specialist in `outputs/modality_specialist_compare/retry_patch_seed42_patch_aware_code_router/` ties the seed-42 retry generalist on the mixed held-out test (**98.74% / 1.07% FT**) because both are already perfect on candidate code rows. This does **not** justify replacing or augmenting the retry-patch joint generalist. |
| `pyrrho-nano-g3-modality-patch-aware-structured-specialist diagnostic` (ModernBERT local structured specialist comparison) | **Complete local-only seed-42 diagnostic; do not publish.** Prepared `data/processed_v8_plus_structured_retry_patch_candidate` by keeping `unstructured` + `structured` rows from the retry-patch processed set, and trained `outputs/modality_retraining/structured_retry_patch_seed42/`. Held-out structured+unstructured test is **98.32% / 1.02% FT**; structured slice is **100.00% / 0.00% FT**, but unstructured is **97.64% / 1.42% FT**. Routing structured rows to this specialist in `outputs/modality_specialist_compare/retry_patch_seed42_patch_aware_structured_router/` ties the seed-42 retry generalist on the mixed held-out test (**98.74% / 1.07% FT**) because both are already perfect on candidate structured rows. Tabular OOD is unsafe at **88.89% / 16.67% FT**, so this does **not** justify a separate structured encoder. |
| `pyrrho-nano-g3-modality-threshold-policy diagnostic` (ModernBERT local policy comparison) | **Complete local-only 3-seed diagnostic; do not publish.** Added `scripts/modality_threshold_sweep.py` and evaluated per-modality TRUSTWORTHY thresholds on the retry-patch joint-generalist branch. Artifacts: `outputs/modality_threshold_sweep/retry_patch_3seed/`. Global retry-patch thresholds remain best on held-out accuracy: **98.62 ± 0.12% / 1.06 ± 0.04% FT**. Per-modality thresholds selected to avoid eval FT regression scored **98.61 ± 0.11% / 1.04 ± 0.05% FT**, trading **-0.01 pp** accuracy for **-0.02 pp** FT. This does **not** justify augmenting the joint generalist with a simple modality-threshold policy. |
| `pyrrho-nano-g3-modality-patch-QA setup` (fitz-gov local QA preparation) | **Complete local QA setup; no scoring yet.** Built blind-label queues/manifests and Codex blind shards for the retry-conflict and missing-evidence patches in fitz-gov without LM Studio/API calls or row generation. All three patch QA dirs now exist: `modality_code_patch_v1_20260528` (**720** rows / **12** shards), `modality_code_retry_conflict_patch_v1_20260529` (**360** rows / **12** shards), and `modality_missing_evidence_patch_v1_20260529` (**360** rows / **12** shards). Labels remain local-control-only until full blind-label QA passes. |
| `pyrrho-nano-g2.1-v8-probe` (ModernBERT local V8 probe retrain) | **Local-only experimental retrain** on published V7 splits plus the 525-row V8 cohort appended by manifest. Mixed held-out test result: **95.51 ± 0.43% accuracy / 3.56 ± 0.38% false-trustworthy** on 1,107 rows. Automotive ECU OOD probe improved from **7.00/10** to **8.33/10** mean across the same 3 seeds. Not published. |
| `pyrrho-nano-g2.1-v8-verdict-patch` (ModernBERT local ablation) | **Failed local ablation; do not publish.** Added 105 hard `verdict_conflict` rows on top of the 525-row V8 probe. Held-out test still passed gates (**94.92 ± 0.41% / 4.08 ± 0.92% FT**), and `ecu_04` improved **1/3 -> 2/3**, but ECU OOD mean regressed **8.33/10 -> 7.33/10** by over-predicting DISPUTED on nearby TRUSTWORTHY/ABSTAIN controls. |
| `pyrrho-nano-g2.2` (ModernBERT local V8 balanced-controls retrain) | **Trained + 3-seed validated locally; do not publish.** Uses published V7 plus the older **840-row** V8 clean set. Held-out test passes gates at **95.49 ± 0.15% accuracy / 3.06 ± 0.61% false-trustworthy**, but it is now superseded by the active **14,092-row** V8 target-50 vault and should be treated as an ablation checkpoint, not a release candidate. |
| `pyrrho-nano-g2.3-v8-claude4200` (ModernBERT local V8 expanded retrain) | **Older data prep only; do not train as the next run.** It used published V7 plus the earlier **4,200-row** V8 vault. Prepared data: `data/processed_v8_claude4200` with train=11,773 / eval=1,439 / test=1,488. Superseded by the active **14,092-row** V8 target-50 vault; rebuild prep before the next retrain. |
| `pyrrho-small-g2` | Not started. See [ROADMAP.md](ROADMAP.md). |
| `pyrrho-MoE-g4-real` | **Active target after 2026-05-31 reset; gate zero now passed for the OLMoE carrier shape, and the first donor-initialized seed loads in stock runtimes.** Public carrier is `configs/moe/pyrrho_moe_g4_real_olmoe_stock_runtime.yaml`: **3.969692721B total / 0.402437169B active inclusive / 0.299414577B active excluding embeddings**, **24** all-MoE layers, **19** experts/layer, top-1 routing, `ffn_dim=2688`, full `kv_heads=16`, untied 50,304-token embeddings, no dense-only FFN layers, no Qwen3MoE `mlp_only_layers`. Full random structural proof and donor-initialized F16 GGUF both load in clean upstream llama.cpp and LM Studio; donor artifact: `outputs/moe/g4_real_olmoe_donor_init/pyrrho_g4_real_olmoe_1b7b_block_resize_f16.gguf`, report: `outputs/moe/g4_real_olmoe_donor_init/donor_init_runtime_probe_report.json`. Quality path remains negative: bounded SFT, stable expert/router adaptation, teacher distillation, raw expert gate/up+down LoRA, second head-topnorm donor resize, and external Qwen/LFM controls all fail to move past the ~39% small-probe ceiling without collapse or unsafe FT. Latest external-control summary: `outputs/external_baselines/pyrrho_moe_external_baseline_control_summary_2026-05-31.json`. Mixtral remains internal evidence only. Next step is stronger OLMoE initialization/upcycling or a materially broader training surface before any longer fitz-gov scaling. |
| `pyrrho-MoE-g3-mvp` / `pyrrho-MoE-g3` | **Published MVP candidate on Hugging Face at [`yafitzdev/pyrrho-MoE-g3-mvp`](https://huggingface.co/yafitzdev/pyrrho-MoE-g3-mvp); latest checked commit `f637325ba5a49429952c122b14913dd4cf355411` after documenting the LM Studio limitation.** The final downloaded-artifact validation was performed after full-package commit `6b44a30c531c286fcfc0d5b9b618c25c5c86441e`; architecture naming was clarified in commit `ff26a761ec6adf37cb99be8eaffe58142a0843ec`; public quick start landed in commit `022825604abedec31d78f91f6706bbfd70000507`. Public naming is **Project architecture: Pyrrho MoE MVP** and **Runtime / loader architecture: Qwen3MoE-compatible sparse MoE**; HF/GGUF may still show `qwen3moe` as loader metadata. Real Qwen-seeded 4.083B / 0.424B generative sparse-MoE path now has a hardened adapter package at `models/pyrrho-MoE-g3-mvp/` and a bundled Q4_K_M GGUF decision runtime. New operator guide: `docs/PYRRHO_MOE_MVP_RUN_GUIDE.md`. Selected-output label-score runtime clears full-split gates at **83.69% / 4.29% FT** on eval and **82.39% / 4.44% FT** on held-out test using **tau 0.50**. Bounded 512-row full-generation evidence is **100%** JSON+label parse, **81.64%** route, **62.30%** taxonomy. Local package verifier passes (`release_verify_report.json`; **34** files / **3,349,873,300** bytes) and SHA256-checks the bundled Q4_K_M GGUF plus llama.cpp patch; final downloaded-snapshot verifier passes at **34** files / **3,349,869,920** bytes. Hub reports **34** sibling files and **3,348,978,530** bytes used storage. Low-memory GGUF artifacts now exist: merged HF checkpoint at `outputs/moe/pyrrho_moe_g3_mvp_merged_hf/`, BF16 GGUF **8,174,622,880 bytes**, and package-bundled Q4_K_M GGUF **2,703,385,760 bytes**. The local llama.cpp checkout at `C:/Users/yanfi/.unsloth/llama.cpp` is patched for Qwen3MoE `mlp_only_layers` and `norm_topk_prob=false`. LM Studio is not a supported runtime for this GGUF because its bundled llama.cpp lacks the patch; generic "Failed to load the model" is expected there. Q4 raw generation is parseable but unsafe (**84.38% / 20.83% FT** on the 32-row random smoke), and first-token label scoring failed at scale. Full-sequence GGUF label scoring is now the low-memory CPU decision path: Q4_K_M full held-out test at tau **0.50** scored **82.15%** accuracy / **5.27%** FT on **2,459** rows, with **100%** label parse, **4.224 GiB** peak RSS, and **96.38%** agreement with the HF selected-output decisions. Downloaded-snapshot Q4 random-32 smoke passed at **93.75% / 0.00% FT**, **100%** label parse, and **4.220 GiB** peak RSS. Runtime safety, local packaging, upload, downloaded-snapshot gates, public architecture naming clarification, and public quick-start docs are now passed; remaining work is post-publish integration/iteration. |
| `pyrrho-MoE-g3-alpha` | **Published on Hugging Face at [`yafitzdev/pyrrho-MoE-g3-alpha`](https://huggingface.co/yafitzdev/pyrrho-MoE-g3-alpha).** Local mirror: `models/pyrrho-MoE-g3-alpha/`. It packages the Stage 0.7 support-aggregation seed checkpoints (42/1337/7), per-seed post-hoc HGB verifiers, config, metadata, model card, LFS attributes, and policy reports. Default policy is `trustworthy_quorum_2_of_3`: held-out **90.65% accuracy / 1.90% false-TRUSTWORTHY / 81.71% TRUSTWORTHY recall**. Final checked Hub commit: `a1d672201bedc83a5ca66a759f5e521420185bd1`; remote has **24** expected files and **691,739,877** bytes used storage. CPU smokes passed from both the local release dir and a downloaded Hub snapshot. Beta-track release tooling now exists: `scripts/verify_moe_release.py` checks package integrity and `scripts/benchmark_moe_release.py` measures local runtime/RSS plus batch-size profiles. Current local 32-row CPU evidence: batch-16 quorum **100.51 ± 0.44 ms/row**, **9.95 rows/s**, peak RSS **1.70 GiB**; batch-16 seed-42 **32.72 ± 0.19 ms/row**, **30.56 rows/s**, peak RSS **1.30 GiB**. One-load batch profiles found the fastest quorum setting at batch **4** (**70.11 ms/row**, **14.26 rows/s**) and fastest seed-42 setting at batch **1** (**21.92 ms/row**, **45.62 rows/s**). This is a research/alpha MoE governance artifact, **not** the final 4B-A0.4B sparse model. True custom 4B-A0.4B remains future work; best full-eval Qwen/upcycled-style checkpoint is only **54.66%** calibrated accuracy / **5.35%** false-trustworthy, so do not represent this alpha as the finished 4B model. |

## pyrrho-MoE architecture clarification

Canonical spec: `C:/Users/yanfi/PycharmProjects/pyrrho/docs/PYRRHO_MOE_ARCHITECTURE.md`.

The terminal `pyrrho-MoE` target is still **custom 4B total / 0.4B active**, CPU-runnable, with pyrrho-defined experts and supervised routing from fitz-gov (`routing.expert_fired`). It is not an off-the-shelf LFM2/Qwen/other MoE checkpoint renamed as pyrrho. Because full general-language pretraining from scratch is outside the project budget, the intended path is **dense-to-MoE upcycling plus distillation**: pick a compatible pretrained dense SLM seed, clone its dense FFNs into sparse experts, preserve general competence through teacher distillation, then train the custom sparse expert layout on fitz-gov governance/routing tasks. Baseline spec is 24 layers, hidden size 1024, 20 MoE layers, 16 physical experts/layer grouped into 8 semantic expert groups, top-1 routing, and **3.950935086B total / 0.411991086B active inclusive** under the implemented counter. `LiquidAI/LFM2-8B-A1B` is only a proxy/teacher/comparison candidate, not the final 4B-A0.4B architecture.

Current goal reset (2026-05-31): **build `pyrrho-MoE-g4-real`.** `pyrrho-MoE-g3-mvp` is now experimental evidence only because it depends on Qwen3MoE `mlp_only_layers`, patched llama.cpp, and a label-scoring decision path. The clean target had to pass stock runtime shape/loadability before training scale-up, and that gate is now passed for OLMoE. First generic config is `configs/moe/pyrrho_moe_g4_real_stock_runtime.yaml`; local runtime-shape audit passed at `outputs/moe/g4_real_runtime_shape_gate.json`, but Mixtral is internal evidence only because the user does not want public Mixtral association. The current public-carrier candidate is OLMoE: `configs/moe/pyrrho_moe_g4_real_olmoe_stock_runtime.yaml` passes budget/audit at **3.969692721B total / 0.402437169B active inclusive**; full random structural proof with clean upstream llama.cpp and LM Studio CLI is `outputs/moe/g4_real_stock_runtime_carrier/olmoe_full_stock_runtime_probe_report.json`; full SFT smoke is `outputs/moe/olmoe_g4_real_sft_full_smoke_fp32loss/train_report.json`; the first donor-initialized seed from `allenai/OLMoE-1B-7B-0924` also loads in clean llama.cpp and LM Studio at `outputs/moe/g4_real_olmoe_donor_init/`. Bounded donor SFT and bounded teacher distillation are now quality-negative; see [OLMOE_TRAINING_PATH_2026-05-31.md](OLMOE_TRAINING_PATH_2026-05-31.md), `outputs/moe/g4_real_olmoe_donor_init/sft_probe_summary_2026-05-31.json`, and `outputs/moe/g4_real_olmoe_donor_init/teacher_distillation_probe_summary_2026-05-31.json`.

Current MoE scaffold:
- Config: `configs/moe/pyrrho_moe_g3_alpha.yaml`.
- Parameter counter: `scripts/count_moe_params.py`.
- MoE V8 prep/audit: `scripts/prepare_moe_data.py`; strict audit against the V8 row set wrote `data/moe_v8` with **train=19,674 / eval=2,459 / test=2,459**, **0** missing required fields, **23** taxonomy patterns, and route IDs for the 8 semantic expert groups. Future prep should pull HF `v8.0.1` so every row carries `meta.modality: "unstructured"`.
- Stage 0 trainer: `scripts/train_moe.py`; full local run at `outputs/moe/stage0_route_proto/` trained a **10,505,009-param** hashed-token top-1 MoE prototype for 3 epochs in ~38s. Held-out test: **82.47%** governance accuracy, **5.63%** false-trustworthy, **81.09%** route accuracy, **65.80%** taxonomy accuracy.
- Stage 0 g3 distillation diagnostics: `scripts/generate_moe_teacher_logits.py` wrote full `pyrrho-nano-g3` sidecars at `outputs/moe/teacher_logits/pyrrho_nano_g3_full_v8/` (**train=19,674 / eval=2,459 / test=2,459**). `outputs/moe/stage0_route_proto_distill_g3_route15/final_metrics.json` used governance KL distillation plus higher route loss and reached held-out test **82.43%** calibrated accuracy / **5.45%** FT / **82.80%** route / **64.99%** taxonomy. This confirms the V8 route signal is learnable when supervised semantic route is the actual active expert path; it does **not** rescue the current Qwen adapter path.
- Stage 0.5 route-coupled custom student: `configs/moe/pyrrho_moe_stage0_5_route_coupled.yaml` trains a **53,861,425-param** hash-token student where the selected semantic route drives every residual expert layer. Three-seed full V8 run at `outputs/moe/stage0_5_route_coupled_g3_3seed/summary.json`: held-out test **83.91 ± 1.18%** calibrated accuracy / **5.55 ± 0.03%** FT / **82.92 ± 0.35%** route / **67.64 ± 1.23%** taxonomy. Per-seed calibrated test accuracy / FT: seed 42 **84.47% / 5.51%**, seed 1337 **84.71% / 5.57%**, seed 7 **82.55% / 5.57%**. Gold-route mean was **84.64 ± 1.05%** calibrated / **5.29 ± 0.42%** FT, so the remaining gap is not primarily route prediction. A too-conservative route-heavy variant (`stage0_5_route_coupled_g3_route15`) scored only **77.51%** calibrated / **0.77%** FT.
- Stage 0.5 failure analysis: `scripts/analyze_moe_failures.py` writes per-case predictions, per-route/per-taxonomy breakdowns, seed-overlap histograms, and top hard-error lists. Test report at `outputs/moe/stage0_5_route_coupled_g3_3seed/failure_analysis_test/failure_report.md`; eval report at `outputs/moe/stage0_5_route_coupled_g3_3seed/failure_analysis_eval/failure_report.md`. Test split has **109/2,459** all-seed hard errors (**4.43%**), **12** all-seed false-TRUSTWORTHY cases, and **202** rows with FT in at least one seed. Weak test routes: `science_medicine` (**78.80%** accuracy / **12.38%** FT), then `technology_computing` and `general_commonsense`. Weak test taxonomy groups: `consistent_chain` (**66.43%**), `multi_source_corroboration` (**67.38%**), `quantitative_consensus` (**71.11%**), and `factual_contradiction` (**77.88%**, **12.98%** FT). Eval split repeats the core pattern: **106** all-seed hard errors and `consistent_chain` is still weakest (**62.99%**).
- Stage 0.6 token route-coupled baseline: `TokenRouteCoupledMoEForGovernance` in `src/pyrrho/moe/modeling.py` is a **55,728,817-param** hash-token student with RoPE self-attention, RMSNorm pre-norms, route-selected SwiGLU FFNs, and last-token/mean pooled heads. Config: `configs/moe/pyrrho_moe_stage0_6_token_route_coupled.yaml`. Three-seed run at `outputs/moe/stage0_6_token_route_coupled_g3_3seed/summary.json`: held-out test **87.23 ± 1.29%** calibrated accuracy / **2.92 ± 1.06%** FT / **86.06 ± 0.94%** route / **71.97 ± 0.72%** taxonomy. Per-seed calibrated test accuracy / FT: seed 42 **88.33% / 2.37%**, seed 1337 **87.56% / 4.15%**, seed 7 **85.81% / 2.25%**. Gold-route mean was **87.61 ± 1.51%** calibrated / **2.86 ± 1.22%** FT, so the remaining gap is still mostly trunk/pattern handling rather than route prediction.
- Stage 0.6 failure analysis: reports at `outputs/moe/stage0_6_token_route_coupled_g3_3seed/failure_analysis_{eval,test}/`. Test split has **140/2,459** all-seed hard errors (**5.69%**), **18** all-seed false-TRUSTWORTHY cases, and **92** rows with FT in at least one seed. Compared with Stage 0.5, headline metrics and safety improved strongly, especially `science_medicine` (**78.80% / 12.38% FT → 82.22% / 4.08% FT**) and `factual_contradiction` (**77.88% / 12.98% FT → 89.68% / 5.31% FT**). The unresolved regression is support-pattern TRUSTWORTHY recall: `multi_source_corroboration` fell **67.38% → 59.50%** and `quantitative_consensus` fell **71.11% → 65.40%**, while `consistent_chain` only improved **66.43% → 69.27%**.
- Stage 0.6b-e support-recall recipe sweep: added per-pattern support weights in `src/pyrrho/moe/losses.py` and configs `pyrrho_moe_stage0_6{b,c,d,e}_*.yaml`. Seed-42 probes show scalar weighting is not enough. 0.6c/0.6d can recover `multi_source_corroboration` / `quantitative_consensus`, but leak FT; 0.6e guards FT (**1.66%**) but collapses support recall. Do **not** scale 0.6b/0.6c/0.6d/0.6e to 3 seeds.
- Stage 0.7 support-aggregation baseline: `SupportAggregatingMoEForGovernance` in `src/pyrrho/moe/modeling.py` keeps the Stage 0.6 flat token route-coupled trunk, adds query/source tensors in `src/pyrrho/moe/data.py`, and fuses query-source support pooling into the terminal heads. Config: `configs/moe/pyrrho_moe_stage0_7_support_aggregation.yaml` (**4 epochs**; the same recipe overfit/shifted unsafe at 5 epochs on seed 42). Three-seed run at `outputs/moe/stage0_7_support_aggregation_g3_3seed/summary.json`: held-out test **89.49 ± 0.47%** calibrated accuracy / **3.06 ± 0.45%** FT / **82.61 ± 2.50%** route / **75.78 ± 0.21%** taxonomy. Per-seed calibrated test accuracy / FT: seed 42 **90.04% / 3.26%**, seed 1337 **89.18% / 2.55%**, seed 7 **89.26% / 3.38%**. Gold-route mean was **89.60 ± 0.55%** calibrated / **3.14 ± 0.52%** FT, so route prediction is not the limiting factor. Support patterns improved versus Stage 0.6: `consistent_chain` **69.27% → 75.18%**, `multi_source_corroboration` **59.50% → 69.53%**, `quantitative_consensus` **65.40% → 79.05%**. Safety caveat: `science_medicine` accuracy improved **82.22% → 85.21%**, but FT worsened **4.08% → 5.58%**; `factual_contradiction` stayed near Stage 0.6 accuracy but FT worsened **5.31% → 6.19%**. Next work should preserve Stage 0.7 support gains while restoring Stage 0.6 safety on these slices.
- Stage 0.7b-d guarded support probes: configs `pyrrho_moe_stage0_7{b,c,d}_*.yaml` ran seed-42 probes. 0.7b restored safety best (**88.98%** accuracy / **2.13%** FT; `science_medicine` FT **2.86%**, `factual_contradiction` FT **3.54%**) but cut support recall (`multi_source_corroboration` **61.29%**, `quantitative_consensus` **73.33%**). 0.7c restored multi-source recall but not safety; 0.7d did not dominate. Post-hoc stricter TRUSTWORTHY thresholds and the existing `false_trustworthy_risk` scalar head also trade off support recall too quickly. Do **not** scale 0.7b/0.7c/0.7d to 3 seeds; move to an architectural guard head.
- Stage 0.8 guarded-head scaffold: `GuardedSupportAggregatingMoEForGovernance` adds a learned positive TRUSTWORTHY penalty on top of the Stage 0.7 support-fused state. Config: `configs/moe/pyrrho_moe_stage0_8_guarded_support_aggregation.yaml`. CUDA smoke/reload passed, but seed-42 quality is negative. Four epochs overfit to **85.28%** calibrated accuracy / **5.57%** FT; the 3-epoch checkpoint was safer but too conservative at **85.40%** accuracy / **2.31%** FT / **76.78%** route / **73.32%** taxonomy. Do **not** scale this Stage 0.8 implementation.
- Stage 0.9 explicit trust-guard probe: `TrustGuardedSupportAggregatingMoEForGovernance` adds a separately supervised binary trust verifier over Stage 0.7 candidate logits/support state. Config: `configs/moe/pyrrho_moe_stage0_9_trust_guarded_support_aggregation.yaml`. CUDA smoke/reload/tests passed, but seed-42 quality is negative. Three epochs reached **86.50%** calibrated accuracy / **1.24%** FT / **82.19%** route / **72.83%** taxonomy; four epochs reached **84.75%** / **0.59%** FT / **84.38%** route / **73.97%** taxonomy. It restores safety by collapsing support recall (`multi_source_corroboration` **45.16%** at 3 epochs, **38.71%** at 4 epochs). Do **not** scale this implementation.
- Stage 0.7 post-hoc verifier: `scripts/train_moe_posthoc_verifier.py` trains a separate HGB verifier on frozen Stage 0.7 candidate outputs and only demotes candidate TRUSTWORTHY predictions. Safety-heavy 2.5% eval-FT target artifact: `outputs/moe/stage0_7_posthoc_verifier_g3_3seed/summary.json` (**88.97 ± 0.51%** accuracy / **1.99 ± 0.17%** FT). Preferred support-retaining 2.8% eval-FT target artifact: `outputs/moe/stage0_7_posthoc_verifier_g3_3seed_ft028/summary.json`, which moves the verifier-script baseline **89.37 ± 0.59% / 2.94 ± 0.36% FT** to guarded **89.29 ± 0.69% / 2.37 ± 0.26% FT**. Minimal-intervention 3.0% eval-FT target artifact: `outputs/moe/stage0_7_posthoc_verifier_g3_3seed_ft030/summary.json` (**89.35 ± 0.64% / 2.61 ± 0.36% FT**) preserves support best but is a weaker safety move and no-ops seed 7. `scripts/package_moe_posthoc_verifier.py` packages the preferred 2.8% reranker as `outputs/moe/stage0_7_posthoc_verifier_g3_ft028_package/`; reload evaluation over eval+test reproduced packaged per-seed metrics with **0** max absolute delta and feature schema width **120**. `src/pyrrho/moe/posthoc_verifier.py` exposes the package runtime API (feature construction, manifest/checksum validation, seed loading, accept scoring, and demotion policy), and `src/pyrrho/moe/inference.py` / `scripts/infer_moe_posthoc.py` provide local inference for raw `query` + `contexts` JSONL. Policy compare artifact: `outputs/moe/stage0_7_posthoc_policy_compare_ft028/summary.json`. Best support-retaining ensemble is `trustworthy_quorum_2_of_3`: eval **90.81% / 1.88% FT / 83.77% T recall**, test **90.65% / 1.90% FT / 81.71% T recall**. `trustworthy_unanimous` is safer but too conservative: test **86.95% / 1.07% FT / 68.61% T recall**. Single-seed threshold approximation artifact: `outputs/moe/stage0_7_posthoc_single_seed_threshold_sweep_ft028/summary.json`; eval-selected thresholds targeting quorum eval FT scored held-out **88.55 ± 0.65% / 1.38 ± 0.18% FT / 76.26 ± 1.94% T recall**, so threshold-only single-pass approximation is too conservative. Quorum-distillation artifact: `outputs/moe/stage0_7_posthoc_quorum_distill_ft028/summary.json`; one-seed HGB distillers reached held-out **88.42 ± 0.48% / 1.80 ± 0.33% FT / 76.01 ± 0.85% T recall** with **94.66 ± 0.06%** target agreement, still losing too much support/accuracy. Gap analysis artifact: `outputs/moe/stage0_7_posthoc_quorum_gap_analysis_ft028/summary.json`; quorum target wins over single-pass approximations are concentrated in TRUSTWORTHY -> ABSTAIN/DISPUTED demotions on support patterns (`consistent_chain`, `multi_source_corroboration`, `quantitative_consensus`, `expert_consensus`, `single_authoritative`). Treat the verifier as the current positive guard direction, not a trunk architecture.
- Stage 0 evaluator: `scripts/eval_moe.py`; reproduces saved tiny, pooled route-coupled, token route-coupled, and support-aggregating checkpoints and writes eval/test reports without retraining.
- Seed/upcycling decision: `docs/MOE_UPCYCLING_DECISION_2026-05-26.md`; first upcycling target is `configs/moe/pyrrho_moe_g3_alpha_qwen.yaml` using `Qwen/Qwen3-0.6B-Base`, Qwen's full 151,936-token vocab, 28 layers, KV=8, explicit `head_dim=128`, 24 MoE layers, 48 experts/layer, and FFN dim 1056. Count with the 15 V8 scalar heads: **4.083139633B total / 0.423871537B active inclusive**. This keeps the active budget but requires FFN compression from Qwen's 3072-wide dense FFNs.
- Upcycling inspector: `scripts/upcycle_dense_to_moe.py --inspect-only` validates the Qwen seed/config match and wrote `outputs/moe/upcycling/qwen_alpha_inspect.json`. `--real-weight-smoke` verifies real Qwen safetensors on one layer and wrote `outputs/moe/upcycling/qwen_alpha_real_weight_smoke.json`. Direct-copy surfaces are embeddings, attention, Q/K attention norms, and final norm; FFNs require **3072 -> 1056** compression before cloning into experts.
- FFN compression utility: `src/pyrrho/moe/upcycling.py` selects the strongest seed FFN channels by combined gate/up/down norm and slices `gate_proj`, `up_proj`, and `down_proj` consistently into the 1056-wide target.
- Upcycled seed pack: `scripts/upcycle_dense_to_moe.py --write-seed-pack outputs/moe/upcycling/qwen_alpha_seed_pack --output outputs/moe/upcycling/qwen_alpha_seed_pack_plan.json` materialized a Qwen3-MoE-compatible local seed pack: **30** safetensors shards, **310** tensors, **8.166 GB** total tensor bytes. Shape validation passed via `outputs/moe/upcycling/qwen_alpha_seed_pack/load_shape_report.json`: expected 311 model tensors, manifest has 310, with tied `lm_head.weight` intentionally omitted and mapped to `model.embed_tokens.weight`.
- Generative Qwen SFT path: `scripts/train_moe_qwen_sft.py` is the first MVP-oriented generative trainer for the 4B/A0.4B seed pack. It loads the seed pack with `AutoModelForCausalLM`, disables Qwen3-MoE router aux output for generation/training, trains PEFT LoRA, left-pads prompt-only eval batches, supports asymmetric/classification-token loss weights, optional auxiliary prompt-state classification, target modes `json` / `label-only` / `label-json`, saved-adapter reload, calibrated label-score evaluation, skip-generation full-split scoring, optional bitsandbytes quantized load probes, and auditable `selected_output` JSON separate from raw generation. The broad PEFT reload bug is fixed for this local Qwen-MoE adapter path by bypassing PEFT's Transformers-v5 MoE weight-conversion hook before `PeftModel.from_pretrained`; reload smokes passed on the tiny, 1k, and 4k adapters. Current best checkpoint is the 4k label-first JSON adapter at `outputs/moe/qwen_generative_sft_label_json_mild_weighted_512ctx_4096x512_tau047/` (`target-mode label-json`, weights **2/2/1**, classification-token multiplier **4**, smoothing **0.05**). On the bounded 512-row full-generation eval, it reached **100.0%** JSON parse / **100.0%** label parse / **85.35%** selected accuracy / **5.26%** FT / **81.64%** route / **62.30%** taxonomy. Free generation is slightly higher raw accuracy (**85.55%**) but unsafe (**13.45%** FT), so selected-output/label-score remains required. Full-split skip-generation validation moved the selected threshold to **tau 0.50**: full eval **83.69%** accuracy / **4.29%** FT (`outputs/moe/qwen_label_score_threshold_sweep_4096_full_eval/`), and held-out full test **82.39%** accuracy / **4.44%** FT (`outputs/moe/qwen_label_score_threshold_sweep_4096_full_test/`). Local package hardening is in `models/pyrrho-MoE-g3-mvp/`: `scripts/verify_moe_qwen_sft_package.py` passes, package size is **34** files / **3,349,873,300** bytes including the bundled Q4_K_M GGUF and llama.cpp patch, seeded 32-row packaged full-generation smoke is **100%** JSON parse with **87.50% / 0.00% FT** selected-output accuracy/FT, BF16 CPU smokes pass but peak at **9.84-10.16 GiB** RSS, final downloaded-snapshot verifier/smoke passed after HF upload, and the model card/manifest now distinguish **Project architecture: Pyrrho MoE MVP** from **Runtime / loader architecture: Qwen3MoE-compatible sparse MoE**. `docs/PYRRHO_MOE_MVP_RUN_GUIDE.md` is the current operator path: clone pyrrho, download the HF package, patch/build llama.cpp, run `scripts/smoke_moe_gguf_server.py --decision-mode sequence-label-score`. Local bnb4 CPU is negative for runtime: base load succeeds, but packaged-adapter selected-output inference timed out after **900s** with no output. GGUF conversion is now release-relevant: `scripts/materialize_moe_qwen_sft_merged.py` materialized `outputs/moe/pyrrho_moe_g3_mvp_merged_hf/`, `scripts/convert_qwen3moe_hf_to_gguf.py` produced BF16 and Q4_K_M GGUFs under `outputs/moe/gguf/`, and a patched local llama.cpp Qwen3MoE runtime runs Q4 on CPU at **4.224 GiB** peak RSS for the full held-out selected-label smoke. GGUF raw generation remains unsafe, but full-sequence label scoring passes held-out gates.
- GGUF selected-decision status: first-token scoring is rejected because pyrrho labels are multi-token (`ABSTAIN` 3 tokens, `DISPUTED` 3, `TRUSTWORTHY` 5). The first-token bridge scored **93.75% / 4.17% FT** on a 32-row random smoke but failed on the first **512** ordered test rows at **60.94% / 33.33% FT**; that ordered prefix is a hard slice where HF selected-output also only scores **56.64% / 13.33% FT**. `scripts/smoke_moe_gguf_server.py --decision-mode sequence-label-score` now scores every label token through llama-server mixed string/token prompts. Q4_K_M sequence scoring passed a seeded random 512-row sample at **82.81% / 4.97% FT** with **95.90%** agreement to HF, then passed the full held-out test at **82.15% / 5.27% FT**, **100%** label parse, **4.224 GiB** peak RSS, and **96.38%** HF agreement. Full-test artifacts: `outputs/moe/gguf/smoke_q4_sequence_label_score_full_test_tau050/report.json` and `threshold_sweep_and_hf_agreement.json`.
- Governance wrapper smoke: `scripts/smoke_moe_qwen_wrapper.py --max-length 64 --batch-size 2` loaded `outputs/moe/upcycling/qwen_alpha_seed_pack/` on CUDA, attached pyrrho heads via `src/pyrrho/moe/qwen_governance.py`, and wrote `outputs/moe/upcycling/qwen_alpha_wrapper_smoke.json`. Output shapes passed: governance `[2,3]`, route `[2,8]`, taxonomy `[2,23]`, scalar `[2,15]`; multitask loss computed (`11.4245`) with no-training random heads.
- Stage 1 Qwen trainer: `scripts/train_moe_qwen_heads.py` freezes the Qwen3-MoE trunk by default and trains pyrrho governance/route/taxonomy/scalar heads. It supports deterministic bounded sampling, calibrated TRUSTWORTHY threshold reporting, `heads.pt` eval-only reload, split head/router/trunk LRs, final-dense partial unfreeze, PEFT LoRA, trainable physical-expert residual adapters (`--expert-adapter-r`), trainable semantic-route pooled adapters (`--semantic-adapter-r`), optional g3 governance-logit distillation via `--teacher-logits-dir` / `--loss-distillation`, and oracle-route comparison via `--eval-compare-gold-routes`.
- Stage 1 Qwen result: bounded heads-only learning is real but weak. Best full-eval artifact remains `outputs/moe/qwen_heads_stage1_8192_random_lr3e4_ft12_eval_full/eval_report.json`: raw **57.18%** accuracy / **15.99%** FT; calibrated **54.66%** accuracy / **5.35%** FT at tau **0.54**; route **43.51%**, taxonomy **34.40%**. Negative probes now include internal-router-only (**39.45%** calibrated accuracy / **3.85%** FT), final dense unfreeze (**38.09%** calibrated accuracy on the 512-row slice), attention-only LoRA (**43.55%** calibrated accuracy / **5.22%** FT / **26.17%** route), physical expert adapters + g3 distillation (**50.00%** calibrated accuracy / **4.40%** FT / **24.02%** route on the 512-row slice), and semantic-route adapters + g3 distillation (**44.34%** calibrated accuracy / **1.65%** FT / **26.37%** route on the 512-row slice; only **45.51%** calibrated accuracy when forced to gold routes). Do not scale these Qwen Stage 1 variants as release candidates.

## Validated metrics

### `pyrrho-nano-g1` (encoder, calibrated, 3-seed mean ± std on V5.1 eval, 584 cases)

| Metric | pyrrho-nano-g1 | sklearn baseline | Δ |
|---|---|---|---|
| Overall accuracy | **86.13 ± 0.86%** | 78.7% | **+7.43** |
| False-trustworthy rate | **5.27 ± 0.21%** | 5.7% | **-0.43** (safer) |
| Trustworthy recall | **79.38 ± 1.64%** | 70.0% | **+9.38** |
| Disputed recall | **94.81 ± 1.28%** | 86.1% | **+8.71** |
| Abstain recall | **92.94 ± 1.11%** | 86.5% | **+6.44** |
| CPU inference (est.) | ~30 ms | ~500–2000 ms (5 LLM calls) | ~50× faster |

Every margin is multiple standard deviations larger than seed noise (LOG 2026-05-14).

### `pyrrho-nano-g2` (encoder, calibrated, 3-seed mean ± std on V7 held-out test, 1,050 cases)

Trained on published fitz-gov **V7** default `v7` splits: train=8,400 / validation=1,050 / test=1,050. V7.0.1 is a schema-clean republish of V7.0.0 with the same rows, labels, and splits; no retrain was required. Checkpoint and TRUSTWORTHY threshold are selected on validation; headline numbers below are from the separate held-out test split.

| Metric | pyrrho-nano-g2 | release gate / baseline | Δ |
|---|---|---|---|
| Overall accuracy | **95.24 ± 0.48%** | 78.7% | **+16.54** |
| False-trustworthy rate | **3.48 ± 0.40%** | 5.7% | **-2.22** (safer) |
| Trustworthy recall | **93.66 ± 0.30%** | 70.0% | **+23.66** |
| Disputed recall | **97.00 ± 1.17%** | 86.1% | **+10.90** |
| Abstain recall | **95.25 ± 0.00%** | 86.5% | **+8.75** |
| Trustworthy precision | **94.06 ± 0.66%** | n/a | — |

Validation-split calibrated metrics were **94.92 ± 0.29% accuracy / 2.89 ± 0.26% false-trustworthy**. Every seed passed both gates on validation and held-out test. Training artifacts: `outputs/multi_seed_g2/summary.json`, per-seed best checkpoints under `outputs/multi_seed_g2/seed_*/best_model/`, and per-seed breakdown reports at `outputs/multi_seed_g2/seed_*/eval_report.json`. Release dir: `models/pyrrho-nano-g2/`.

### `pyrrho-nano-g3` (encoder, calibrated, 3-seed mean ± std on V8 held-out test, 2,459 cases)

Trained on the published fitz-gov V8 row set, now available as **V8.0.1** with explicit row-level `meta.modality: "unstructured"` and the same default `v8` splits: train=19,674 / validation=2,459 / test=2,459. Config: `configs/encoder/modernbert_base_g3_v8.yaml`. Checkpoint and TRUSTWORTHY threshold are selected on validation; headline numbers below are from the separate held-out test split.

| Metric | pyrrho-nano-g3 | vs published `g2` |
|---|---|---|
| Overall accuracy | **97.52 ± 0.43%** | **+2.28** |
| False-trustworthy rate | **1.42 ± 0.16%** | **-2.06** (safer) |
| Trustworthy recall | **96.28 ± 0.83%** | **+2.62** |
| Disputed recall | **98.34 ± 0.24%** | **+1.34** |
| Abstain recall | **97.83 ± 0.76%** | **+2.58** |
| Trustworthy precision | **96.87 ± 0.34%** | **+2.81** |

Validation-split calibrated metrics were **97.84 ± 0.07% accuracy / 1.23 ± 0.10% false-trustworthy**. Every seed passed both gates on validation and held-out test. Per-seed held-out calibrated results: seed 42 **97.03% / 1.48% FT** at tau **0.68**; seed 1337 **97.68% / 1.54% FT** at tau **0.58**; seed 7 **97.84% / 1.24% FT** at tau **0.60**. Training artifacts: `outputs/multi_seed_g3_v8/summary.json`, per-seed best checkpoints under `outputs/multi_seed_g3_v8/seed_*/best_model/`, and per-seed breakdown reports at `outputs/multi_seed_g3_v8/seed_*/eval_report.json`. Release dir: `models/pyrrho-nano-g3/`, rebuilt from seed **1337** after packaged smoke selection; published at [`yafitzdev/pyrrho-nano-g3`](https://huggingface.co/yafitzdev/pyrrho-nano-g3), HF commit `397393718985e7bfa101042e89ecc60103e9c447`.

### `pyrrho-nano-g3.1` (multitask encoder, 3-seed mean ± std on V8.1-style held-out test, 2,459 cases)

Published custom package; downloaded-snapshot verified. This is not the old single-head `AutoModelForSequenceClassification` shape. It is a custom ModernBERT multitask encoder with a shared trunk and five output surfaces: governance class from full `(query, evidence)` input, pre-retrieval `query_contract` from query-only input, semantic route/domain from query-only input, taxonomy pattern from full input, and scalar regression for `evidence_sufficiency`, `query_evidence_alignment`, `answer_coverage`, `conflict_density`, `retrieval_retry_value`, and `false_trustworthy_risk`. Config: `configs/encoder/modernbert_base_g3_1_multitask.yaml`. Prepared data: `data/multitask_v8_1_query_contract` with train=19,674 / eval=2,459 / test=2,459.

| Metric | pyrrho-nano-g3.1 |
|---|---:|
| Governance accuracy | **97.84 ± 0.15%** |
| False-trustworthy rate | **0.85 ± 0.07%** |
| Query-contract accuracy | **95.15 ± 0.17%** |
| Query-contract macro F1 | **94.24 ± 0.28%** |
| Route/domain accuracy | **93.41 ± 0.32%** |
| Taxonomy accuracy | **89.26 ± 0.23%** |
| Scalar MAE | **0.0592 ± 0.0005** |

Held-out query-contract recall by class: `evidence_sufficiency` **96.70 ± 0.21%**, `structured_lookup` **94.94 ± 0.26%**, `temporal_grounding` **89.75 ± 0.31%**, `exhaustive_coverage` **96.51 ± 0.31%**, `comparison_coverage` **91.98 ± 2.31%**, and `representative_overview` **92.59 ± 0.00%**. Per-seed best epochs: seed 42 **5**, seed 1337 **5**, seed 7 **5**. Training artifacts: `outputs/pyrrho-nano-g3_1_multitask/summary.json`, per-seed reports at `outputs/pyrrho-nano-g3_1_multitask/seed_*/final_metrics.json`, and per-seed best checkpoints under `outputs/pyrrho-nano-g3_1_multitask/seed_*/best_model/`.

Release package: `models/pyrrho-nano-g3.1/`, built from seed **7** with TRUSTWORTHY threshold **0.39**. The package contains `config.json`, `model.safetensors`, `pyrrho_multitask_config.json`, tokenizer files, `manifest.json`, reports, and README. `src/pyrrho/multitask_inference.py` exposes `PyrrhoMultiTaskPredictor`, which returns governance metadata (`raw_label`, `final_label`, threshold fallback, probabilities, confidence, runner-up, margin, entropy), query-contract prediction, route/domain prediction, taxonomy prediction, and scalars. CPU package verification passed locally via `scripts/package_multitask_encoder.py verify --package-dir models/pyrrho-nano-g3.1 --device cpu` and against the downloaded Hub snapshot at `outputs/hf_download_pyrrho_nano_g3_1_final_verify/`; smoke examples correctly produced TRUSTWORTHY, DISPUTED, and ABSTAIN governance outputs at roughly **100-120 ms/row** on CPU.

fitz-sage integration is now locally present and verified on branch `codex/qwen-enrichment-json-repair`: `fitz_sage/governance/pyrrho.py` points to `yafitzdev/pyrrho-nano-g3.1`, uses a package-local custom multitask ModernBERT loader, and exposes governance, query-contract, route, taxonomy, and scalar metadata. Targeted fitz-sage tests passed (`tests/unit/test_pyrrho.py` + `tests/unit/test_krag_engine.py`: **37 passed**), the full fitz-sage unit suite passed (**1112 passed**, **14 warnings**, **54.95s**), and a live CPU smoke loaded the Hub checkpoint and returned a TRUSTWORTHY decision plus g3.1 head metadata. The all-tests `pytest -q` command timed out after **304s** because it includes heavier non-unit shards; split e2e/load/security/performance tests separately before a release. Do not claim ONNX release status until a dedicated multi-head ONNX export path is built and verified.

### `pyrrho-nano-g3.2` (retrieval-control multitask encoder, 3-seed mean ± std on V8.2 held-out test, 2,459 cases)

Published custom package; downloaded-snapshot verified. This extends the g3.1 multitask shape with V8.2 `routing.retrieval_control` outputs: retrieval action, gap type, answerability shape, retrieval modality, and `evidence_failure_severity`. Config: `configs/encoder/modernbert_base_g3_2_retrieval_control.yaml`. Prepared data: `data/multitask_v8_2_retrieval_control` with train=19,674 / eval=2,459 / test=2,459.

| Metric | pyrrho-nano-g3.2 |
|---|---:|
| Governance accuracy | **97.29 ± 0.22%** |
| False-trustworthy rate | **1.11 ± 0.17%** |
| Query-contract macro F1 | **93.92 ± 0.22%** |
| Route/domain accuracy | **92.59 ± 0.54%** |
| Taxonomy accuracy | **88.80 ± 0.19%** |
| Scalar MAE | **0.0640 ± 0.0005** |
| Retrieval-action macro F1 | **86.69 ± 0.23%** |
| Gap-type macro F1 | **76.75 ± 1.56%** |
| Answerability-shape macro F1 | **75.53 ± 0.54%** |
| Retrieval-modality macro F1 | **55.75 ± 1.75%** |

Every seed passed held-out governance gates. Per-seed held-out calibrated results: seed 42 **97.03% / 1.30% FT** at tau **0.46**; seed 1337 **97.56% / 0.89% FT** at tau **0.34**; seed 7 **97.28% / 1.13% FT** at tau **0.47**. Per-seed best epochs: seed 42 **5**, seed 1337 **4**, seed 7 **5**. Release package: `models/pyrrho-nano-g3.2/`, built from seed **1337** with TRUSTWORTHY threshold **0.34**. Local CPU package verification passed via `python scripts/package_multitask_encoder.py verify --package-dir models\pyrrho-nano-g3.2 --device cpu`; downloaded-snapshot verification passed at `outputs/hf_download_pyrrho_nano_g3_2_verify/release_verify_report.json`. Hub repo: [`yafitzdev/pyrrho-nano-g3.2`](https://huggingface.co/yafitzdev/pyrrho-nano-g3.2), commit `36a5940eb04bbfa0638948ee41953088bafdb0a3`, **13** sibling files including `.gitattributes`, and Hub-reported safetensors parameter count **149,077,330**.

### `pyrrho-nano-g4` (official V9.0.0 multitask encoder, 3-seed mean ± std on held-out test, 4,026 cases)

Published custom package; downloaded-snapshot CPU verified. Trained on published fitz-gov V9.0.0 official query-grouped splits from `yafitzdev/fitz-gov`, config/tag `v9.0.0`, commit `874fd18d4952eec0e72b6df2264f8281615fd350`. Prepared data: `data/multitask_g4_v9_official` with train **32,625** / eval **4,104** / test **4,026** from **40,755** rows: V6 **2,980** + V7 **7,520** + V8 **14,092** + V9 **16,163**. Config: `configs/encoder/modernbert_base_g4_v9_official.yaml`.

| Metric | pyrrho-nano-g4 |
|---|---:|
| Governance accuracy | **97.46 ± 0.09%** |
| False-trustworthy rate | **1.21 ± 0.06%** |
| Query-contract macro F1 | **86.00 ± 0.23%** |
| Route/domain accuracy | **93.53 ± 0.09%** |
| Taxonomy accuracy | **77.95 ± 0.25%** |
| Scalar MAE | **0.0690 ± 0.0003** |
| Retrieval-action macro F1 | **85.92 ± 0.43%** |
| Gap-type macro F1 | **69.85 ± 1.00%** |
| Collapsed answerability macro F1 | **94.92 ± 0.19%** |
| Retrieval-modality macro F1 | **51.94 ± 0.59%** |

Every seed passed held-out governance gates. Per-seed held-out calibrated results: seed 42 **97.39% / 1.16% FT** at tau **0.34**; seed 1337 **97.42% / 1.19% FT** at tau **0.48**; seed 7 **97.57% / 1.27% FT** at tau **0.34**. Per-seed best epochs are all **2**. Release package: `models/pyrrho-nano-g4/`, built from seed **1337** because it had the strongest validation composite score and best balanced auxiliary-head tradeoff. Local CPU package verification passed via `python scripts/package_multitask_encoder.py verify --package-dir models\pyrrho-nano-g4 --device cpu`; repository smoke passed via `pytest tests\test_smoke.py -v` (**11 passed**, **2 warnings**). Hub repo: [`yafitzdev/pyrrho-nano-g4`](https://huggingface.co/yafitzdev/pyrrho-nano-g4), commit `43789ecdda918c9382e3f42aebf72f57bb82944d`, **13** sibling files including `.gitattributes`, and Hub-reported safetensors parameter count **149,071,947**. Downloaded-snapshot verification passed at `outputs/hf_download_pyrrho_nano_g4_verify/release_verify_report.json`. Training summary: `outputs/pyrrho-nano-g4_v9_official/summary.json`.

### `pyrrho-nano-g4-target100` (local V9 target-100 multitask encoder, 3-seed mean ± std on held-out test, 4,087 cases)

Local-only pre-public candidate; do not publish directly as `pyrrho-nano-g4`. It was prepared from the QA-gated local fitz-gov V9 target-100 vault before the public split was finalized: `data/multitask_g4_v9_target100` has train **32,617** / eval **4,051** / test **4,087** from **40,755** rows (**24,592** published V8.2 + **16,163** local V9). Public fitz-gov V9.0.0 uses official query-grouped splits train **32,625** / validation **4,104** / test **4,026**, and the official-split `pyrrho-nano-g4` run now supersedes this package. Config: `configs/encoder/modernbert_base_g4_v9_target100.yaml`.

| Metric | pyrrho-nano-g4-target100 |
|---|---:|
| Governance accuracy | **97.05 ± 0.11%** |
| False-trustworthy rate | **1.36 ± 0.13%** |
| Query-contract macro F1 | **85.42 ± 0.20%** |
| Route/domain accuracy | **93.83 ± 0.23%** |
| Taxonomy accuracy | **77.14 ± 0.39%** |
| Scalar MAE | **0.0710 ± 0.0004** |
| Retrieval-action macro F1 | **84.99 ± 0.09%** |
| Gap-type macro F1 | **69.79 ± 0.36%** |
| Collapsed answerability macro F1 | **94.69 ± 0.11%** |
| Retrieval-modality macro F1 | **51.98 ± 0.03%** |

Every seed passed held-out governance gates. Per-seed held-out calibrated results: seed 42 **97.16% / 1.21% FT** at tau **0.34**; seed 1337 **96.94% / 1.42% FT** at tau **0.34**; seed 7 **97.04% / 1.45% FT** at tau **0.34**. Per-seed best epochs are all **2**. Local package: `models/pyrrho-nano-g4-target100/`, rebuilt from seed **42** after 3-seed validation because it had the strongest held-out governance accuracy and lowest false-TRUSTWORTHY rate. CPU package verification passed via `python scripts/package_multitask_encoder.py verify --package-dir models\pyrrho-nano-g4-target100 --device cpu`. Training summary: `outputs/pyrrho-nano-g4_v9_target100/summary.json`.

### `pyrrho-nano-g2.1-v8-probe` (encoder, calibrated, 3-seed mean ± std on local V7+V8 mixed held-out test, 1,107 cases)

Local experiment only. Data prep preserved the published V7 train/validation/test contract and appended the **525-row V8** cohort from the local fitz-gov vault by manifest assignment: **train +414 / eval +54 / test +57**, producing `data/processed_v8_probe` with train=8,814 / eval=1,104 / test=1,107.

| Metric | pyrrho-nano-g2.1-v8-probe | vs published `g2` |
|---|---|---|
| Overall accuracy | **95.51 ± 0.43%** | **+0.27** |
| False-trustworthy rate | **3.56 ± 0.38%** | **+0.08** (slightly worse) |
| Trustworthy recall | **94.72 ± 0.29%** | **+1.06** |
| Disputed recall | **96.21 ± 0.27%** | **-0.79** |
| Abstain recall | **95.71 ± 0.96%** | **+0.46** |

Automotive ECU/test-management OOD probe on the recovered 10-case fixture stays exact-string OOD against both `data/processed_v7` and `data/processed_v8_probe` (**0/10 exact query matches** in each). Mean calibrated score improved from **7.00/10** on `g2` to **8.33/10** on `g2.1-v8-probe`; per-seed scores moved **7/10 -> 8/10** (seed 42), **6/10 -> 9/10** (seed 1337), and **8/10 -> 8/10** (seed 7). Biggest gains were `resolved_candidate_selection`-style and wrong-release abstain behavior (`ecu_02`, `ecu_07`); explicit PASS/FAIL conflict resolution (`ecu_04`) improved only **0/3 -> 1/3**.

### `pyrrho-nano-g2.1-v8-verdict-patch` (failed local ablation, 1,115-case mixed held-out test)

Local experiment only. Added 105 hard final-verdict PASS/FAIL conflict rows to the V8 probe, producing `data/processed_v8_verdict_patch` with train=8,901 / eval=1,114 / test=1,115. Three-seed held-out test passed gates at **94.92 ± 0.41% accuracy / 4.08 ± 0.92% false-trustworthy**, but it is not a release candidate. ECU OOD mean dropped from the initial V8 probe's **8.33/10** to **7.33/10**. The target `ecu_04_disputed_dtc_powercycle` improved **1/3 -> 2/3**, but nearby controls regressed: `ecu_01` **2/3 -> 1/3**, `ecu_02` **2/3 -> 1/3**, and `ecu_07` **2/3 -> 0/3**. Artifact: `outputs/automotive_ood_probe/comparison_v8_verdict_patch.json`.

### `pyrrho-nano-g2.2` (local V7+840-row V8 balanced-controls retrain, 1,132-case mixed held-out test)

Local experiment only. `configs/encoder/modernbert_base_g2_2.yaml` retrained ModernBERT on `data/processed_v8_balanced_controls`, preserving the published V7 split contract and appending the 840-row V8 cohort by manifest (**+661 train / +97 eval / +82 test**). Prepared split sizes are train=9,061 / eval=1,147 / test=1,132.

| Metric | `pyrrho-nano-g2.2` | vs published `g2` |
|---|---|---|
| Overall accuracy | **95.49 ± 0.15%** | **+0.25** |
| False-trustworthy rate | **3.06 ± 0.61%** | **-0.42** (safer) |
| Trustworthy recall | **93.61 ± 0.61%** | -0.05 |
| Disputed recall | **96.27 ± 0.53%** | -0.73 |
| Abstain recall | **96.91 ± 0.44%** | +1.66 |

ECU OOD comparison artifact: `outputs/automotive_ood_probe/comparison_g2_2.json`. Exact-query leakage check stayed clean: **0/10** exact query matches in `data/processed_v7`, `data/processed_v8_probe`, and `data/processed_v8_balanced_controls`. OOD mean is **8.00/10** (per-seed **8/10, 8/10, 8/10**), which improves over published `g2` (**7.00/10**) and the failed verdict patch (**7.33/10**) but does not beat the original 525-row V8 probe (**8.33/10**). It fixes `ecu_04_disputed_dtc_powercycle` completely (**1/3 -> 3/3** vs V8 probe) and `ecu_01` (**2/3 -> 3/3**), but regresses `ecu_02_trustworthy_acceptance_run` (**2/3 -> 0/3**) and `ecu_07_abstain_wrong_ecu_release` (**2/3 -> 0/3**).

### Aviation maintenance / airworthiness OOD probe

The 10-case aviation maintenance probe is exact-query OOD against all three checked processed datasets: **0/10** exact query matches in `data/processed_v7`, `data/processed_v8_probe`, and `data/processed_v8_balanced_controls`. Artifact: `outputs/aviation_ood_probe/comparison_g2_g21_g22.json`.

| Run | Mean score | Seed scores (42 / 1337 / 7) |
|---|---:|---|
| `g2` | **7.00/10** | 7/10, 6/10, 8/10 |
| `g2.1-v8-probe` | **8.00/10** | 8/10, 9/10, 7/10 |
| `g2.2` | **8.67/10** | 9/10, 7/10, 10/10 |

This did **not** uncover a grave new field-specific taxonomy gap. The persistent hard case is `air_02_trustworthy_superseded_sb_resolved` (**1/3** on `g2.2`), which looks like the existing `resolved_candidate_selection` / superseded-candidate boundary. The other residuals are one-seed `authority_status_conflict` drift (`air_05`) and revision mismatch (`air_10`, **2/3** on `g2.2`).

### `pyrrho-small-g1` (SLM, plain SFT, 3-seed mean ± std on same eval split)

| Metric | pyrrho-small-g1 | vs `nano-g1` | vs sklearn baseline |
|---|---|---|---|
| Overall accuracy | **90.01 ± 0.55%** | **+3.88** | **+11.31** |
| False-trustworthy rate | **12.13 ± 1.27%** | **+6.86 (worse)** | **+6.43 (worse)** — **fails gate** |
| Trustworthy recall | **92.09 ± 0.19%** | +12.71 | +22.09 |
| Disputed recall | **87.16 ± 1.54%** | -7.65 | +1.06 |
| Abstain recall | **88.08 ± 2.23%** | -4.86 | +1.58 |
| Tier0 sanity accuracy | **99.44 ± 0.96%** (60-case set) | vs ~83% (encoder) | n/a |
| Decode-time fallback rate | **0.00%** (every case produced a parseable label) | — | — |

Headline finding: pre-trained world knowledge + reasoning depth genuinely lifts overall accuracy and nearly-perfects tier0, but the SLM systematically over-predicts TRUSTWORTHY because plain SFT has no safety-asymmetric signal. 12.13% FT rate is stable across seeds (11.40 / 11.40 / 13.60), so it's a recipe-level finding, not noise.

### `pyrrho-small-g1.1` (SLM, same SFT + class_weights=[2.3, 2.3, 1.0] + label_smoothing=0.15, 3-seed mean ± std)

Recipe-fix re-spin of g1 with the encoder's anti-FT regularization transplanted onto the token-level CE loss (per-example weighting in `WeightedLossSFTTrainer`).

| Metric | pyrrho-small-g1.1 | vs g1 | vs `nano-g1` |
|---|---|---|---|
| Overall accuracy | **89.55 ± 1.40%** | -0.46 | +3.42 |
| False-trustworthy rate | **9.31 ± 1.06%** | **-2.82 (improved)** | +4.04 — **still fails gate by ~3.6 pts** |
| Trustworthy recall | 89.00 ± 2.45% | -3.09 (model less aggressive on T — by design) | +9.62 |
| Disputed recall | **91.60 ± 1.13%** | **+4.44 (improved)** | -3.21 |
| Abstain recall | 88.81 ± 2.56% | +0.73 | -4.13 |
| Tier0 sanity accuracy | **96.67 ± 0.00%** | -2.77 | vs ~83% (encoder) |
| Decode-time fallback rate | **0.00%** | — | — |
| Per-seed FT rate | 8.09 / 9.93 / 9.93 | — | — |

Direction is exactly what the recipe predicts: model is less aggressive on TRUSTWORTHY (TR ↓ 3.09), more aggressive on DISPUTED (DR ↑ 4.44), FT drops ~3 pts. But the absolute FT of 9.31% is still ~3.6 pts above the 5.7% gate — the encoder's [2.3, 2.3, 1.0] + 0.15 smoothing recipe lands 5.27% on the encoder but only 9.31% on the SLM. Token-level CE on the assistant turn diffuses the safety pressure across many tokens (~11/example: 6 think-block + 3–5 label tokens + im_end) while the encoder's class-weighted CE on a single classification head concentrates it. Bumping to more aggressive interventions (e.g., class_weights=[5, 5, 1], label_smoothing=0.25, or moving to ft_penalized_accuracy selection / threshold-based post-processing / DPO) is the next lever set for a `small-g1.2`.

## Hard V8 Dataset Contract

V8 must be a coherent SDGP dataset, not a compatibility layer. The
source-of-truth contract is
`C:/Users/yanfi/PycharmProjects/fitz-gov/docs/V8_SCHEMA_CONTRACT.md`.

- No legacy shims, no compatibility configs, no old pre-SDGP report axes.
- V8 keeps the current V7.0.1 SDGP row shape: `id`, `version`, `input`, `governance`, `taxonomy`, `routing`, `meta`, `evaluation` plus local `_vault` provenance.
- Taxonomy gaps are first-class `taxonomy.pattern` values, not `taxonomy.subpattern` or side-channel fields.
- Existing 10,500 rows are not rewritten for this additive taxonomy expansion.
- New V8 rows use the existing cohort marker: `version: "fitz-gov-8.0"` and `meta.dataset_version: "v8"`.
- Any future V8 testcase addition must follow `C:/Users/yanfi/PycharmProjects/fitz-gov/docs/SDGP_TESTCASE_ADDITION_CYCLE.md`: candidate rows get structural dry-run and offline blind-label QA before active-vault merge. The current local Qwen QA settings are pinned there (`qwen3.6-35b-a3b@q5_k_s`, `max_tokens=2048`, `request_timeout_s=300`, `temperature=0.0`).
- Initial V8 taxonomy-gap implementation is in fitz-gov: five new primary patterns (`resolved_candidate_selection`, `verdict_conflict`, `authority_status_conflict`, `version_build_mismatch`, `missing_execution_result`) expanded across 7 current domains x 3 difficulties = 105 cells. The active local V8 set is now **14,092 QA-clean rows**: original 840 clean rows + 3,360 repaired Claude-handoff rows + 5,198 target-40 rows + 4,694 target-50 rows. Plan file: `C:/Users/yanfi/PycharmProjects/fitz-gov/docs/V8_TAXONOMY_EXPANSION_PLAN.md`.
- Expansion rows are generated, merged, structurally audited, repaired, and blind-label clean locally: vault is **24,592 rows** = 10,500 V6/V7 + **14,092 V8**. Current clean manifest is `C:/Users/yanfi/PycharmProjects/fitz-gov/data/sdgp_v8_qa/blind_label_manifest.jsonl`. Training-schema audit is **14,092/14,092 complete** at `C:/Users/yanfi/PycharmProjects/fitz-gov/data/sdgp_v8_qa/training_schema_summary.json`.
- The Claude-generated candidate handoff at `C:/Users/yanfi/PycharmProjects/fitz-gov/data/sdgp_handoff_v8_candidate_20260525_claude_expand/` has been repaired and merged. Raw intake initially failed structural dry-run (**1,915 accepted / 0 existing / 624 rejected**). The normalized pack had **124** Codex subagent triage rows; those rows were replaced with deterministic V8 template cases in `subagent_outputs_patched_124_template/`. Patched candidate QA passed **3,360/3,360 agreement**, **0 missing / 0 invalid / 0 error**, **0 triage**, then merged as batch `v8_candidate_20260525_claude_expand_patched_124_template` (**3,360 added / 0 duplicate**).
- Merge recheck on 2026-05-25 confirmed there are no pending Claude rows to add: active vault contains **3,360** rows from the patched Claude batch, and the separate **315-row** `standalone_35cell_topup_outputs` dry-run reports **0 accepted / 315 existing / 0 rejected**. Do not re-merge either pack.
- Gap detector check after merge: the active five V8 gap patterns have **105/105 cells at 40/cell** and **0 gap**. The Claude patched pack alone is not full 40/cell coverage: `authority_status_conflict` and `missing_execution_result` contribute **35/cell**, while `resolved_candidate_selection`, `verdict_conflict`, and `version_build_mismatch` contribute **30/cell**. The preexisting V8 rows supply the remaining 5 or 10 rows/cell. Report: `C:/Users/yanfi/PycharmProjects/fitz-gov/data/sdgp_v8_qa/gap_report_20260525_after_claude_patch.json`.
- Whole-dataset target **40/cell is complete**. Across all **483** canonical generation cells, target 40 has **483/483** cells at target and **0** gap. Report: `C:/Users/yanfi/PycharmProjects/fitz-gov/data/sdgp_v8_qa/full_dataset_gap_target40_after_merge.json`.
- Whole-dataset target-40 pack is generated, Codex-subagent blind-label QA clean, and merged. Candidate pack: `C:/Users/yanfi/PycharmProjects/fitz-gov/data/sdgp_handoff_v8_target40/subagent_outputs/` (**174** `batch_*.jsonl` files / **5,198** rows). Final Codex blind score: `C:/Users/yanfi/PycharmProjects/fitz-gov/data/sdgp_qa_v8_target40/score_codex_subagents_combined/` = **5,198/5,198 agreement**, **0 missing / 0 invalid / 0 error**, **0 triage**. Merge batch: `v8_target40_template_20260526`, **5,198 added / 0 duplicate**. Pre-merge backup: `C:/Users/yanfi/PycharmProjects/fitz-gov/data/sdgp_vault_v51_enriched/cases.before_v8_target40_merge_20260526_005837.jsonl`.
- Whole-dataset target **50/cell is complete**. The target-50 pack at `C:/Users/yanfi/PycharmProjects/fitz-gov/data/sdgp_handoff_v8_target50/subagent_outputs/` has **157** `batch_*.jsonl` files / **4,694** rows. Initial Codex-subagent blind QA found **82** triage rows; template repairs fixed `factual_contradiction`, `numerical_conflict`, and `resolved_candidate_selection`. Final Codex blind score: `C:/Users/yanfi/PycharmProjects/fitz-gov/data/sdgp_qa_v8_target50/score_codex_subagents_combined/` = **4,694/4,694 agreement**, **0 missing / 0 invalid / 0 error**, **0 triage**. Merge batch: `v8_target50_template_20260526`, **4,694 added / 0 duplicate**. Pre-merge backup: `C:/Users/yanfi/PycharmProjects/fitz-gov/data/sdgp_vault_v51_enriched/cases.before_v8_target50_merge_20260526_013413.jsonl`. Active target-50 report: `C:/Users/yanfi/PycharmProjects/fitz-gov/data/sdgp_v8_qa/full_dataset_gap_target50_after_merge.json` = **483/483** cells at target, **0** gap.

## Future modality expansion decision

Structured-data and code governance should live under the **same fitz-gov benchmark family**, not separate benchmark repos. Future modality-expanded releases should keep the row-level `meta.modality` axis with allowed values `unstructured`, `structured`, and `code`; current V8.0.1 rows explicitly carry `meta.modality: "unstructured"`. Keep `meta.modality` separate from `routing.expert_fired`: modality describes evidence representation, while route remains the semantic/domain expert target.

Current decision / stop point (2026-05-29 afternoon): **freeze structured/code modality work for now and resume pyrrho-MoE.** The retry-patch branch is already far above the original "good enough to keep moving" bar for local controls: held-out **98.62 ± 0.15% accuracy / 1.06 ± 0.05% FT**, code OOD **99.07 ± 1.60% / 1.39 ± 2.41% FT**, and tabular OOD **93.52 ± 8.93% / 1.39 ± 2.41% FT**. This is not publishable yet because patch labels are label-trusted only, and blind-label QA is intentionally deferred for speed. Do not generate more modality rows, train more simple specialists, or tune more modality thresholds unless the branch is explicitly reopened. No merge/publish before full blind-label QA passes.

Current candidate state: fitz-gov has **20,000 QA-clean structured/code candidate rows** under `data/_workspaces/handoff/modality_{structured,code}_v1_20260527/cases.jsonl` (**10,000 structured**, **10,000 code**, class-balanced at **6,668 ABSTAIN / 6,666 DISPUTED / 6,666 TRUSTWORTHY**). Codex blind QA over the full pack is clean: `data/_workspaces/qa/modality_v1_codex_blind_full_repair1_20260528/score_codex_subagents_combined/` = **20,000/20,000 agreement**, **0** triage, **0/0/0** missing/invalid/error. These rows are still candidate-only and are not merged into active `data/fitz-gov/cases.jsonl`.

`scripts/modality_candidate_probe.py` builds pyrrho-side manifests and scores `models/pyrrho-nano-g3` on these candidate rows. Artifacts are under `outputs/modality_candidate_probe/g3_release/`: `full_20k` (**20,000** rows), `balanced_pattern` (**14,628** rows, 636/pattern), and `balanced_modality_pattern` (**13,938** rows, 303/modality-pattern cell). Results confirm a severe modality gap, not just bucket skew: full pack **52.69%** calibrated accuracy / **51.69%** false-TRUSTWORTHY; balanced-pattern **52.24% / 50.24% FT**; balanced modality-pattern **52.22% / 50.79% FT**. Full-pack modality split: code **49.35%** accuracy / **71.38% FT**; structured **56.02%** / **31.99% FT**.

`scripts/prepare_data.py` now has a modality-isolated candidate append path: repeatable `--append-candidate-pack`, optional `--candidate-selection-manifest`, deterministic query-grouped candidate splitting, candidate duplicate/query-overlap guards, `modality` in processed rows, and `prep_summary.json`. `scripts/eval_report.py` now reports modality breakdowns. Generated local processed dirs are `data/processed_v8_plus_structured_candidate`, `data/processed_v8_plus_code_candidate`, and `data/processed_v8_plus_structured_code_candidate`.

Seed-42 controlled retraining probes are under `outputs/modality_retraining/`, with aggregate summary at `outputs/modality_retraining/summary_seed42.json`. Mixed held-out test results: structured-only **98.17% accuracy / 0.98% FT**, code-only **98.00% / 1.27% FT**, joint structured+code **98.09% / 1.09% FT**. Candidate held-out modality rows scored **100.00% / 0.00% FT** in all branches, showing the candidate distribution is trainable.

The joint structured+code branch now has a 3-seed stability result at `outputs/modality_retraining/structured_code_3seed_summary.json` with per-seed reports under `outputs/modality_retraining/structured_code_seed{42,1337,7}/eval_report.json`. Mixed held-out test passes aggregate gates at **98.34 ± 0.23% accuracy / 1.14 ± 0.33% FT**. Structured rows are **100.00 ± 0.00% / 0.00 ± 0.00% FT** (n=996/test seed) and code rows are **100.00 ± 0.00% / 0.00 ± 0.00% FT** (n=999/test seed). The unstructured slice is the tradeoff: **96.99 ± 0.41% accuracy / 2.03 ± 0.60% FT** (n=2,459/test seed), worse on FT than published g3's V8 baseline (**97.52 ± 0.43% / 1.42 ± 0.16% FT**). Current conclusion: do not merge/publish; same-generator candidate splits prove trainability, not robust modality generalization.

Hard structured OOD sanity check: the existing hand-authored `scripts/tabular_ood_probe.py` now has joint-branch 3-seed results at `outputs/tabular_ood_probe/structured_code_3seed_summary.json`. Frozen g3 baseline was **55.56% accuracy / 45.83% FT** on this 36-row probe; the joint branch improves to **83.33 ± 4.81% / 4.17 ± 7.22% FT**, but seed 7 still predicts TRUSTWORTHY on all three `missing_execution_result` serializations. This is a positive direction, not release clearance.

Hard code OOD sanity check: `scripts/code_ood_probe.py` adds 12 hand-authored code scenarios x 3 serializations (36 rows) covering exact support, implementation/config conflicts, wrong symbol, wrong API version, missing audit field, and missing test result. Frozen g3 baseline is **58.33% accuracy / 62.50% FT**. The joint branch improves the mean but is still unsafe: **77.78 ± 14.70% / 29.17 ± 27.32% FT** at `outputs/code_ood_probe/structured_code_3seed_summary.json`. Seed 42 is promising (**88.89% / 4.17% FT**), seed 1337 is mixed (**83.33% / 25.00% FT**), and seed 7 is effectively not rescued (**61.11% / 58.33% FT**). Main leakage is ABSTAIN code evidence being over-trusted, especially wrong-symbol/version/missing-result cases. The label-trusted joint+patch branch improved this probe to **95.37 ± 3.21% accuracy / 6.94 ± 4.81% FT** at `outputs/code_ood_probe/structured_code_patch_3seed_summary.json`; every remaining false-TRUSTWORTHY failure was a DISPUTED retry-limit code/config numerical conflict. The second retry-conflict patch improves the 3-seed OOD aggregate to **99.07 ± 1.60% accuracy / 1.39 ± 2.41% FT** at `outputs/code_ood_probe/structured_code_retry_patch_3seed_summary.json`; targeted `constant_config_conflict` is now **100.00 ± 0.00% / 0.00 ± 0.00% FT** across seeds. The only remaining OOD failure is seed 42 on `code_10_missing_audit__diff_context`, an inherited `missing_specific_field` / `evidence_absent` ABSTAIN case.

Code modality axis contract/audit: `docs/CODE_MODALITY_AXES.md` now clarifies that code language is a modality-specific coverage axis, not a replacement for `routing.expert_fired`. `scripts/audit_code_modality_axes.py` audited the 10,000-row code candidate pack and wrote `outputs/code_modality_axis_audit/modality_code_v1_20260527/`. Findings: **10,000/10,000** rows carry `meta.modality: "code"`; semantic routes are still subject domains (**7,144** technology / **1,428** finance / **1,428** science); current hard-code OOD target gaps are **0** direct control-flow-support rows and **0** missing-specific-field rows; **239/10,000** rows have extension/syntax mismatch flags after per-context syntax checking, mostly synthetic language rotation such as `.sql`/`.yaml`/`.go` paths containing JS-like `function ...` snippets.

Targeted code modality patch v1: fitz-gov now has a candidate-only patch at `C:/Users/yanfi/PycharmProjects/fitz-gov/data/_workspaces/handoff/modality_code_patch_v1_20260528/`, generated by `scripts/sdgp_generate_code_modality_patch.py` in fitz-gov. It contains **720** syntax-matched code rows, label-balanced at **240/240/240**, with targeted mechanisms: control-flow support, decorator/guard support, transaction order support, missing-specific-field ABSTAIN, wrong-symbol/version ABSTAIN, test-definition-without-run ABSTAIN, unresolved same-environment runtime status conflicts, docs/code conflicts, and test/implementation conflicts. Structural validation passes with **0 errors**. Pyrrho audits are at `outputs/code_modality_axis_audit/modality_code_patch_v1_20260528/` and `outputs/code_modality_axis_audit/modality_code_v1_plus_patch_v1_20260528/`: the patch-only syntax mismatch rate is **0/720**, and original+patch has **10,720** rows with **0** audited hard-code OOD target gaps. Blind-label QA was intentionally paused by user decision: Qwen/LM Studio checked targeted slices clean (**10/10** config/runtime after repair, **10/10** wrong-symbol) and a partial full run reached **115** predictions before stopping. Current working assumption is "labels trusted for local controls; validate later."

Label-trusted patch retraining control: prepared `data/processed_v8_plus_structured_code_patch_candidate` from published V8.0.1 plus the 10k structured pack, 10k original code pack, and 720-row code patch. Splits are **train=36,258 / eval=4,529 / test=4,525**; patch rows split **588 train / 66 eval / 66 test**. Seeds **42/1337/7** completed at `outputs/modality_retraining/structured_code_patch_seed{42,1337,7}/`; aggregate summary is `outputs/modality_retraining/structured_code_patch_3seed_summary.json`. Calibrated held-out test is **98.52 ± 0.13% accuracy / 1.06 ± 0.22% FT** overall; modality slices are code **100.00 ± 0.00% / 0.00 ± 0.00% FT** (n=1,071), structured **100.00 ± 0.00% / 0.00 ± 0.00% FT** (n=995), unstructured **97.28 ± 0.25% / 1.92 ± 0.40% FT** (n=2,459). This is not release evidence yet because the patch is not blind-label-clean.

Retry-conflict patch control: fitz-gov has a second candidate-only patch at `C:/Users/yanfi/PycharmProjects/fitz-gov/data/_workspaces/handoff/modality_code_retry_conflict_patch_v1_20260529/`, generated by `scripts/sdgp_generate_code_retry_conflict_patch.py`. It contains **360** code rows: **120** retry-limit code/config numerical conflicts, **120** code/config agreement controls, and **120** wrong-service ABSTAIN controls, serialized evenly across `code_excerpt`, `review_packet`, and `diff_context`; structural validation passes with **0 errors**. Pyrrho prepared `data/processed_v8_plus_structured_code_retry_patch_candidate` offline from the local V8 split manifest plus all candidate packs; splits are **train=36,555 / eval=4,584 / test=4,533**. Seeds **42/1337/7** completed at `outputs/modality_retraining/structured_code_retry_patch_seed{42,1337,7}/`; aggregate summaries are `outputs/modality_retraining/structured_code_retry_patch_3seed_summary.json`, `outputs/modality_retraining/structured_code_retry_patch_3seed_report.md`, `outputs/code_ood_probe/structured_code_retry_patch_3seed_summary.json`, and `outputs/tabular_ood_probe/structured_code_retry_patch_3seed_summary.json`. Calibrated held-out test is **98.62 ± 0.15% accuracy / 1.06 ± 0.05% FT** overall; modality slices are code **100.00 ± 0.00% / 0.00 ± 0.00% FT** (n=1,083), structured **100.00 ± 0.00% / 0.00 ± 0.00% FT** (n=991), unstructured **97.47 ± 0.28% / 1.94 ± 0.09% FT** (n=2,459). The 36-row code OOD probe is **99.07 ± 1.60% / 1.39 ± 2.41% FT**; the 36-row tabular OOD probe is **93.52 ± 8.93% / 1.39 ± 2.41% FT**. `constant_config_conflict` is **100.00 ± 0.00% / 0.00 ± 0.00% FT** across all three seeds. Remaining OOD failures are seed 42 `code_10_missing_audit__diff_context` and one tabular seed-42 metric-mismatch false-TRUSTWORTHY; seed 7 is conservative on tabular TRUSTWORTHY support. This remains the best local baseline.

Missing-evidence patch exposed-seed control: fitz-gov has a third candidate-only patch at `C:/Users/yanfi/PycharmProjects/fitz-gov/data/_workspaces/handoff/modality_missing_evidence_patch_v1_20260529/`, generated by `scripts/sdgp_generate_missing_evidence_patch.py`. It contains **360** rows: **180** code diff-context rows and **180** structured rows, label-balanced at **120/120/120** across `ABSTAIN` / `DISPUTED` / `TRUSTWORTHY`; structural validation passes with **0 errors**. Pyrrho prepared `data/processed_v8_plus_structured_code_missing_evidence_patch_candidate` offline from the local V8 split manifest plus all candidate packs; splits are **train=36,819 / eval=4,610 / test=4,603**. Exposed seeds **42/7** completed at `outputs/modality_retraining/structured_code_missing_evidence_patch_seed{42,7}/`; aggregate summaries are `outputs/modality_retraining/structured_code_missing_evidence_patch_2seed_summary.json`, `outputs/modality_retraining/structured_code_missing_evidence_patch_2seed_report.md`, `outputs/code_ood_probe/structured_code_missing_evidence_patch_2seed_summary.json`, and `outputs/tabular_ood_probe/structured_code_missing_evidence_patch_2seed_summary.json`. Calibrated held-out test is **98.70 ± 0.18% accuracy / 0.75 ± 0.25% FT** overall; code and structured test slices are **100.00 ± 0.00% / 0.00 ± 0.00% FT**, and unstructured is **97.56 ± 0.35% / 1.39 ± 0.46% FT**. The targeted seed-42 `missing_specific_field` FT is fixed, but the branch is a mixed tradeoff rather than a release candidate: code OOD drops to **93.06 ± 5.89% / 2.08 ± 2.95% FT** because seed 42 over-demotes TRUSTWORTHY code support, while seed 7 still false-trusts one retry-limit conflict; tabular OOD is **87.50 ± 13.75% / 0.00 ± 0.00% FT** with large seed spread.

Threshold/policy sweep: `outputs/modality_policy_sweep/retry_vs_missing_evidence/` compares retry-patch and missing-evidence probabilities on the saved code/tabular OOD probes without retraining, LM Studio, or API calls. The diagnostic best fixed OOD threshold for retry-patch is tau **0.73**, giving code OOD **100.00 ± 0.00% / 0.00 ± 0.00% FT** and tabular OOD **93.52 ± 11.23% / 0.00 ± 0.00% FT** on the small probes. The missing-evidence branch's best fixed OOD threshold is tau **0.34**, with lower combined OOD accuracy (**93.06%** vs retry-patch **96.76%**). More importantly, missing-evidence seed 42 wants a lower threshold to recover TRUSTWORTHY support while seed 7 wants a higher threshold to demote the remaining retry-limit conflict, so threshold tuning does not make it a clean successor.

Specialist routing diagnostic: `scripts/modality_specialist_compare.py` now compares fixed checkpoints under simple routing by the existing `modality` column. The smallest useful comparison is complete at `outputs/modality_specialist_compare/retry_patch_seed42_router/`: keep unstructured on the retry-patch seed-42 generalist, route code rows to the existing seed-42 code-only specialist, and route structured rows to the existing seed-42 structured-only specialist. It does not justify replacing or augmenting retry-patch with those older specialists. On the retry-patch mixed held-out test, seed-42 retry generalist was **98.74% / 1.07% FT**; routing code rows regressed to **98.26% / 1.46% FT** because code-slice quality fell to **97.97% / 1.66% FT**; routing structured rows tied on in-distribution structured candidate rows. On OOD, the code-only specialist was safer but too conservative (**86.11% / 0.00% FT**, TRUSTWORTHY recall **58.33%**) and the structured-only specialist was unsafe (**91.67% / 8.33% FT**). Treat retry-patch as the current local baseline; a future specialist would need to be patch-aware and evaluated as a new local control.

Patch-aware code-specialist diagnostic: `scripts/filter_processed_modalities.py` filtered the retry-patch processed set to `data/processed_v8_plus_code_retry_patch_candidate` (**train=28,564 / eval=3,566 / test=3,542**) with only `unstructured` and `code` rows. Seed 42 trained at `outputs/modality_retraining/code_retry_patch_seed42/`. The specialist is perfect on held-out candidate code rows (**100.00% / 0.00% FT**) but weaker on unstructured (**97.48% / 2.13% FT**), giving **98.25% / 1.49% FT** on the code+unstructured test. Code OOD ties the retry-patch seed-42 generalist at **97.22% / 4.17% FT**, but the miss moves to retry-limit `constant_config_conflict` on `code_excerpt`. Routing code rows to this patch-aware specialist ties, not beats, the retry-patch seed-42 generalist on the mixed held-out test (**98.74% / 1.07% FT**). There is no local evidence to replace or augment the joint generalist with a separate code encoder.

Patch-aware structured-specialist diagnostic: `scripts/filter_processed_modalities.py` also filtered the retry-patch processed set to `data/processed_v8_plus_structured_retry_patch_candidate` (**train=27,665 / eval=3,477 / test=3,450**) with only `unstructured` and `structured` rows. Seed 42 trained at `outputs/modality_retraining/structured_retry_patch_seed42/`. The specialist is perfect on held-out candidate structured rows (**100.00% / 0.00% FT**) but does not improve mixed routing: `outputs/modality_specialist_compare/retry_patch_seed42_patch_aware_structured_router/` ties the retry-patch seed-42 generalist at **98.74% / 1.07% FT**. Hand-authored tabular OOD is unsafe at **88.89% / 16.67% FT**, with ABSTAIN metric-mismatch and missing-result rows false-trusted. There is no local evidence to replace or augment the joint generalist with a separate structured encoder.

Modality-threshold policy diagnostic: `scripts/modality_threshold_sweep.py` selected per-modality TRUSTWORTHY thresholds on eval and applied them to held-out test for the retry-patch 3-seed joint-generalist branch. Artifacts: `outputs/modality_threshold_sweep/retry_patch_3seed/`. Global retry-patch thresholds scored **98.62 ± 0.12% / 1.06 ± 0.04% FT** on held-out test. Per-modality thresholds selected to avoid eval FT regression scored **98.61 ± 0.11% / 1.04 ± 0.05% FT**, a **-0.01 pp** accuracy trade for **-0.02 pp** FT. The only threshold movement was seed 42 unstructured **0.34 -> 0.53**; seeds 1337 and 7 kept their global thresholds. Simple per-modality thresholding does not justify augmenting retry-patch.

QA readiness status: all three patch packs now have blind-label queue/shard setup, but none is full-QA-clean yet. Existing directories:

- `C:/Users/yanfi/PycharmProjects/fitz-gov/data/_workspaces/qa/modality_code_patch_v1_20260528/`: **720** blind queue rows, **720** manifest rows, and **12** Codex blind shards of **60** rows each. Existing scores are partial/targeted, not a completed full pass.
- `C:/Users/yanfi/PycharmProjects/fitz-gov/data/_workspaces/qa/modality_code_retry_conflict_patch_v1_20260529/`: **360** blind queue rows, **360** manifest rows, and **12** Codex blind shards of **30** rows each.
- `C:/Users/yanfi/PycharmProjects/fitz-gov/data/_workspaces/qa/modality_missing_evidence_patch_v1_20260529/`: **360** blind queue rows, **360** manifest rows, and **12** Codex blind shards of **30** rows each.

The earlier small tabular probe remains useful as a sanity check: `scripts/tabular_ood_probe.py` scored `pyrrho-nano-g3` at **55.56%** calibrated accuracy / **45.83%** false-TRUSTWORTHY on 36 hand-labeled structured rows. The full 20k candidate probe now supersedes it as the main modality-gap measurement.

## Known limitations

### `pyrrho-nano-g1` (in production)

1. **Multi-source-convergence misclassified as DISPUTED.** When multiple authoritative sources agree on a fact with slight numerical variation, ~57% error rate on this fitz-gov subcategory (n=7). Deferred to v2.
2. **Short clean TRUSTWORTHY contexts trigger over-abstention.** Tier1 training is 62.7% hard cases; the model never learned the short-clean-answer pattern. Fixable in v2.

### `pyrrho-nano-g2`

1. **Not the production default yet.** `fitz-sage` still uses `pyrrho-nano-g1`; `g2` is live as the V7 benchmark release but has not been integrated into fitz-sage's production path.
2. **Breakdowns are SDGP-only.** V7.0.1 public rows and regenerated pyrrho processed data no longer expose pre-SDGP report axes (`meta.domain`, `meta.subcategory`, `meta.reasoning_type`, `meta.query_type`, `meta.evidence_pattern`). Use `taxonomy.pattern`, `taxonomy.cell_id`, `routing.expert_fired`/processed `expert`, and `meta.difficulty`/processed `difficulty`.
3. **Weakest held-out expert domain is science/medicine.** Across the 3 seed reports, calibrated held-out test metrics by `expert` put `science_medicine` last at **90.93 ± 0.74% accuracy / 5.99 ± 1.06% false-trustworthy** (n=169 per seed). It is the first V8 candidate for targeted eval-probe and augmentation.
4. **Automotive/ECU test-management OOD probe exposed generic taxonomy gaps.** A 10-case synthetic ECU-test probe with exact query strings absent from `data/processed_v7` scored **7/10, 6/10, 8/10** across seeds 42/1337/7. Manual gold-label audit found **10/10 expected labels defensible**. The misses are now represented as V8 primary taxonomy gaps and expanded across all current domains, not as an automotive-only domain.

### `pyrrho-nano-g3`

1. **Not the production default yet.** `fitz-sage` still uses `pyrrho-nano-g1`; `g3` is live as the V8 benchmark release and is the next encoder candidate to integrate.
2. **Output contract is intentionally narrow.** The published nano encoder emits three-class governance logits only. Normalized product output is derived from logits/probabilities/thresholds; taxonomy/category, route, and scalar fields in reports are benchmark metadata or MoE-only research outputs.
3. **Aggregate metrics are very strong, but deployment still needs field-specific checks.** The held-out V8 result is **97.52 ± 0.43% accuracy / 1.42 ± 0.16% false-trustworthy**, but production integrations should still run domain-specific probes before relying on it in high-cost workflows.
4. **Published g3 is unsafe on structured/code OOD; the local modality controls are not release-ready.** Frozen g3 scored **52.69%** calibrated accuracy / **51.69%** false-TRUSTWORTHY on the full 20k QA-clean candidate pack; code was worse (**49.35% / 71.38% FT**) than structured (**56.02% / 31.99% FT**). The 3-seed joint candidate retrain fixes held-out candidate rows in-distribution (**100.00 ± 0.00% / 0.00 ± 0.00% FT** on structured and code), but it relies on same-generator candidate splits and raised unstructured FT. Targeted label-trusted patches improved hard OOD, with the retry patch still best after threshold/policy sweep: current code OOD **99.07 ± 1.60% / 1.39 ± 2.41% FT**, current tabular OOD **93.52 ± 8.93% / 1.39 ± 2.41% FT**, and OOD-only fixed-threshold tau 0.73 removes probe FT without making missing-evidence a better branch. Routing diagnostics did not justify simple specialists: older one-seed specialists regressed or raised OOD FT, and the patch-aware code specialist only tied the retry-patch seed-42 generalist while shifting the OOD miss to retry-limit `constant_config_conflict`. Patch blind-label QA remains required before any merge/publish decision.

### `pyrrho-nano-g3.2`

1. **Not integrated into fitz-sage yet.** The package is live and verified, but fitz-sage still points at `pyrrho-nano-g3.1` on the local integration branch.
2. **Retrieval-control labels are mechanically validated, not separately blind-QA scored.** fitz-gov V8.2.0 has `routing.retrieval_control` on all **24,592** rows with validation clean, but the new retrieval-control labels did not receive an independent blind-label QA pass. Existing V8 governance blind-label QA remains clean.
3. **Retrieval modality is the weak new head.** Held-out retrieval-modality macro F1 is **55.75 ± 1.75%**, with sparse subclasses such as `pdf_layout` still effectively unsupported. Use retrieval modality as a hint, not an authoritative decision, until the modality label distribution is expanded and QA hardened.

### `pyrrho-nano-g5`

1. **Not a clean fitz-sage production upgrade yet.** The 120-case fitz-sage benchmark dropped from **63/120** on the previous local g4-alpha baseline to **57/120** on g5, despite better aggregate evidence reach.
2. **Training-prep retrieval-action alias bug.** Local g5 prep normalized V10 answer aliases such as `answer`, `answer_from_evidence`, and `use_retrieved_evidence` to `retrieve_more`. Result: **1,830** local V10 TRUSTWORTHY rows trained with `retrieval_action=retrieve_more` and `gap_type=none`. Published fitz-gov V10's upload normalizer fixed most of this, leaving **40** public V10 rows with the same contradiction.
3. **Table/status/owner coverage is weak.** V10 `structured_table` obligation rows are sparse and mostly negative/retry: only **22 / 1,082** V10 structured-table rows are TRUSTWORTHY, and the `status_or_outcome` obligation appears only with `log_trace`. This explains table queries drifting to `code`, `log_trace`, or `mixed` in fitz-sage.
4. **fitz-sage is not using `retrieval_obligation` structurally yet.** The g5 obligation head is exposed in metadata, but the current evidence contract still routes mostly from `retrieval_modality`; obligation-aware catalog planning remains a fitz-sage-side follow-up.

### `pyrrho-nano-g5.1`

1. **Local repair only; not published.** g5.1 fixes the g5 prep contradiction and is CPU-package verified locally, but it has not been pushed to Hugging Face.
2. **The action-blocker bug is mostly gone.** Across the 120-case fitz-sage benchmark, Pyrrho action-blocker failures dropped **11 -> 1** after retraining on the repaired prep.
3. **Strict role-boundary correction supersedes the compactbridge score as current state.** Compactbridge reached **94/120** on 2026-06-14, but it also exposed that fitz-sage still co-owned some pre-retrieval planning. On 2026-06-15, fitz-sage was tightened so Pyrrho owns pre-retrieval route/modality/obligation/temporal/comparison/broadness decisions; fitz-sage executes literal retrieval, closure, table/code search, and evidence packaging.
4. **Current strict-owner benchmark is 92/120.** The strict path passes fitz-sage full unit tests (**1209 passed**, **14 warnings**) and `python -m tools.contract_map`. Fresh benchmark scores are core **16/20**, holdout **41/50**, holdout2 **35/50**, with **5** forbidden-evidence hits. Do not quote **94/120** as the current strict architecture score.
5. **The V10 gap detector closed its own matrix, not the fitz-sage failure matrix.** V10 target-10 is truly closed on `modality_obligation`, `gap_type_tail`, and `taxonomy_tail`, but those matrices did not enforce `governance_class x query_contract x retrieval_action x obligation x stale/final-decoy shape x evidence-pack length`. Concrete blind spots: processed g5.1 data has median **2** contexts while strict fitz-sage packs have median **9** evidence items; structured-table `row_key_lookup`, `column_value_lookup`, and `multi_row_comparison` have only **4/3/2** TRUSTWORTHY rows respectively; `stale_row_version` has only **12** TRUSTWORTHY rows; `code_plus_changelog` has only **26** DISPUTED rows. These explain the remaining table, final/latest, and stale-doc conflict failures better than generic "more rows" would.
6. **V11 is now official and g5.5 is published.** fitz-gov V11.0.0 is live on Hugging Face with **60,883** rows and closes the strict-owner repair matrices at target: class-obligation **1,134/1,134**, failure-focus **714/714**, pack-shape **1,428/1,428**, independent gap **0**. The V11 row block is **7,380/7,380** blind-label validated with **0** triage after duplicate-text, DISPUTED semantic, and blind-triage repair. `pyrrho-nano-g5.5` has a completed 3-seed local run at `outputs/pyrrho-nano-g5_5_v11_official/summary.json`, a local package at `models/pyrrho-nano-g5.5/`, and a downloaded-snapshot CPU verifier pass at `outputs/hf_download_pyrrho_nano_g5_5_verify/release_verify_report.json`. The remaining question is fitz-sage integration quality, not model packaging.

### `pyrrho-nano-g5.5`

1. **Published and better on fitz-gov heads, but not better in fitz-sage.** Compared with g5, the 3-seed held-out heads improved: governance accuracy **+0.59 pp**, false-TRUSTWORTHY **-0.17 pp**, taxonomy accuracy **+4.09 pp**, retrieval-modality macro F1 **+3.20 pp**, and retrieval-obligation macro F1 **+3.90 pp**. But strict-owner fitz-sage scored **90/120**, below local g5.1 strict-owner **92/120**.
2. **The fitz-sage score moved sideways, not forward.** g5.5 wins **6** cases over g5.1 and regresses **8**. It reduces forbidden evidence **5 -> 4**, but increases mode mismatches **16 -> 19** while missing-required-evidence failures stay at **7**.
3. **Domain tradeoff is visible.** g5.5 keeps code **22/28** and mixed **14/23**, improves unstructured **27/37 -> 29/37**, but regresses structured **29/32 -> 25/32**. Most structured regressions have full required evidence present and fail because g5.5 abstains or disputes instead of choosing TRUSTWORTHY.
4. **The hard slices are still hard.** g5.5 passes **0/11** conflict, **0/7** stale-docs, and **0/4** governance-tagged dispute cases in the 120-case strict-owner benchmark. Temporal/final selection is still weak: temporal **6/11** with **4** forbidden hits, final_fact **1/4** with **3** forbidden hits.
5. **The V11 7,380-row repair block was fitz-sage-shaped, but too broad and too thin per critical cell.** The V11 detector closed its 5/8/5 profile across **1,134** class-obligation cells, **714** failure-focus cells, and **1,428** pack-shape cells. That gives coverage, not mastery. Raising the same matrix from 5/8/5 to broad 10/10/10 after V11 requires only **2,910** practical rows, with gaps concentrated in class-obligation (**1,766**) and pack-shape (**1,385**) while failure-focus is already closed at target 10.
6. **Next gap-detector plan is focused, not broad.** A strict-owner g6/V12 repair profile was generated at `C:/Users/yanfi/PycharmProjects/fitz-gov/data/_workspaces/reports/v12_g6_strict_owner_repair_plan_20260615.md` and row targets at `.../v12_g6_strict_owner_repair_row_targets_20260615.jsonl`. It opens **2,889** independent focused deficits and compresses them to **2,200** practical rows across **770** row-target specs. Row mix: latest/final **609**, mixed bridge **375**, table comparison **294**, table lookup **294**, stale forbidden decoy **288**, stale-doc conflict **252**, code-doc dispute **88**. Governance mix: TRUSTWORTHY **1,612**, DISPUTED **588**. Pack mix: **900** retrieval-pack rows, **900** long-pack rows, **400** obligation/focus rows without an added pack-shape deficit.
7. **Do not wire g5.5 as the fitz-sage default yet.** It is a public research/release artifact and a better fitz-gov model, but the current fitz-sage candidate remains local g5.1 for strict-owner benchmark purposes until the conflict/stale/final and structured-abstention regressions are repaired.

### `pyrrho-nano-g5.6` and `pyrrho-nano-g5.7`

1. **g5.6 remains the best downstream candidate; g5.7 is a useful negative control.** g5.6 strict-owner fitz-sage scored **97/120**. g5.7 improved held-out model heads slightly but scored **96/120** downstream, so do not publish or wire g5.7.
2. **The V12.1 1,200-row repair was too broad for the remaining 120-case failures.** g5.7 fixed **3** g5.6 failures (`holdout_travel_meal_cap`, `holdout_mixed_dep902_guard`, `holdout2_mixed_biometric_export_retention`) but introduced **4** regressions (`holdout_cobalt_security_addendum`, `holdout2_incident_inc730_status_commander`, `holdout2_highest_spend_vendor`, `holdout2_mixed_meridian_vendor_policy_code`).
3. **Current failure shape after g5.7:** **24** failures = **15** mode-only and **9** evidence-only. Mode errors are `disputed->trustworthy` **9**, `trustworthy->abstain` **4**, `trustworthy->disputed` **1**, and `disputed->abstain` **1**. Missing evidence is now mostly `source_not_in_pack` (**7**) rather than `source_touched_but_not_final`.
4. **The hardest remaining Pyrrho-side problem is still conflict governance.** Failed cases still cluster around unstructured/code/mixed conflicts and stale/latest/final evidence. The next data should be sharper near the exact contrast boundary: contradictory evidence present but not enough to call TRUSTWORTHY, plus clean trustworthy temporal/final cases that should not over-abstain.

### `pyrrho-MoE-g3-mvp`

0. **Architecture naming is now explicit.** The project architecture name is **Pyrrho MoE MVP**. The runtime / loader architecture is **Qwen3MoE-compatible sparse MoE** because GGUF/llama.cpp and HF auto metadata need that compatibility family. Public wording should be "Pyrrho MoE MVP, Qwen3MoE-compatible and Qwen-seeded."
1. **Selected-output / sequence-label only.** The deployable adapter decision is `selected_output` from label-score thresholding at **tau 0.50**. The deployable GGUF decision is the top-level `classification` from `--decision-mode sequence-label-score`. Raw generated `classification` is audit/debug text and is unsafe as the governance decision source.
2. **Full-generation evidence is bounded.** Full eval/test gate evidence is selected-label skip-generation scoring. JSON parse, route, taxonomy, and raw-generation safety evidence is still bounded to a **512-row** full-generation slice plus package smokes.
3. **Low-memory Q4 selected-label runtime is packaged as the main CPU decision runtime.** The patched llama.cpp Q4_K_M path plus full-sequence label scoring clears the full held-out test at **82.15% / 5.27% FT** with **4.224 GiB** peak RSS and is documented as the authoritative low-memory decision path.
4. **GGUF raw generation is parseable but unsafe.** The 32-row Q4 GGUF smoke reached **100%** JSON+label parse and **84.38%** accuracy, but **20.83%** false-TRUSTWORTHY. Raw generated `classification` remains audit/debug text only.
5. **First-token GGUF label scoring is not enough.** The 32-row first-token bridge looked gate-clean (**93.75% / 4.17% FT**) but failed on the first **512** ordered held-out test rows (**60.94% / 33.33% FT** at tau **0.58**). Use full-sequence label scoring instead.
6. **Local bitsandbytes 4-bit is not enough.** A direct bnb4 CPU base-model load succeeded (`outputs/moe/package_hardening/bnb4_cpu_load_probe.json`), but packaged-adapter selected-output inference timed out after **900s** with no output (`outputs/moe/package_hardening/bnb4_cpu_adapter_skipgen_timeout.json`). Use GGUF/llama.cpp or another runtime backend instead.
7. **LM Studio does not load the current GGUF.** That is expected because LM Studio uses a bundled llama.cpp build without the required Qwen3MoE `mlp_only_layers` patch. Use the patched `llama-server` flow instead.
8. **Run guide exists.** Use `docs/PYRRHO_MOE_MVP_RUN_GUIDE.md` for the current operator flow. It covers HF download, patched llama.cpp build, JSONL input shape, output fields, the LM Studio limitation, and the "do not use raw generation" rule.

### `pyrrho-nano-g2.1-v8-probe`

1. **Still local-only; not benchmark-contract clean enough to publish as `g2.1` yet.** The run is on a mixed local contract (`data/processed_v8_probe`) that preserves V7 splits and appends V8 rows by manifest. It is the right ablation, but not yet the public release.
2. **Verdict-conflict robustness is still the weak spot.** The recovered ECU PASS/FAIL conflict case (`ecu_04_disputed_dtc_powercycle`) improved only **0/3 -> 1/3** across seeds after V8 retraining. Candidate-selection and wrong-release abstain gaps improved more cleanly than explicit final-verdict contradiction handling.
3. **One seed traded fixes instead of improving cleanly.** Seed 7 stayed **8/10** overall by fixing `ecu_02` and `ecu_04` but regressing `ecu_01` and `ecu_07`. The V8 pack is directionally useful, but not yet a fully stable OOD fix.
4. **Verdict-only densification was not enough.** The 105-row hard `verdict_conflict` patch improved `ecu_04` but pushed adjacent clean cases toward DISPUTED. The later 840-row g2.2 retrain fixed `ecu_04` completely but still traded off adjacent controls, so more data is not automatically better unless the OOD probe improves too.

### `pyrrho-nano-g2.2`

1. **Not a publish candidate yet despite better FT.** It has the best held-out false-trustworthy rate so far (**3.06 ± 0.61%**) and passes gates, but OOD mean is **8.00/10**, below the original V8 probe's **8.33/10**.
2. **OOD tradeoff moved rather than vanished.** g2.2 fixed explicit same-build PASS/FAIL conflict handling (`ecu_04` **3/3**) but lost resolved-candidate and wrong-release controls (`ecu_02` **0/3**, `ecu_07` **0/3**).
3. **Aviation probe is stronger, but not clean.** g2.2 is best on the new aviation maintenance probe (**8.67/10**), but `air_02_trustworthy_superseded_sb_resolved` is still only **1/3** and maps to the same resolved/superseded-candidate boundary rather than a new aviation-specific taxonomy.

### `pyrrho-small-g1` (not shipped)

1. **Fails the false-trustworthy gate (12.13% vs 5.7%).** Plain SFT has no anti-FT pressure. See g1.1 below for the partial fix.
2. **Disputed/abstain recall regressed vs encoder.** The SLM's preference for TRUSTWORTHY pulls cases out of those buckets — same root cause as #1.

### `pyrrho-small-g1.1` (not shipped)

1. **Still fails the false-trustworthy gate (9.31% vs 5.7%).** Class weights + label smoothing closed ~40% of the gap from g1 (12.13 → 9.31), but the encoder-style recipe transplant under-delivers on the SLM because token-level CE diffuses the safety pressure across many assistant-turn tokens. To clear the 5.7% gate without ditching SFT, would need stronger weights (e.g., 5.0/5.0/1.0), stronger smoothing (0.25+), or `ft_penalized_accuracy` checkpoint selection (currently `eval_loss`). Cleaner long-term fix: DPO/GRPO with asymmetric FT-penalized reward per [ROADMAP §8 Phase 3](ROADMAP.md).
2. **Tier0 dropped from 99.44 → 96.67%.** The class-weight pressure makes the model more cautious in general, costing it 2 tier0 cases. Within the dropped-95%-gate budget, but worth noting.

## Pipeline / tooling — what exists now

| Script | Purpose |
|---|---|
| [`scripts/prepare_data.py`](../scripts/prepare_data.py) | fitz-gov HF/Vault → train/eval/test JSONL + HF DatasetDict; current default reads published V8.0.1 (`yafitzdev/fitz-gov`, config `v8`, revision `v8.0.1`); `--split-manifest` preserves local vault train/eval/test splits offline; explicit V7 args still reproduce `data/processed_v7`; candidate structured/code packs can be appended with selection manifests, duplicate/query-overlap guards, deterministic query-grouped candidate splits, modality columns, and `prep_summary.json` |
| [`scripts/filter_processed_modalities.py`](../scripts/filter_processed_modalities.py) | Local processed-DatasetDict filter for modality controls; preserves source split membership while writing JSONL, `hf_dataset/`, `prep_summary.json`, and a manifest for kept `unstructured` / `structured` / `code` rows |
| [`scripts/prepare_moe_data.py`](../scripts/prepare_moe_data.py) | Published fitz-gov V8/V8.2 → flattened MoE/multitask JSONL with governance labels, query-only text, optional mandatory `routing.query_contract`, optional mandatory `routing.retrieval_control`, route IDs, taxonomy IDs, scalar targets, context features, metadata, and strict required-field audit |
| [`scripts/prepare_g4_official_data.py`](../scripts/prepare_g4_official_data.py) | Official `pyrrho-nano-g4` prep path: loads `yafitzdev/fitz-gov` config/tag `v9.0.0`, verifies every row against public `v9/split_assignments.jsonl`, writes `data/multitask_g4_v9_official`, and fails if splits are not exactly train **32,625** / eval **4,104** / test **4,026** |
| [`scripts/prepare_g4_alpha_data.py`](../scripts/prepare_g4_alpha_data.py) | Local V9/V10 multitask flattener used by g4/g5 prep: starts from `data/multitask_v8_2_retrieval_control`, adds local V9 rows, supports `--config` and `--skip-candidates`, canonicalizes enum drift, collapses answerability to four planning labels, normalizes answer-style retrieval-action aliases to `answer_now`, and forces `TRUSTWORTHY + gap_type=none` rows to `answer_now` |
| [`scripts/prepare_g5_alpha_data.py`](../scripts/prepare_g5_alpha_data.py) | Local `pyrrho-nano-g5-alpha` prep path: starts from official `data/multitask_g4_v9_official`, appends V10 5/5/5 candidate JSONL rows, adds the 31-label `retrieval_obligation` target, masks V9 rows with `retrieval_obligation_id=-1`, and normalizes V10 answer-shape enum drift into the four collapsed planning labels |
| [`scripts/prepare_g5_data.py`](../scripts/prepare_g5_data.py) | Real `pyrrho-nano-g5` / local `g5.1` target-10 prep path: starts from official `data/multitask_g4_v9_official`, appends both repaired blind-QA-clean local V10 candidate dirs (**6,058** 5/5/5 rows + **6,690** target-10 continuation rows), writes a caller-selected output dir such as `data/multitask_g5_v10_target10_candidate` or `data/multitask_g5_1_v10_repaired`, keeps V9 obligation labels masked, and records V10 blind-QA-clean status in metadata/manifest |
| [`scripts/prepare_g5_5_official_data.py`](../scripts/prepare_g5_5_official_data.py) | Official `pyrrho-nano-g5.5` V11 prep path: loads `yafitzdev/fitz-gov` config `v11` at tag `v11.0.0`, verifies `v11/split_assignments.jsonl`, writes `data/multitask_g5_5_v11_official` with train **48,800** / eval **6,028** / test **6,055**, keeps `retrieval_obligation=none` rows masked, and records **18,868** labeled obligation rows |
| [`scripts/train_multitask_encoder.py`](../scripts/train_multitask_encoder.py), [`src/pyrrho/multitask.py`](../src/pyrrho/multitask.py) | `pyrrho-nano-g3.1` / `g3.2` / `g4-alpha` / `g4-target100` / `g4` / `g5-alpha` / `g5` / `g5.1` / `g5.5` custom ModernBERT multitask encoder trainer/runtime: full-input governance/taxonomy/scalar heads plus query-only query-contract and route/domain heads, with optional retrieval-action/gap/answerability/modality/obligation heads; supports partial initialization from a previous multitask checkpoint via `model.init_from`; saves `pyrrho_multitask_config.json`, `model.safetensors`, tokenizer files, per-seed metrics, and reloadable best checkpoints |
| [`scripts/package_multitask_encoder.py`](../scripts/package_multitask_encoder.py), [`src/pyrrho/multitask_inference.py`](../src/pyrrho/multitask_inference.py) | Local multitask encoder package creator/verifier and public prediction API; package manifest hashes files, records seed/threshold/head labels/metrics, copies reports, writes README, and runs CPU smoke through `PyrrhoMultiTaskPredictor`; README generation now distinguishes `g5.5`, `g5.1`, `g5`, `g5-alpha`, `g4`, `g4-alpha`, `g4-target100`, and normal published V8.2 releases |
| [`scripts/count_moe_params.py`](../scripts/count_moe_params.py) | Exact pyrrho-MoE total/active parameter accounting from `configs/moe/pyrrho_moe_g3_alpha.yaml` |
| [`scripts/audit_moe_runtime_shape.py`](../scripts/audit_moe_runtime_shape.py) | Gate-zero audit for `pyrrho-MoE-g4-real`; rejects configs that already violate the clean runtime shape before GGUF export/load tests |
| [`scripts/export_olmoe_structural_checkpoint.py`](../scripts/export_olmoe_structural_checkpoint.py) | Exports full or tiny random OLMoE structural checkpoints from the `g4-real` YAML for stock GGUF conversion/load gates |
| [`scripts/init_olmoe_from_donor.py`](../scripts/init_olmoe_from_donor.py) | Initializes the exact `g4-real` OLMoE carrier from a larger OLMoE donor with explicit layer mapping and tensor resizing; supports `--resize-strategy block-mean` and `head-topnorm-slice`; current block-mean donor-init artifact passes stock llama.cpp and LM Studio load, while head-topnorm is a quality-negative diagnostic seed |
| [`scripts/train_moe_olmoe_sft.py`](../scripts/train_moe_olmoe_sft.py) | OLMoE-default wrapper around the generative SFT harness for `g4-real`; keeps the active path out of Qwen-named defaults |
| [`scripts/probe_fitz_sage_enrichment_bus.py`](../scripts/probe_fitz_sage_enrichment_bus.py) | Imports the real fitz-sage `KragEnricher` and tests whether a local HF checkpoint or OpenAI-compatible endpoint can satisfy the KRAG enrichment JSON-array contract for keywords, entities, and temporal metadata; endpoint reports include OpenAI-style usage tokens and completion tok/s when available |
| [`scripts/analyze_moe_seed_budget.py`](../scripts/analyze_moe_seed_budget.py) | Compares canonical and Qwen-aligned MoE budget variants; selected `pyrrho_moe_g3_alpha_qwen.yaml` for first upcycling attempt |
| [`scripts/train_moe.py`](../scripts/train_moe.py) | Stage 0-0.7 MoE route prototype trainer/evaluator; supports tiny, pooled route-coupled, token route-coupled, and support-aggregating custom students, top-1 expert selection, route/governance/taxonomy/scalar losses, g3 teacher-logit distillation, oracle-route comparison, and expert traffic reporting |
| [`scripts/eval_moe.py`](../scripts/eval_moe.py) | Standalone Stage 0-0.7 MoE checkpoint evaluator; loads tiny, route-coupled, token route-coupled, or support-aggregating checkpoints and writes eval/test reports without retraining |
| [`scripts/upcycle_dense_to_moe.py`](../scripts/upcycle_dense_to_moe.py) | Dense-to-MoE upcycling path; validates Qwen seed/config compatibility, real-weight FFN compression, sharded seed-pack writing, and seed-pack shape validation |
| [`scripts/smoke_moe_qwen_wrapper.py`](../scripts/smoke_moe_qwen_wrapper.py) | Loads the Qwen3-MoE seed pack with pyrrho governance heads and runs a tiny no-training forward/loss smoke |
| [`scripts/generate_moe_teacher_logits.py`](../scripts/generate_moe_teacher_logits.py) | Generates sidecar governance logits from `models/pyrrho-nano-g3` for MoE distillation without rewriting canonical MoE JSONL |
| [`scripts/analyze_moe_failures.py`](../scripts/analyze_moe_failures.py) | Reloads Stage 0-0.7 checkpoints, emits per-case predictions, per-route/per-taxonomy breakdowns, seed-overlap histograms, and markdown failure reports |
| [`scripts/train_moe_posthoc_verifier.py`](../scripts/train_moe_posthoc_verifier.py) | Trains a separate frozen-output Stage 0.7 verifier/reranker over candidate governance/route/taxonomy/scalar features and selects demotion thresholds on eval |
| [`scripts/package_moe_posthoc_verifier.py`](../scripts/package_moe_posthoc_verifier.py) | Packages Stage 0.7 post-hoc verifier artifacts with manifest, feature schema, checksums, reload evaluation against frozen base checkpoints, and release-mode copying for checkpoints/config/metadata/policy reports |
| [`src/pyrrho/moe/posthoc_verifier.py`](../src/pyrrho/moe/posthoc_verifier.py) | Runtime API for packaged Stage 0.7 post-hoc verifiers: builds frozen-output features, validates package checksums/schema, loads per-seed verifiers, scores accept probabilities, and applies the TRUSTWORTHY demotion policy |
| [`src/pyrrho/moe/posthoc_policies.py`](../src/pyrrho/moe/posthoc_policies.py), [`scripts/compare_moe_posthoc_policies.py`](../scripts/compare_moe_posthoc_policies.py) | Packaged-verifier seed/ensemble policy helpers and comparison harness; reports per-seed mean, majority vote, 2-of-3 TRUSTWORTHY quorum, unanimous TRUSTWORTHY, and support-retaining vs safety-first recommendations |
| [`src/pyrrho/moe/posthoc_thresholds.py`](../src/pyrrho/moe/posthoc_thresholds.py), [`scripts/sweep_moe_posthoc_single_seed_thresholds.py`](../scripts/sweep_moe_posthoc_single_seed_thresholds.py) | Single-seed verifier-threshold sweep for packaged Stage 0.7 artifacts; selects thresholds on eval against an ensemble-policy FT target, then applies them to held-out test |
| [`scripts/distill_moe_posthoc_quorum.py`](../scripts/distill_moe_posthoc_quorum.py) | Local single-seed quorum-distillation diagnostic; trains a small HGB classifier from one seed's frozen Stage 0.7 features to the packaged ensemble target, then eval-selects a TRUSTWORTHY threshold and applies it to held-out test |
| [`scripts/analyze_moe_posthoc_quorum_gaps.py`](../scripts/analyze_moe_posthoc_quorum_gaps.py) | Compares the 3-forward packaged quorum against single-pass approximations by row outcome, label transition, route, taxonomy, and false-TRUSTWORTHY deltas |
| [`src/pyrrho/moe/inference.py`](../src/pyrrho/moe/inference.py), [`scripts/infer_moe_posthoc.py`](../scripts/infer_moe_posthoc.py) | End-to-end local Stage 0 MoE inference harness for raw `query` + `contexts` JSONL; loads checkpoints, optional packaged verifier, emits base/guarded predictions, route/taxonomy names, scalar signals, accept scores, demotion flags, packaged 3-seed ensemble policies, and explicit `--device cpu` runtime smokes |
| [`scripts/verify_moe_release.py`](../scripts/verify_moe_release.py) | Offline release verifier for packaged MoE artifacts; validates manifest-relative paths, checkpoint/config/verifier/report sizes and SHA-256 hashes, packaged verifier loadability, feature width, and optional CPU inference smoke with timing |
| [`scripts/benchmark_moe_release.py`](../scripts/benchmark_moe_release.py) | Offline runtime/RSS benchmark for packaged MoE releases; loads package/runtimes once, runs warmup and measured repeats, reports load time, ms/row, rows/s, prediction counts, seed-level verifier rejections, RSS memory when `psutil` is available, and can profile multiple batch sizes in one loaded run |
| [`scripts/train_moe_qwen_heads.py`](../scripts/train_moe_qwen_heads.py) | Stage 1 Qwen3-MoE trainer; freezes trunk by default and trains pyrrho heads, optional internal routers/final dense layers/LoRA, physical expert adapters, semantic-route adapters, and g3 logit distillation |
| [`scripts/train_moe_qwen_sft.py`](../scripts/train_moe_qwen_sft.py) | MVP-oriented generative SFT path for local CausalLM seed packs; trains LoRA on compact pyrrho JSON targets, scores generation parseability, supports selected-output label-score thresholding, can skip generation for full-split validation, exposes optional bitsandbytes load probes, has `--unfreeze-parameter-patterns` for explicit raw-parameter diagnostics, has OLMoE-only `--olmoe-expert-down-lora-*`, `--olmoe-expert-gate-up-lora-*`, and `--olmoe-router-lora-*` hooks for raw expert/router adapter probes, and can attach teacher logits with label-candidate KL distillation |
| [`scripts/infer_moe_qwen_sft.py`](../scripts/infer_moe_qwen_sft.py) | Minimal local runtime for the Qwen generative MVP adapter; loads the seed pack plus PEFT adapter, emits authoritative `selected_output` JSON, keeps raw generation as audit/debug text, and can attempt optional bitsandbytes quantized loads |
| [`scripts/verify_moe_qwen_sft_package.py`](../scripts/verify_moe_qwen_sft_package.py) | Local package verifier for `models/pyrrho-MoE-g3-mvp`; validates manifest/package files, metric gates, report JSON/JSONL parseability, seed-pack presence, and a packaged adapter smoke |
| [`scripts/materialize_moe_qwen_sft_merged.py`](../scripts/materialize_moe_qwen_sft_merged.py) | Materializes the Qwen-seeded pyrrho-MoE adapter into a plain merged HF checkpoint for GGUF export |
| [`scripts/convert_qwen3moe_hf_to_gguf.py`](../scripts/convert_qwen3moe_hf_to_gguf.py) | Wrapper around llama.cpp's converter that enables Qwen3MoE dense `mlp_only_layers` tensors without editing the upstream Python converter |
| [`scripts/smoke_moe_gguf_server.py`](../scripts/smoke_moe_gguf_server.py) | Starts a local llama.cpp HTTP server for a pyrrho-MoE GGUF, runs raw-generation, first-token label-score, or full-sequence label-score smokes, parses pyrrho JSON when applicable, reports metrics, disables server prompt-cache RAM by default, and records peak RSS when `psutil` is available |
| [`patches/llama_cpp_qwen3moe_mlp_only_layers.patch`](../patches/llama_cpp_qwen3moe_mlp_only_layers.patch) | Local llama.cpp patch needed for the current GGUF runtime: Qwen3MoE dense `mlp_only_layers`, dense tensor-name registration, and `norm_topk_prob=false` routing semantics |
| [`scripts/train_encoder.py`](../scripts/train_encoder.py) | Single-run encoder fine-tuning, config-driven, writes manifest.json; supports optional held-out `test` and optional `tier0_sanity` |
| [`scripts/train_slm.py`](../scripts/train_slm.py) | Single-run SLM QLoRA fine-tune (TRL SFTTrainer + PEFT) with decode-based eval; auto-uses `WeightedLossSFTTrainer` (per-example class weights + label smoothing) when the config sets `training.class_weights` or `training.label_smoothing` |
| [`scripts/eval_slm.py`](../scripts/eval_slm.py) | Eval-only path for a saved SLM LoRA adapter — re-runs the decode-based eval pass without re-training. Use when the in-script eval was interrupted (e.g., the stdout-buffering hang we hit on g1.1 seed 1337) |
| [`scripts/run_seeds.py`](../scripts/run_seeds.py) | Multi-seed orchestrator (encoder-shaped output), aggregates mean ± std |
| [`scripts/aggregate_slm_seeds.py`](../scripts/aggregate_slm_seeds.py) | Multi-seed aggregator for `train_slm.py` outputs (no threshold calibration) |
| [`scripts/sweep.py`](../scripts/sweep.py) | Hyperparameter sweep (coordinate-descent or grid) |
| [`scripts/eval.py`](../scripts/eval.py) | 5-fold CV runner |
| [`scripts/eval_report.py`](../scripts/eval_report.py) | Full per-breakdown evaluation report on a checkpoint, including modality breakdowns |
| [`scripts/compare_runs.py`](../scripts/compare_runs.py) | Diff two runs (or vs baseline), markdown table out |
| [`scripts/automotive_ood_probe.py`](../scripts/automotive_ood_probe.py) | Recovered 10-case ECU/test-management OOD probe; exact-query leakage check against processed datasets plus calibrated old-vs-new multi-seed checkpoint comparison |
| [`scripts/aviation_ood_probe.py`](../scripts/aviation_ood_probe.py) | 10-case aviation maintenance / airworthiness OOD probe; compares `g2`, `g2.1-v8-probe`, and `g2.2` with exact-query leakage checks |
| [`scripts/modality_candidate_probe.py`](../scripts/modality_candidate_probe.py) | Builds manifests for the fitz-gov structured/code candidate pack and scores encoder checkpoints with per-modality/per-pattern reports |
| [`scripts/modality_specialist_compare.py`](../scripts/modality_specialist_compare.py) | Local-only modality routing/specialist diagnostic; evaluates fixed generalist/code/structured checkpoints by existing `modality` column without training, row generation, or schema changes |
| [`scripts/modality_threshold_sweep.py`](../scripts/modality_threshold_sweep.py) | Local-only modality threshold policy diagnostic; selects per-modality TRUSTWORTHY thresholds on eval and applies them to held-out test for fixed checkpoints |
| [`scripts/code_ood_probe.py`](../scripts/code_ood_probe.py) | 36-row hand-authored code-evidence OOD probe across exact support, code/config conflicts, wrong symbol/version, missing field, and missing test-result cases |
| [`scripts/audit_code_modality_axes.py`](../scripts/audit_code_modality_axes.py) | Audits one or more code candidate JSONLs for modality-specific language, artifact, question-target, failure-mode, syntax-mismatch, and hard-OOD target coverage without modifying candidate data |
| [`scripts/inspect_tier0_failures.py`](../scripts/inspect_tier0_failures.py) | Dump misclassified tier0 cases with full context |
| [`scripts/smell_test.py`](../scripts/smell_test.py) | 10-case sanity check (ad-hoc) |
| [`scripts/build_model_card.py`](../scripts/build_model_card.py) | Encoder HF model card builder |
| [`scripts/build_slm_model_card.py`](../scripts/build_slm_model_card.py) | SLM HF model card builder (uses summary.json + LoRA adapter dir) |
| [`scripts/render_public_model_cards.py`](../scripts/render_public_model_cards.py) | Public-facing model card renderer for current pyrrho release dirs; follows [`docs/MODEL_CARD_TEMPLATE.md`](MODEL_CARD_TEMPLATE.md), documents raw vs derived outputs, clarifies nano encoder vs MoE-only outputs, uses `±` metric notation, and avoids internal dataset/pipeline terms |
| [`tests/test_smoke.py`](../tests/test_smoke.py) | pytest version of the smell test for CI regression |

Reproducibility: every artifact-producing script writes `manifest.json` (git/pip/hw/seed/timing) via `pyrrho.manifest.write_manifest`.

Full methodology, release gates, and W&B conventions in [METHODOLOGY.md](METHODOLOGY.md).

## What's live

- **pyrrho-nano-g1 on HF**: https://huggingface.co/yafitzdev/pyrrho-nano-g1 (public, CC BY-NC 4.0, 1.35 GB: safetensors + FP32 ONNX + INT8 ONNX). README-only public-card refresh commit: `29e4eecba2676a0fca03637d1515ab03a6e7379f`. The card frames pyrrho as a RAG governance co-processor / anti-hallucination evidence gate, includes explicit Outputs with normalized JSON, label-oriented Results, uses `±` notation, and states that taxonomy/category tags, route IDs, and scalar diagnostics are not published nano encoder outputs. (Was `pyrrho-modernbert-base-v1` + Apache-2.0 through 2026-05-19; renamed under the new `pyrrho-{tier}-{generation}` scheme.)
- **pyrrho-nano-g2 on HF**: https://huggingface.co/yafitzdev/pyrrho-nano-g2 (public, CC BY-NC 4.0, README-only public-card refresh commit `4b66447636c14155640461a84639bb6ea7ebcd09`). Release has 10 files: `model.safetensors`, FP32 ONNX external-data pair, INT8 ONNX external-data pair, tokenizer/config, README, and `.gitattributes`.
- **pyrrho-nano-g3.1 on HF**: https://huggingface.co/yafitzdev/pyrrho-nano-g3.1 (public, CC BY-NC 4.0, latest checked commit `811bc4940d356ddf7b5bcf9102f211622d326cf1`, a README/model-card refresh over full-package commit `211d3131b2b1b0e74302b4213b16eb242b5b1e31`). Release has **13** Hub sibling files including `.gitattributes`: `model.safetensors`, `config.json`, `pyrrho_multitask_config.json`, tokenizer files, `manifest.json`, `training_config.yaml`, README, `release_verify_report.json`, and reports. Hub used storage reports **596,210,628 bytes** for the safetensors object; downloaded-snapshot verification passed at `outputs/hf_download_pyrrho_nano_g3_1_final_verify/release_verify_report.json`, and the refreshed README was downloaded after commit `811bc4940d356ddf7b5bcf9102f211622d326cf1`.
- **pyrrho-nano-g3.2 on HF**: https://huggingface.co/yafitzdev/pyrrho-nano-g3.2 (public, CC BY-NC 4.0, full-package commit `36a5940eb04bbfa0638948ee41953088bafdb0a3`). This is the V8.2 retrieval-control multitask encoder package built from seed **1337** with TRUSTWORTHY threshold **0.34**. Hub reports **13** sibling files including `.gitattributes`, `model.safetensors`, `config.json`, `pyrrho_multitask_config.json`, tokenizer files, `manifest.json`, `training_config.yaml`, README, `release_verify_report.json`, and reports. Downloaded-snapshot verification passed at `outputs/hf_download_pyrrho_nano_g3_2_verify/release_verify_report.json`. Training summary at `outputs/pyrrho-nano-g3_2_retrieval_control/summary.json`: held-out test **97.29 ± 0.22%** governance accuracy / **1.11 ± 0.17%** false-trustworthy, **93.92 ± 0.22%** query-contract macro F1, **92.59 ± 0.54%** route accuracy, **88.80 ± 0.19%** taxonomy accuracy, **0.0640 ± 0.0005** scalar MAE, **86.69 ± 0.23%** retrieval-action macro F1, **76.75 ± 1.56%** gap-type macro F1, **75.53 ± 0.54%** answerability-shape macro F1, and **55.75 ± 1.75%** retrieval-modality macro F1 across seeds 42/1337/7.
- **pyrrho-nano-g4 on HF**: https://huggingface.co/yafitzdev/pyrrho-nano-g4 (public, CC BY-NC 4.0, full-package commit `43789ecdda918c9382e3f42aebf72f57bb82944d`). This is the official public-split V9 multitask encoder built from seed **1337** with TRUSTWORTHY threshold **0.48**. Hub reports **13** sibling files including `.gitattributes`, `model.safetensors`, `config.json`, `pyrrho_multitask_config.json`, tokenizer files, `manifest.json`, `training_config.yaml`, README, `release_verify_report.json`, and reports; safetensors parameter count **149,071,947**. Downloaded-snapshot verification passed at `outputs/hf_download_pyrrho_nano_g4_verify/release_verify_report.json`. Training summary: `outputs/pyrrho-nano-g4_v9_official/summary.json`; 3-seed held-out test **97.46 ± 0.09%** governance accuracy / **1.21 ± 0.06%** false-trustworthy, **86.00 ± 0.23%** query-contract macro F1, **93.53 ± 0.09%** route accuracy, **77.95 ± 0.25%** taxonomy accuracy, **0.0690 ± 0.0003** scalar MAE, **85.92 ± 0.43%** retrieval-action macro F1, **69.85 ± 1.00%** gap-type macro F1, **94.92 ± 0.19%** collapsed answerability macro F1, and **51.94 ± 0.59%** retrieval-modality macro F1.
- **pyrrho-nano-g5-alpha local package**: `models/pyrrho-nano-g5-alpha/` (local-only, not on HF). This is the first V10 5/5/5 retrieval-obligation integration checkpoint for fitz-sage corpus-aware planning, trained from `models/pyrrho-nano-g4` and packaged from `outputs/pyrrho-nano-g5_alpha_v10_5x5_candidate/seed_1337/best_model` with TRUSTWORTHY threshold **0.34**. CPU package verifier passed at `models/pyrrho-nano-g5-alpha/release_verify_report.json`; 3-case CPU smoke passed at `models/pyrrho-nano-g5-alpha/reports/package_smoke.json`. It exposes governance, query contract, route, taxonomy, seven scalars, retrieval action, gap type, four-way collapsed answerability, retrieval modality, and retrieval obligation. Training summary: `outputs/pyrrho-nano-g5_alpha_v10_5x5_candidate/summary.json`; held-out test **96.98%** governance accuracy / **1.22%** false-trustworthy, **78.36%** retrieval-modality macro F1, **67.15%** retrieval-obligation macro F1.
- **pyrrho-nano-g5 on HF**: https://huggingface.co/yafitzdev/pyrrho-nano-g5 (public, CC BY-NC 4.0). This is the V10 target-10 retrieval-obligation multitask encoder built from `outputs/pyrrho-nano-g5_v10_target10_candidate/seed_7/best_model` with TRUSTWORTHY threshold **0.34** after the 3-seed run. Hub reports **13** sibling files including `.gitattributes`, `model.safetensors`, `config.json`, `pyrrho_multitask_config.json`, tokenizer files, `manifest.json`, `training_config.yaml`, README, `release_verify_report.json`, and reports. Local CPU verifier passed at `models/pyrrho-nano-g5/release_verify_report.json`; downloaded-snapshot CPU verification passed at `outputs/hf_download_pyrrho_nano_g5_verify/release_verify_report.json`; package smoke passed at `models/pyrrho-nano-g5/reports/package_smoke.json`. Training summary: `outputs/pyrrho-nano-g5_v10_target10_candidate/summary.json`; 3-seed held-out test **97.39 ± 0.13%** governance accuracy / **1.09 ± 0.25%** false-trustworthy, **85.98 ± 0.10%** retrieval-modality macro F1, **82.48 ± 0.47%** retrieval-obligation macro F1. Training data is the official V10 row set (**53,503** rows), with the g5 training split preserved as train **42,826** / eval **5,372** / test **5,305**.
- **pyrrho-nano-g5.1 local package**: `models/pyrrho-nano-g5.1/` (local repair, not on HF). This is the repaired g5 follow-up built from `outputs/pyrrho-nano-g5_1_v10_repaired/seed_7/best_model` with TRUSTWORTHY threshold **0.34**. CPU package verifier passed at `models/pyrrho-nano-g5.1/release_verify_report.json`; package smoke passed at `models/pyrrho-nano-g5.1/reports/package_smoke.json`. Training summary: `outputs/pyrrho-nano-g5_1_v10_repaired/summary.json`; fitz-sage benchmark reports: `C:/Users/yanfi/PycharmProjects/fitz-sage/benchmarks/results/core_g5_1.json`, `holdout_g5_1.json`, and `holdout2_g5_1.json`. It improves the 120-case fitz-sage pass count over g5 **57 -> 64** and reduces action-blocker failures **11 -> 1**, but does not fix mixed retrieval.
- **pyrrho-nano-g4-alpha local package**: `models/pyrrho-nano-g4-alpha/` (local-only, not on HF). This is a one-seed V9-candidate integration checkpoint for fitz-sage, built from `outputs/pyrrho-nano-g4_alpha_v9_candidate/seed_1337/best_model` with TRUSTWORTHY threshold **0.44**. CPU package verifier passed at `models/pyrrho-nano-g4-alpha/release_verify_report.json`; 3-case CPU smoke passed at `models/pyrrho-nano-g4-alpha/reports/package_smoke.json`. It exposes governance, query contract, route, taxonomy, seven scalars, retrieval action, gap type, four-way collapsed answerability, and retrieval modality.
- **pyrrho-nano-g4-target100 local package**: `models/pyrrho-nano-g4-target100/` (local-only pre-public candidate; do not publish directly). This is the QA-gated V9 target-100 3-seed candidate for fitz-sage work, rebuilt from `outputs/pyrrho-nano-g4_v9_target100/seed_42/best_model` with TRUSTWORTHY threshold **0.34** after seed 42 had the strongest held-out governance accuracy and lowest false-TRUSTWORTHY rate. CPU package verifier passed at `models/pyrrho-nano-g4-target100/release_verify_report.json`; 3-case CPU smoke passed at `models/pyrrho-nano-g4-target100/reports/package_smoke.json`. fitz-sage local integration smoke also passed via `create_governance("pyrrho/C:\\Users\\yanfi\\PycharmProjects\\pyrrho\\models\\pyrrho-nano-g4-target100")`; it loaded threshold **0.34**, returned TRUSTWORTHY on direct support, and exposed query-contract, answerability, modality, retrieval-action, gap-type, and all seven scalar heads. Training summary: `outputs/pyrrho-nano-g4_v9_target100/summary.json`; 3-seed held-out test **97.05 ± 0.11%** governance accuracy / **1.36 ± 0.13%** false-trustworthy, **85.42 ± 0.20%** query-contract macro F1, **93.83 ± 0.23%** route accuracy, **77.14 ± 0.39%** taxonomy accuracy, **0.0710 ± 0.0004** scalar MAE, **84.99 ± 0.09%** retrieval-action macro F1, **69.79 ± 0.36%** gap-type macro F1, **94.69 ± 0.11%** collapsed answerability macro F1, and **51.98 ± 0.03%** retrieval-modality macro F1.
- **pyrrho-nano-g3 on HF**: https://huggingface.co/yafitzdev/pyrrho-nano-g3 (public, CC BY-NC 4.0, README-only public-card refresh commit `f52f4a6a1ff6a008086aa3d1352b560b32e851cb`; original model upload commit `397393718985e7bfa101042e89ecc60103e9c447`). Release has 10 files: `model.safetensors`, FP32 ONNX external-data pair, INT8 ONNX external-data pair, tokenizer/config, README, and `.gitattributes`. Local mirror is `models/pyrrho-nano-g3/` with 9 source files / **1.506 GB**; the Hub adds `.gitattributes`.
- **pyrrho-MoE-g3-alpha on HF**: https://huggingface.co/yafitzdev/pyrrho-MoE-g3-alpha (public, CC BY-NC 4.0, final checked commit `a1d672201bedc83a5ca66a759f5e521420185bd1`). Release has **24** files / **691,739,877 bytes**: three Stage 0.7 `model.pt` checkpoints, per-seed `verifier.joblib` artifacts, portable `manifest.json`, config, metadata, policy reports, smoke outputs, README, and `.gitattributes`. Default policy is `trustworthy_quorum_2_of_3`; held-out test **90.65% accuracy / 1.90% false-TRUSTWORTHY / 81.71% TRUSTWORTHY recall**. The card intentionally has no `pipeline_tag` because this is a custom PyTorch package, not a hosted `transformers.AutoModel` artifact. Downloaded-snapshot CPU smoke passed at `outputs/moe/hf_download_pyrrho_moe_g3_alpha_verify/downloaded_snapshot_smoke.jsonl`. Offline verifier report for the local mirror: `models/pyrrho-MoE-g3-alpha/release_verify_report.json` (`ok=true`, feature width **120**, all checkpoint/config/verifier/report SHA-256 checks passed, 4-row CPU quorum smoke **4 ABSTAIN / 0 seed-level rejections**, smoke elapsed ~**2.77s**). CPU benchmark reports: `models/pyrrho-MoE-g3-alpha/cpu_benchmark_32.json`, `cpu_benchmark_32_seed42.json`, `cpu_batch_profile_32.json`, and `cpu_batch_profile_32_seed42.json`. This is not the active MVP target; it is a reference artifact.
- **pyrrho-MoE-g3-mvp on HF**: https://huggingface.co/yafitzdev/pyrrho-MoE-g3-mvp (public, CC BY-NC 4.0, latest checked commit `f637325ba5a49429952c122b14913dd4cf355411`; final downloaded-artifact validation was after full-package commit `6b44a30c531c286fcfc0d5b9b618c25c5c86441e`). Release has **34** Hub sibling files and **3,348,978,530 bytes** used storage: PEFT LoRA adapter, bundled Q4_K_M GGUF, llama.cpp patch, metadata, reports, manifest, verifier report, README, and `.gitattributes`. The model card now has a public quick start and explicitly names **Project architecture: Pyrrho MoE MVP** and **Runtime / loader architecture: Qwen3MoE-compatible sparse MoE**; HF/GGUF may still show `qwen3moe` as loader metadata. Local repo run guide: `docs/PYRRHO_MOE_MVP_RUN_GUIDE.md`. Authoritative low-memory CPU path is full-sequence GGUF label scoring at tau **0.50**; full held-out Q4 evidence is **82.15% accuracy / 5.27% false-TRUSTWORTHY**, **4.224 GiB** peak RSS, **96.38%** HF selected-output agreement. Final downloaded snapshot verifier passed at `outputs/moe/hf_download_pyrrho_moe_g3_mvp_final_verify/release_verify_report.json`, and downloaded Q4 random-32 smoke passed at **93.75% / 0.00% FT** with **4.220 GiB** peak RSS.
- **fitz-gov-v1 dataset on HF**: https://huggingface.co/datasets/yafitzdev/fitz-gov-v1 (public, CC BY-NC 4.0, frozen v1/classic line). The old `yafitzdev/fitz-gov` repo was moved to `yafitzdev/fitz-gov-v1` on 2026-06-19; the old URL redirects. Current frozen tag is `v1-final` with `v1-final/train.parquet` (**54,416** rows), `v1-final/validation.parquet` (**6,778**), `v1-final/test.parquet` (**6,750**), `v1-final/split_assignments.jsonl`, `v1-final/metadata.json`, and `v1-final/manifest.json`. It is a pyrrho-ready flattened training view from `data/multitask_g5_6_v12_focused_medium_candidate`: **67,944** rows = V6 **2,980** + V7 **7,520** + V8 **14,092** + V9 **16,163** + V10 **12,748** + V11 **7,380** + V12 candidate **7,061**. The V12 candidate cohort is structurally clean / duplicate-clean / gap-closed but **not blind-label QA'd**. Older public files remain under `v11/`, and tags `v7.0.0` through `v11.0.0` remain for continuity.
- **fitz-gov-v1 discarded archive on HF**: `yafitzdev/fitz-gov-v1-discarded-archive` (private dataset, tag `20260618-reset-archive`). This is the audit-only archive for rejected sage-shaped rows, g5.7/g5.8 candidates, and failed repair artifacts from the 2026-06-18 reset. It is not training data. The archive contains **5,126** raw small files and a compressed package for the 49 large JSONL files: `compressed_large_jsonl/missing_large_jsonl_raw.tar.zst` (**873,676,053** bytes, SHA256 `98dc1f88c810665bfa555ce72e05b64abac6cc70d985b92c3a83708a75d1c103`) plus `compressed_large_jsonl/missing_large_jsonl_manifest.json`.
- **fitz-gov V10 public expansion**: V10 target-10 corpus-aware retrieval-planning expansion is published and QA-clean. The V10 block has **12,748/12,748** blind-label agreement, **0** triage, **0** missing/invalid/error. Target matrices are closed at target: modality obligation **651/651**, gap-type tail **756/756**, taxonomy tail **1,134/1,134**, each with **0** remaining gap. Upload verification loaded `yafitzdev/fitz-gov` revision `v10.0.0` and confirmed the expected Hub siblings.
- **fitz-gov V8 local rows**: the active local data access point is `C:/Users/yanfi/PycharmProjects/fitz-gov/data/fitz-gov/cases.jsonl`. There is no separate canonical `v8_manifest.jsonl`; derive V8-only indexes from `cases.jsonl` when needed. Full V8 audit reports **14,092** V8 rows, training-schema completeness is **14,092/14,092**, target-50 coverage is **483/483 cells / 0 gap**, and the stricter all-Claude/Codex full V8 second pass is **14,092/14,092 agreement**, **0 missing / 0 invalid / 0 error**, **0 triage**. V8.2 retrieval-control validation shows **24,592/24,592** rows with labels, **0** missing, **0** duplicate active row indexes after cleanup, **0** read errors, and **0** enum/range/shape errors; the V8.2 HF upload was verified by loading revision `v8.2.0` from the Hub.
- **fitz-gov V9 public expansion**: V9 retrieval-control answerability expansion is published on Hugging Face as `yafitzdev/fitz-gov`, config/tag **v9.0.0**, commit `874fd18d4952eec0e72b6df2264f8281615fd350`. Public splits are train **32,625** / validation **4,104** / test **4,026** and include `v9/split_assignments.jsonl`. The vault is **40,755** rows = V6 **2,980** + V7 **7,520** + V8 **14,092** + V9 **16,163**. The V9 target matrix is **189/189** non-direct-answer cells at **100/cell**, **0** empty cells, **0** remaining gap. Final report: `C:/Users/yanfi/PycharmProjects/fitz-gov/data/_workspaces/qa/v9_answerability_closure_buffer_20260612/gap_after_closure_buffer_validated_merge.json`.
  Validated-only merge sequence: initial target-100 block **13,988/15,394** validated and merged, replacement block **1,219/1,406**, final replacement block **177/187**, closure buffer **29/30**. Triage rows were skipped at every stage. The final active V9 set is QA-gated locally, not merely structurally generated.
- **pyrrho V8 local prep**: `scripts/prepare_data.py` now defaults to HF `v8` / `v8.0.1`; existing `data/processed_v8` was built on the same row-equivalent V8 splits with **train=19,674 / eval=2,459 / test=2,459**. Candidate append support generated `data/processed_v8_plus_structured_candidate`, `data/processed_v8_plus_code_candidate`, `data/processed_v8_plus_structured_code_candidate`, `data/processed_v8_plus_structured_code_patch_candidate`, `data/processed_v8_plus_structured_code_retry_patch_candidate`, and `data/processed_v8_plus_structured_code_missing_evidence_patch_candidate` for local modality probes without merging candidate rows into active fitz-gov. `--split-manifest` can rebuild from local `fitz-gov/data/fitz-gov/cases.jsonl` plus `data/_workspaces/qa/sdgp_v8_qa/split_assignments.jsonl` without Hugging Face/API access. `scripts/prepare_moe_data.py --strict` wrote `data/moe_v8` with the same splits, **0** required-field misses, **23** taxonomy patterns, and route IDs for all 8 semantic expert groups; rebuild from `v8.0.1` before any new training prep that needs `meta.modality`.
- **Stopped partial LM Studio V8 blind-label pass**: user explicitly asked to stop the overnight LM Studio run on 2026-05-26 morning. The background worker PID **3712** was killed, LM Studio/server processes were stopped, and the partial predictions file is preserved at `C:/Users/yanfi/PycharmProjects/fitz-gov/data/sdgp_v8_qa/blind_label_predictions_v8_target50_full_lmstudio_qwen36_35b_q5_20260526.jsonl` with **4,164/14,092** rows. Do not treat this as a completed full-cohort QA score. The run is resumable with `scripts/sdgp_run_blind_label.py` because the output file contains case IDs for completed rows.
- **Mixed LM Studio + Claude remainder QA did not pass**: Claude Code labeled the **9,928** rows not covered by the stopped LM Studio pass in `C:/Users/yanfi/PycharmProjects/fitz-gov/data/sdgp_v8_qa/claude_remainder_blind/`. The combined file `C:/Users/yanfi/PycharmProjects/fitz-gov/data/sdgp_v8_qa/blind_label_predictions_lmstudio4164_claude_remainder_combined.jsonl` has **14,092** unique case IDs and scores **13,871/14,092 agreement** with **221** triage rows at `C:/Users/yanfi/PycharmProjects/fitz-gov/data/sdgp_v8_qa/score_lmstudio4164_claude_remainder_combined_20260526/`. All **221** disagreements are from provider `lm_studio`; Claude residual rows had **0** disagreements. Treat this as evidence that the stopped LM Studio partial is not usable as a clean QA component.
- **Stricter full V8 second-pass QA is now clean after 87-row repair**: the all-Claude/Codex full second pass initially found **87** false-trustworthy triage rows in the 4,164-row hard V8-gap replacement subset. Those active vault rows were rewritten to remove ambiguous source-of-record/final-status wording and make the intended conflict or exact-version gap explicit. Repair backup: `C:/Users/yanfi/PycharmProjects/fitz-gov/data/sdgp_vault_v51_enriched/cases.before_v8_second_pass_triage87_repair_20260526_102013.jsonl`; repair batch marker: `v8_second_pass_triage87_repair_20260526`. Narrow repair blind recheck: `C:/Users/yanfi/PycharmProjects/fitz-gov/data/sdgp_v8_qa/score_second_pass_triage87_repair_only_20260526/` = **87/87 agreement**, **0 triage**. Final all-Claude/Codex full score: `C:/Users/yanfi/PycharmProjects/fitz-gov/data/sdgp_v8_qa/score_claude_full_repaired87_combined_20260526/` = **14,092/14,092 agreement**, **0 missing / 0 invalid / 0 error**, **0 triage**. Final combined predictions: `C:/Users/yanfi/PycharmProjects/fitz-gov/data/sdgp_v8_qa/blind_label_predictions_claude_full_repaired87_combined_20260526.jsonl`.
- **V7 10.5k target reached + schema unified + blind-label triage repaired + cross-label query review clean + schema-clean V7.0.1 published:** SDGP cell-targeted generation totals **10,500 rows**: 2,980 V6 + **7,520 V7**. Strict full training-schema audit reports V6 **2,980/2,980** and V7 **7,520/7,520** complete against the rich V6/MoE schema after removing pre-SDGP report axes. Every row has canonical `evaluation`; duplicate legacy/compatibility aliases have been removed. All **2,348 V7 TRUSTWORTHY rows** carry evaluator quality constraints, and **0** V6/V7 TRUSTWORTHY rows are missing them. Gap detector rerun on 2026-05-24: target 20/cell and 25/cell are complete across all **378/378** primary taxonomy cells; target 30/cell is a stretch backlog with **20 / 378** cells at target and **1,575** rows remaining. Fresh reports are under `fitz-gov/data/sdgp_vault_v51_enriched/coverage_report_v7_target{20,25,30}.md`. QA audit artifacts exist under `fitz-gov/data/sdgp_v7_qa/`: query-grouped split assignments with **0 query-group leakage**, duplicate reports, full blind-label resolution ledgers, and cross-label semantic review artifacts. Exact dedup remains clean (**0 duplicate IDs, 0 duplicate exact inputs, 0 duplicate checker hashes**). Repeated raw queries remain by design (**562 exact-query duplicate groups**, **218 cross-label exact-query groups**) because the governed input is `(query, contexts)`; semantic review passed with **0** cross-label pairs sharing the same exact context set, **1** shared-context pair adjudicated valid, and **0** unresolved review pairs. Full LM Studio `qwen3.6-35b-a3b` blind-label coverage is **7,520 / 7,520 V7 rows**: **7,520 validated / 0 triage**. The original **842** triage rows were closed by strict prompt/parser recheck (**362**), provider-assisted repair passes (**389 + 52 + 21**), and manual holdout repair (**18**). `fitz-gov/data/sdgp_v7_qa/training_excluded_triage_case_ids.txt` is empty. **pyrrho-side `g2` encoder training/evaluation has passed.**
- **`pyrrho-nano-g2` local release dir**: `models/pyrrho-nano-g2/` mirrors the HF release. Training summary: `outputs/multi_seed_g2/summary.json` reports held-out test **95.24 ± 0.48% accuracy / 3.48 ± 0.40% false-trustworthy** across seeds 42/1337/7. Config: `configs/encoder/modernbert_base_g2.yaml`. Prepared V7.0.1 data: `data/processed_v7` with train=8,400 / eval=1,050 / test=1,050 / tier0=0 and canonical breakdown columns only.
- **`pyrrho-nano-g3` V8 encoder release**: `configs/encoder/modernbert_base_g3_v8.yaml` trained ModernBERT on the fitz-gov V8 row set via `data/processed_v8` (train=19,674 / eval=2,459 / test=2,459); that same row set is now published as `v8.0.1` with explicit unstructured modality metadata. Training summary at `outputs/multi_seed_g3_v8/summary.json`: held-out test **97.52 ± 0.43% accuracy / 1.42 ± 0.16% false-trustworthy** across seeds 42/1337/7. Per-seed best checkpoints and detailed reports are under `outputs/multi_seed_g3_v8/seed_*/`. Release dir `models/pyrrho-nano-g3/` is built from seed **1337** and live on HF at `yafitzdev/pyrrho-nano-g3`.
- **`pyrrho-nano-g3.1` published multitask encoder package**: `configs/encoder/modernbert_base_g3_1_multitask.yaml` trains a custom ModernBERT multitask encoder on `data/multitask_v8_1_query_contract` (train=19,674 / eval=2,459 / test=2,459). The prepared rows require `routing.query_contract` and preserve query-only text for pre-retrieval classification. Training summary at `outputs/pyrrho-nano-g3_1_multitask/summary.json`: held-out test **97.84 ± 0.15%** governance accuracy / **0.85 ± 0.07%** false-trustworthy, **94.24 ± 0.28%** query-contract macro F1, **93.41 ± 0.32%** route accuracy, **89.26 ± 0.23%** taxonomy accuracy, and **0.0592 ± 0.0005** scalar MAE across seeds 42/1337/7. Local release dir `models/pyrrho-nano-g3.1/` is built from seed **7** with threshold **0.39** and passed CPU package verification; HF repo `yafitzdev/pyrrho-nano-g3.1` is live at latest checked commit `811bc4940d356ddf7b5bcf9102f211622d326cf1`; downloaded package snapshot verifies at `outputs/hf_download_pyrrho_nano_g3_1_final_verify/`. Per-seed training checkpoints and reports remain under `outputs/pyrrho-nano-g3_1_multitask/seed_*/`.
- **`pyrrho-nano-g2.1-v8-probe` local experiment**: `configs/encoder/modernbert_base_g2_v8_probe.yaml` retrained ModernBERT on `data/processed_v8_probe`, which preserves the published V7 split contract and appends the 525-row V8 cohort by manifest (`+414 train / +54 eval / +57 test`). Training summary at `outputs/multi_seed_g2_1_v8_probe/summary.json`: mixed held-out test **95.51 ± 0.43% accuracy / 3.56 ± 0.38% false-trustworthy** across seeds 42/1337/7. Recovered automotive ECU OOD comparison artifact: `outputs/automotive_ood_probe/comparison.json`.
- **`pyrrho-nano-g2.1-v8-verdict-patch` failed ablation**: `configs/encoder/modernbert_base_g2_v8_verdict_patch.yaml` is historical and points to the old clean 630-row manifest. Training summary at `outputs/multi_seed_g2_1_v8_verdict_patch/summary.json`: held-out test **94.92 ± 0.41% accuracy / 4.08 ± 0.92% false-trustworthy**. ECU OOD comparison artifact: `outputs/automotive_ood_probe/comparison_v8_verdict_patch.json`. Do not publish; it regressed OOD mean to **7.33/10**.
- **`pyrrho-nano-g2.2` local retrain**: `configs/encoder/modernbert_base_g2_2.yaml` trained ModernBERT on `data/processed_v8_balanced_controls`: published V7 plus local V8 append counts **train +661 / eval +97 / test +82**, producing train=9,061 / eval=1,147 / test=1,132. Training summary at `outputs/multi_seed_g2_2/summary.json`: held-out test **95.49 ± 0.15% accuracy / 3.06 ± 0.61% false-trustworthy**. ECU OOD artifact: `outputs/automotive_ood_probe/comparison_g2_2.json`. Do not publish yet; it improves FT but does not beat the original V8 probe on OOD.
- **`pyrrho-nano-g2.3-v8-claude4200` local prep**: `configs/encoder/modernbert_base_g2_3_v8_claude4200.yaml` and `data/processed_v8_claude4200` are now historical because they target the older 4,200-row V8 vault. Rebuild pyrrho prep from the active 14,092-row V8 target-50 vault before the next 3-seed run.
- **Aviation maintenance OOD probe**: `scripts/aviation_ood_probe.py` scored `g2`, `g2.1-v8-probe`, and `g2.2` on 10 new airworthiness/maintenance cases. Exact-query leakage check is clean (**0/10** matches in V7, V8 probe, and V8 balanced-control processed data). `g2.2` is best at **8.67/10**, but the hardest miss is still a known resolved/superseded-candidate boundary, not a new aviation-only taxonomy gap.
- **pyrrho GitHub repo**: public, redesigned README in the fitz-sage style.
- **pyrrho is in production.** fitz-sage **v0.13.0** (shipped 2026-05-15, PyPI + GitHub) replaced its constraint+sklearn governance cascade with `yafitzdev/pyrrho-nano-g1` — loaded as INT8 ONNX, ~30 ms/decision on CPU, zero LLM calls on the governance path. The same release also swapped fitz-sage's chat-call reranker for `Alibaba-NLP/gte-reranker-modernbert-base` (a separate ONNX cross-encoder — fitz-sage's call, applying pyrrho's pattern). See LOG 2026-05-15.
  - Release: https://github.com/yafitzdev/fitz-sage/releases/tag/v0.13.0
  - PyPI: https://pypi.org/project/fitz-sage/0.13.0/
- **`pyrrho-small-g1` release dir staged locally** at `models/pyrrho-small-g1/` — LoRA adapter (`adapter_model.safetensors` + `adapter_config.json`), tokenizer files, chat template, and a 3-seed model card. **Not on HF** pending a fix for the false-trustworthy gate (see Known limitations above).
- **`pyrrho-small-g1.1` release dir staged locally** at `models/pyrrho-small-g1.1/` — same layout, includes the model card that documents the class-weight + label-smoothing recipe. **Not on HF** — still fails the FT gate (9.31% vs 5.7%), though it's strictly closer to the target than g1.
- **Run `huggingface-cli upload yafitzdev/pyrrho-small-g1.1 models/pyrrho-small-g1.1/`** after `huggingface-cli login` if you want to push either as a research artifact.

## Immediate next actions

The integration milestone is **closed** — pyrrho v1 is shipped and
`pyrrho-small-g1` is the first generative SLM data point in the family.
Everything below is model-quality upside on an already-live baseline.

Current immediate next action as of 2026-06-21: freeze v2 generation and do not publish or wire the v2 alpha line yet. The v2.1 overlay is now structurally clean and compatibility-clean. Next useful work is to merge the overlay with the original 50k evidence packs into a pyrrho-ready v2.1 data view, then run a fresh stratified 5k blind-QA probe before training another v2 checkpoint.

1. ~~**Phase 0: V5.1 schema enrichment**~~ **COMPLETE (2026-05-20).** All 2,980 V5.1 cases LLM-enriched with V6+ schema fields (query_rewritten, context summaries, governance signals, boundary_proximity, near_miss_reason) via Sonnet subagents + LM Studio local worker. 0 TODO markers remain in vault. See LOG 2026-05-20 evening.

   **Phase 0c: V6 completion** **COMPLETE (2026-05-21).** Added the 4 MoE-training fields: per-chunk `boundary_quality`, per-case `governance.evidence_bias_score`, per-case `input.evidence_chain.{order,reasoning}` (multi-chunk only), and per-case `meta.grounding_targets` (`gold_answer` + per-sentence `attributions`, TRUSTWORTHY only). **All 2,980/2,980 cases complete (100%).** Final case (`t1_qualify_medium_101`, Terravax vaccine query — denied by Sonnet's safety classifier on every attempt) backfilled via LM Studio (qwen3.6-27b local). Re-uploaded to `yafitzdev/fitz-gov` v6.0.0 (16.4 MB, up from 12.9 MB).

2. ~~**V7 schema completion + expansion to 10.5k + QA + publish**~~ **COMPLETE (2026-05-24).** Hugging Face `yafitzdev/fitz-gov` reached **V7.0.1** with default `v7` query-grouped splits: train=8,400 / validation=1,050 / test=1,050. V7.0.1 is the schema-clean public contract used by `pyrrho-nano-g2`: same rows/splits/labels as V7.0.0, no `meta.domain`, `meta.subcategory`, `meta.reasoning_type`, `meta.query_type`, or `meta.evidence_pattern` in public rows, rich training-schema audit clean for V6/V7, full Qwen second-pass coverage **7,520/7,520** with **0 triage**, and cross-label exact-query semantic review **0** unresolved pairs. It is now superseded as the dataset default by V8.0.1, but remains the `g2` model contract.

3. ~~**Phase 3 / `pyrrho-nano-g2` train/package/publish**~~ **COMPLETE (2026-05-24).** `scripts/prepare_data.py` now reads HF V7 and preserves published train/validation/test splits. 3-seed encoder validation passed on held-out test: **95.24 ± 0.48% accuracy / 3.48 ± 0.40% false-trustworthy**. `models/pyrrho-nano-g2/` is staged locally and live at `yafitzdev/pyrrho-nano-g2`.

4. ~~**Rebuild pyrrho V8 prep from published fitz-gov V8.0.0**~~ **COMPLETE (2026-05-26 late morning; defaults updated to V8.0.1 on 2026-05-27).** `scripts/prepare_data.py` now defaults to HF revision `v8.0.1` / config `v8`; the existing local `data/processed_v8` row set has **train=19,674 / eval=2,459 / test=2,459**.

5. ~~**Next encoder run: `pyrrho-nano-g3` / V8 ModernBERT ablation**~~ **COMPLETE (2026-05-26 evening).** `configs/encoder/modernbert_base_g3_v8.yaml` trains ModernBERT on `data/processed_v8`; 3-seed validation passed on held-out V8 test with **97.52 ± 0.43% accuracy / 1.42 ± 0.16% false-trustworthy**.

6. ~~**Package `pyrrho-nano-g3` for release**~~ **COMPLETE (2026-05-26 evening).** `models/pyrrho-nano-g3/` is built from seed 1337, includes safetensors plus FP32/INT8 ONNX external-data pairs, passed packaged smoke (**11/11**) and ruff checks, and is live at `yafitzdev/pyrrho-nano-g3` (HF commit `397393718985e7bfa101042e89ecc60103e9c447`).

7. ~~**MoE Stage 0 tiny route prototype**~~ **COMPLETE (2026-05-26 late morning).** `scripts/train_moe.py` runs end-to-end on `data/moe_v8`. Full 3-epoch prototype: **82.47%** test accuracy, **5.63%** FT, **81.09%** route accuracy, **65.80%** taxonomy accuracy. This is a plumbing/prototype result, not a release candidate.

8. ~~**MoE upcycling decision**~~ **COMPLETE (2026-05-26 late morning; repaired after real-weight inspection).** First upcycling target is `configs/moe/pyrrho_moe_g3_alpha_qwen.yaml`: Qwen tokenizer/vocab preserved, 28 layers, KV=8, `head_dim=128`, 48 experts/layer, FFN dim 1056, 15 scalar heads, **4.083B total / 0.424B active inclusive**.

9. ~~**MoE upcycling inspector**~~ **COMPLETE (2026-05-26 late morning).** `scripts/upcycle_dense_to_moe.py --inspect-only` validates Qwen/config compatibility and writes `outputs/moe/upcycling/qwen_alpha_inspect.json`; `--real-weight-smoke` verifies real Qwen safetensors and writes `outputs/moe/upcycling/qwen_alpha_real_weight_smoke.json`; all direct-copy checks pass and FFN compression is explicitly **3072 -> 1056**.

10. ~~**MoE seed-pack materialization**~~ **COMPLETE (2026-05-26 late morning).** Local artifact: `outputs/moe/upcycling/qwen_alpha_seed_pack/` with 30 shards / 310 tensors / 8.166 GB and a passing meta-model shape validation report.

11. ~~**MoE governance wrapper smoke**~~ **COMPLETE (2026-05-26).** `src/pyrrho/moe/qwen_governance.py` wraps the Qwen3-MoE trunk with governance / route / taxonomy / scalar heads; `scripts/smoke_moe_qwen_wrapper.py` loaded the seed pack on CUDA and produced valid no-training outputs.

12. ~~**MoE Stage 1 heads-only training smoke**~~ **COMPLETE (2026-05-26).** `scripts/train_moe_qwen_heads.py` ran 2 CUDA optimizer steps with the trunk frozen and wrote `outputs/moe/qwen_heads_stage1_smoke/train_report.json`.

13. ~~**MoE Stage 1 bounded Qwen-head sweep**~~ **COMPLETE (2026-05-26 afternoon).** Frozen-trunk Qwen heads train end-to-end and calibration works, but full-eval quality plateaus around **53.5-54.7%** calibrated accuracy at the FT gate; full-data scaling and a first split-LR internal-router probe were worse. Treat this as a negative Stage 1 result for release purposes.

14. ~~**MoE Stage 1 adapter/distillation v2 probes**~~ **COMPLETE (2026-05-26 night).** Added g3 teacher-logit sidecars, governance KL distillation, physical Qwen expert residual adapters, semantic-route pooled adapters, save/load support, and CUDA smokes. Bounded 2,048-row / 512-eval probes were negative: physical expert adapters + distillation scored **50.00%** calibrated accuracy / **4.40%** FT / **24.02%** route; semantic-route adapters + distillation scored **44.34%** / **1.65%** FT / **26.37%** route. These do not clear the **>80%** continuation gate.

15. ~~**MoE Stage 0 route-first distillation diagnostics**~~ **COMPLETE (2026-05-26 night).** Added full g3 teacher-logit sidecars, Stage 0 governance KL distillation, loss-weight CLI overrides, and oracle-route eval. Best route-first Stage 0 diagnostic (`outputs/moe/stage0_route_proto_distill_g3_route15/final_metrics.json`) reached **82.43%** calibrated test accuracy / **5.45%** FT / **82.80%** route. This clears the per-run thresholds locally and shows the route signal is learnable when it controls the active expert path.

16. ~~**MoE Stage 0.5 route-coupled custom student**~~ **COMPLETE (2026-05-26 night).** Added `RouteCoupledMoEForGovernance`, a 53.86M-param hash-token student where the selected semantic route drives every residual expert layer; `train_moe.py` / `eval_moe.py` now load both tiny and route-coupled checkpoints. Three-seed full V8 run (`outputs/moe/stage0_5_route_coupled_g3_3seed/summary.json`) reached **83.91 ± 1.18%** calibrated held-out accuracy / **5.55 ± 0.03%** FT / **82.92 ± 0.35%** route / **67.64 ± 1.23%** taxonomy. This clears the 3-seed continuation bar and shows route-coupled capacity can scale above Stage 0 without using the failed Qwen adapter path.

17. ~~**MoE Stage 0.5 failure reporting**~~ **COMPLETE (2026-05-26 night).** Added `scripts/analyze_moe_failures.py` and generated eval/test reports under `outputs/moe/stage0_5_route_coupled_g3_3seed/failure_analysis_{eval,test}/`. The main design signal is that route prediction is good enough for the next probe, while taxonomy/support-pattern handling is weak: `consistent_chain`, `multi_source_corroboration`, and `quantitative_consensus` are low-accuracy TRUSTWORTHY support patterns, and `factual_contradiction` plus partial-overlap/absence patterns drive FT risk.

18. ~~**MoE Stage 0.6 token route-coupled scaffold**~~ **COMPLETE (2026-05-26 night).** Added `TokenRouteCoupledMoEForGovernance`, `configs/moe/pyrrho_moe_stage0_6_token_route_coupled.yaml`, loader support in `train_moe.py` / `eval_moe.py` / `analyze_moe_failures.py`, and targeted governance sample weights for support-pattern recall plus science/medicine and factual-contradiction FT risk. Bounded CUDA smoke and standalone reload passed at `outputs/moe/stage0_6_token_route_coupled_smoke/`.

19. ~~**MoE Stage 0.6 quality probe / 3-seed stability**~~ **COMPLETE (2026-05-27 morning).** `outputs/moe/stage0_6_token_route_coupled_g3_3seed/summary.json` reached **87.23 ± 1.29%** calibrated held-out accuracy / **2.92 ± 1.06%** FT / **86.06 ± 0.94%** route / **71.97 ± 0.72%** taxonomy. This is the new custom-trunk baseline.

20. ~~**MoE Stage 0.6b-e support-recall recipe sweep**~~ **COMPLETE (2026-05-27 morning).** Added per-pattern support weighting and ran seed-42 probes for 0.6b/0.6c/0.6d/0.6e. Findings: 0.6c/0.6d can improve `multi_source_corroboration` and `quantitative_consensus`, but leak FT; 0.6e guards FT but collapses support recall. Do not scale these recipes.

21. ~~**MoE Stage 0.7 support-aggregation architecture**~~ **COMPLETE (2026-05-27).** Added `SupportAggregatingMoEForGovernance`, query/source dataset tensors, loader/evaluator/failure-analysis support for `model_kind: support_aggregating_token`, and `configs/moe/pyrrho_moe_stage0_7_support_aggregation.yaml`. Four-epoch 3-seed run (`outputs/moe/stage0_7_support_aggregation_g3_3seed/summary.json`) reached **89.49 ± 0.47%** calibrated held-out accuracy / **3.06 ± 0.45%** FT / **82.61 ± 2.50%** route / **75.78 ± 0.21%** taxonomy. It is quality-positive and fixes support-pattern recall, but it is not a clean safety domination of Stage 0.6 because `science_medicine` and `factual_contradiction` FT worsened.

22. ~~**MoE Stage 0.7b-d guarded support aggregation probes**~~ **COMPLETE (2026-05-27).** Added guarded recipe configs and ran seed-42 probes for 0.7b/0.7c/0.7d. 0.7b restored safety best but gave back too much support recall; 0.7c and 0.7d did not dominate. Existing scalar risk and threshold gating also fail to preserve support while restoring safety. Do not scale these recipes.

23. ~~**MoE Stage 0.8 architectural guard scaffold**~~ **COMPLETE / NEGATIVE (2026-05-27).** Added `GuardedSupportAggregatingMoEForGovernance`, a learned TRUSTWORTHY-penalty path on the Stage 0.7 support state. Smoke/reload/tests passed, but seed-42 quality was negative: 4 epochs **85.28% / 5.57% FT**, 3 epochs **85.40% / 2.31% FT**. It is too conservative/unstable and should not be scaled.

24. ~~**MoE Stage 0.9 explicit trust-guard target**~~ **COMPLETE / NEGATIVE (2026-05-27).** Added `TrustGuardedSupportAggregatingMoEForGovernance`, a decoupled binary trust verifier over Stage 0.7 candidate logits/support state, plus `configs/moe/pyrrho_moe_stage0_9_trust_guarded_support_aggregation.yaml`. Smoke/reload/tests passed, but seed-42 quality was negative: 3 epochs **86.50% / 1.24% FT**, 4 epochs **84.75% / 0.59% FT**. It restores safety by suppressing TRUSTWORTHY support recall too aggressively (`multi_source_corroboration` **45.16%** at 3 epochs, **38.71%** at 4 epochs). Do not scale this implementation.

25. ~~**MoE Stage 0.7 frozen-output post-hoc verifier**~~ **COMPLETE / POSITIVE (2026-05-27).** Added `scripts/train_moe_posthoc_verifier.py`, trained HGB verifiers on frozen Stage 0.7 candidate outputs for seeds 42/1337/7, selected verifier thresholds on eval with target FT **2.5%** and max eval accuracy drop **1.5%**, and wrote `outputs/moe/stage0_7_posthoc_verifier_g3_3seed/summary.json`. Held-out test moved from the verifier-script baseline **89.37 ± 0.59% / 2.94 ± 0.36% FT** to guarded **88.97 ± 0.51% / 1.99 ± 0.17% FT**. This is the first positive guard path, but it is a post-hoc reranker over frozen logits and still costs support recall.

26. ~~**MoE Stage 0.7 post-hoc verifier support-retention tune**~~ **COMPLETE / POSITIVE (2026-05-27).** Reran the frozen-output verifier with target eval FT **2.8%** and max eval accuracy drop **1.5%** across seeds 42/1337/7. Artifact: `outputs/moe/stage0_7_posthoc_verifier_g3_3seed_ft028/summary.json`. Held-out test is **89.29 ± 0.69% / 2.37 ± 0.26% FT**, with T recall **80.20 ± 1.64%**. This is the preferred post-hoc guard operating point because it preserves support substantially better than the 2.5% safety-heavy setting.

27. ~~**MoE Stage 0.7 post-hoc verifier minimal-intervention/support-aware check**~~ **COMPLETE (2026-05-27).** Ran target eval FT **3.0%** across seeds 42/1337/7 and wrote `outputs/moe/stage0_7_posthoc_verifier_g3_3seed_ft030/summary.json`: **89.35 ± 0.64% / 2.61 ± 0.36% FT**, T recall **80.85 ± 1.30%**. It preserves support best but is a weaker safety improvement and leaves seed 7 unchanged. Added support/risk metrics plus optional support-aware selection constraints to `scripts/train_moe_posthoc_verifier.py`; a seed-42 support-aware probe showed the 2.8% target cannot keep eval support drop under 3 pts, so target-FT remains the practical knob.

28. ~~**MoE Stage 0.7 post-hoc verifier package/reload/runtime**~~ **COMPLETE (2026-05-29).** Added `scripts/package_moe_posthoc_verifier.py` with `create` and `evaluate` subcommands, hardened the package behind `src/pyrrho/moe/posthoc_verifier.py`, and added `src/pyrrho/moe/inference.py` plus `scripts/infer_moe_posthoc.py` as the end-to-end local inference harness. Preferred 2.8% verifier package: `outputs/moe/stage0_7_posthoc_verifier_g3_ft028_package/` with `manifest.json`, copied per-seed verifier artifacts, feature schema width **120**, and `package_eval_report.json`. Full eval+test reload reproduced packaged per-seed metrics with **0** max absolute delta; held-out test remains **89.29 ± 0.69% / 2.37 ± 0.26% FT**. Runtime smokes: package reload on 8 test rows per seed passed at **95.83% / 0.00% FT** and wrote `package_eval_report_smoke.json`; CLI inference over 8 seed-42 test rows wrote `inference_smoke.jsonl` with **2** verifier demotions.

29. ~~**MoE Stage 0.7 packaged verifier policy comparison**~~ **COMPLETE / POSITIVE (2026-05-29).** Added `src/pyrrho/moe/posthoc_policies.py` and `scripts/compare_moe_posthoc_policies.py`, then extended `scripts/infer_moe_posthoc.py` with packaged ensemble policies. Artifact: `outputs/moe/stage0_7_posthoc_policy_compare_ft028/summary.json` / `report.md`. Support-retaining recommendation is `trustworthy_quorum_2_of_3`: eval **90.81% accuracy / 1.88% FT / 83.77% T recall**, held-out test **90.65% / 1.90% FT / 81.71% T recall**. Safety-first extreme is `trustworthy_unanimous`, but held-out T recall drops to **68.61%**, so it is too conservative for the quality baseline.

30. **`pyrrho-small-g2`** — after the V8 encoder/MoE scaffold stop point, search current permissive 2026 CPU-runnable SLM bases, update `train_slm.py`/`eval_slm.py` for V8 split shape, then run the SLM baseline with asymmetric safety pressure or DPO/GRPO.

31. ~~**Tabular structured OOD probe**~~ **COMPLETE / NEGATIVE (2026-05-27).** Added `scripts/tabular_ood_probe.py`, generated `outputs/tabular_ood_probe/g3_release/`, and scored `pyrrho-nano-g3`: **55.56%** calibrated accuracy / **45.83%** false-TRUSTWORTHY. This shows structured/tabular support cannot simply be serialized into the current unstructured encoder without extra checks or training data.

32. ~~**fitz-gov structured/code candidate QA + g3 probe**~~ **COMPLETE / NEGATIVE (2026-05-28).** fitz-gov now has a candidate-only, full blind-QA-clean structured/code pack: **20,000 rows** = **10,000 structured** + **10,000 code**, **20,000/20,000** Codex blind agreement, **0** triage. Added `scripts/modality_candidate_probe.py` and scored `pyrrho-nano-g3`: full pack **52.69%** calibrated accuracy / **51.69%** false-TRUSTWORTHY; code **49.35% / 71.38% FT**; structured **56.02% / 31.99% FT**. Balanced pattern and modality-pattern subsets stay near **52.2%** accuracy / **50-51%** FT.

33. ~~**Next modality step: candidate data prep + one-seed retraining controls**~~ **COMPLETE (2026-05-28).** Added `prepare_data.py` candidate append, `eval_report.py` modality breakdowns, `tests/test_prepare_data_candidates.py`, generated the three candidate processed dirs, and ran seed-42 structured-only / code-only / joint controls. Candidate held-out modality rows scored **100.00% / 0.00% FT**, but one seed plus same-generator candidate rows were not enough to merge or publish.

34. ~~**Joint modality 3-seed stability**~~ **COMPLETE (2026-05-28).** Trained/evaluated seeds **42/1337/7** for the joint structured+code branch. Summary: `outputs/modality_retraining/structured_code_3seed_summary.json`. Mixed held-out test: **98.34 ± 0.23% accuracy / 1.14 ± 0.33% FT**. Structured and code candidate test rows are both **100.00 ± 0.00% / 0.00 ± 0.00% FT**, but unstructured test is **96.99 ± 0.41% / 2.03 ± 0.60% FT**.

35. ~~**Structured hard OOD sanity check**~~ **COMPLETE / MIXED (2026-05-28).** Scored the existing 36-row hand-authored `tabular_ood_probe` against the joint branch. Summary: `outputs/tabular_ood_probe/structured_code_3seed_summary.json`. Result improved strongly over frozen g3 (**55.56% / 45.83% FT -> 83.33 ± 4.81% / 4.17 ± 7.22% FT**), but seed 7 still false-trusts all three `missing_execution_result` serializations.

36. ~~**Code hard OOD probe**~~ **COMPLETE / NEGATIVE-MIXED (2026-05-28).** Added `scripts/code_ood_probe.py` and scored frozen g3 plus joint seeds. Frozen g3: **58.33% / 62.50% FT**. Joint branch: **77.78 ± 14.70% / 29.17 ± 27.32% FT**. Seed 42 is promising, but seeds 1337/7 leak heavily on wrong-symbol/version/missing-result and config-conflict cases.

37. ~~**Code modality axis contract/audit**~~ **COMPLETE (2026-05-28).** Added `docs/CODE_MODALITY_AXES.md` and `scripts/audit_code_modality_axes.py`. Audit output: `outputs/code_modality_axis_audit/modality_code_v1_20260527/`. The answer to the domain/language question is now explicit: semantic domains stay in `routing.expert_fired`; language/artifact/question-target/failure-mode are code-modality audit axes. The current code pack has useful mechanism coverage but concrete gaps: **0** direct control-flow support rows, **0** missing-specific-field rows, and **239/10,000** extension/syntax mismatch flags.

38. ~~**Targeted code hard-negative/coverage patch**~~ **STRUCTURAL-CLEAN / QA DEFERRED (2026-05-28).** Added fitz-gov `scripts/sdgp_generate_code_modality_patch.py`, generated `data/_workspaces/handoff/modality_code_patch_v1_20260528/` with **720** candidate-only rows, and built blind-label QA files plus **12** Codex blind shards at `data/_workspaces/qa/modality_code_patch_v1_20260528/`. Structural validation passes with **0 errors**. Pyrrho audit: patch-only **0/720** syntax mismatches; original+patch **10,720** rows and **0** audited hard-code OOD target gaps. Qwen blind QA found two ambiguous families, which were repaired; targeted reruns were clean (**10/10** config/runtime, **10/10** wrong-symbol). Full blind QA was paused at **115** predictions by user decision; labels are treated as correct only for local controls and must be validated later.

39. ~~**Label-trusted joint+patch 3-seed control**~~ **COMPLETE / LABEL-TRUSTED ONLY (2026-05-29).** Prepared `data/processed_v8_plus_structured_code_patch_candidate` with published V8.0.1 + 20,000 structured/code candidate rows + the 720-row code patch, then trained/evaluated seeds **42/1337/7**. Summary: `outputs/modality_retraining/structured_code_patch_3seed_summary.json`. Calibrated held-out test: **98.52 ± 0.13% accuracy / 1.06 ± 0.22% FT** overall; code **100.00 ± 0.00% / 0.00 ± 0.00% FT**, structured **100.00 ± 0.00% / 0.00 ± 0.00% FT**, unstructured **97.28 ± 0.25% / 1.92 ± 0.40% FT**. Hand-authored code OOD: **95.37 ± 3.21% accuracy / 6.94 ± 4.81% FT**, improved from the unpatched joint branch but not clean.

40. ~~**Retry-limit conflict patch + 3-seed control**~~ **COMPLETE / LABEL-TRUSTED ONLY (2026-05-29).** Added fitz-gov `scripts/sdgp_generate_code_retry_conflict_patch.py` and generated `data/_workspaces/handoff/modality_code_retry_conflict_patch_v1_20260529/` with **360** candidate-only rows. Prepared `data/processed_v8_plus_structured_code_retry_patch_candidate` offline with local split assignments and trained seeds **42/1337/7** at `outputs/modality_retraining/structured_code_retry_patch_seed{42,1337,7}/`. Summary: `outputs/modality_retraining/structured_code_retry_patch_3seed_summary.json`. Held-out test: **98.62 ± 0.15% accuracy / 1.06 ± 0.05% FT** overall; code and structured slices are **100.00 ± 0.00% / 0.00 ± 0.00% FT**; unstructured is **97.47 ± 0.28% / 1.94 ± 0.09% FT**. Code OOD improves to **99.07 ± 1.60% / 1.39 ± 2.41% FT**; targeted retry-limit `constant_config_conflict` is **100.00 ± 0.00% / 0.00 ± 0.00% FT**. One inherited OOD miss remains: seed 42 `code_10_missing_audit__diff_context`.

41. ~~**Missing-evidence modality patch exposed-seed control**~~ **COMPLETE / LABEL-TRUSTED ONLY (2026-05-29).** Added fitz-gov `scripts/sdgp_generate_missing_evidence_patch.py` and generated `data/_workspaces/handoff/modality_missing_evidence_patch_v1_20260529/` with **360** candidate-only rows spanning code diff-context missing fields plus structured missing-result/exact-row/conflict controls. Prepared `data/processed_v8_plus_structured_code_missing_evidence_patch_candidate` offline with local split assignments and trained exposed seeds **42/7**. Summary: `outputs/modality_retraining/structured_code_missing_evidence_patch_2seed_summary.json`. Held-out test: **98.70 ± 0.18% accuracy / 0.75 ± 0.25% FT** overall; code and structured slices are **100.00 ± 0.00% / 0.00 ± 0.00% FT**; unstructured is **97.56 ± 0.35% / 1.39 ± 0.46% FT**. The seed-42 missing-field FT is fixed, but OOD behavior is mixed: code OOD **93.06 ± 5.89% / 2.08 ± 2.95% FT**, tabular OOD **87.50 ± 13.75% / 0.00 ± 0.00% FT**.

42. ~~**Modality specialist routing diagnostic**~~ **COMPLETE / LABEL-TRUSTED ONLY (2026-05-29).** Added `scripts/modality_specialist_compare.py` and compared the retry-patch seed-42 generalist against existing seed-42 code-only and structured-only specialists. Summary: `outputs/modality_specialist_compare/retry_patch_seed42_router/summary.json`. The retry-patch generalist remained stronger on the mixed held-out test (**98.74% / 1.07% FT**) than routing code to the code-only specialist (**98.26% / 1.46% FT**); routing structured tied on in-distribution structured rows. OOD also did not justify routing: code specialist **86.11% / 0.00% FT** but only **58.33%** TRUSTWORTHY recall; structured specialist **91.67% / 8.33% FT**.

43. ~~**Patch-aware code-specialist diagnostic**~~ **COMPLETE / LABEL-TRUSTED ONLY (2026-05-29).** Added `scripts/filter_processed_modalities.py`, prepared `data/processed_v8_plus_code_retry_patch_candidate`, trained seed 42 at `outputs/modality_retraining/code_retry_patch_seed42/`, and compared it through `outputs/modality_specialist_compare/retry_patch_seed42_patch_aware_code_router/`. The specialist was perfect on held-out candidate code rows (**100.00% / 0.00% FT**) but did not improve the mixed held-out test or code OOD: routed mixed test ties retry-patch seed 42 at **98.74% / 1.07% FT**, and code OOD ties at **97.22% / 4.17% FT** while moving the miss to retry-limit `constant_config_conflict`.

44. ~~**Modality threshold-policy diagnostic**~~ **COMPLETE / LABEL-TRUSTED ONLY (2026-05-29).** Added `scripts/modality_threshold_sweep.py` and evaluated per-modality TRUSTWORTHY thresholds on the retry-patch 3-seed joint branch. Summary: `outputs/modality_threshold_sweep/retry_patch_3seed/summary.json`. Global thresholds remain best on held-out accuracy (**98.62 ± 0.12% / 1.06 ± 0.04% FT**); per-modality thresholds selected to avoid eval FT regression scored **98.61 ± 0.11% / 1.04 ± 0.05% FT**.

45. ~~**Modality patch QA readiness audit**~~ **COMPLETE (2026-05-29).** Audited fitz-gov QA artifacts without starting LM Studio, making API calls, generating rows, or scoring labels. `modality_code_patch_v1_20260528` has a 720-row blind queue/manifest and **12** Codex blind shards. The retry-conflict and missing-evidence patches did not yet have QA directories.

46. ~~**Later patch QA queue/shard preparation**~~ **COMPLETE (2026-05-29).** Built blind-label queues/manifests and **12** Codex blind shards each for `modality_code_retry_conflict_patch_v1_20260529` and `modality_missing_evidence_patch_v1_20260529` in fitz-gov. Each has **360** queue rows and **360** manifest rows; shards are **30** rows each. No label scoring was run.

47. ~~**Patch-aware structured-specialist diagnostic**~~ **COMPLETE / LABEL-TRUSTED ONLY (2026-05-29).** Prepared `data/processed_v8_plus_structured_retry_patch_candidate`, trained seed 42 at `outputs/modality_retraining/structured_retry_patch_seed42/`, and compared it through `outputs/modality_specialist_compare/retry_patch_seed42_patch_aware_structured_router/`. It tied retry-patch seed 42 on mixed held-out test (**98.74% / 1.07% FT**) but was unsafe on tabular OOD (**88.89% / 16.67% FT**), so a separate structured encoder is not justified.

48. ~~**MoE Stage 0.7 single-seed verifier-threshold approximation**~~ **COMPLETE / NEGATIVE (2026-05-29).** Added `src/pyrrho/moe/posthoc_thresholds.py` and `scripts/sweep_moe_posthoc_single_seed_thresholds.py`. Artifact: `outputs/moe/stage0_7_posthoc_single_seed_threshold_sweep_ft028/summary.json` / `report.md`. Selecting one verifier threshold per seed on eval to match the quorum's eval FT selected very strict taus (**0.945**, **0.900**, **0.950**) and held-out mean **88.55 ± 0.65% accuracy / 1.38 ± 0.18% FT / 76.26 ± 1.94% T recall**. This is safer than quorum but loses about **2.10 pp** accuracy and **5.45 pp** T recall, so threshold-only single-pass approximation does not replace `trustworthy_quorum_2_of_3`.

49. ~~**MoE Stage 0.7 single-seed quorum distillation**~~ **COMPLETE / NEGATIVE (2026-05-29).** Added `scripts/distill_moe_posthoc_quorum.py`, which trains one small HGB classifier per seed from frozen single-seed Stage 0.7 features to the packaged `trustworthy_quorum_2_of_3` target, then selects a TRUSTWORTHY threshold on eval and applies it to held-out test. Artifact: `outputs/moe/stage0_7_posthoc_quorum_distill_ft028/summary.json` / `report.md`. Held-out mean is **88.42 ± 0.48% accuracy / 1.80 ± 0.33% FT / 76.01 ± 0.85% T recall**, with **94.66 ± 0.06%** agreement to the quorum target. This preserves safety but still loses about **2.23 pp** accuracy and **5.70 pp** T recall versus the quorum, so one-seed post-hoc distillation does not replace the 3-forward policy.

50. ~~**MoE Stage 0.7 quorum gap analysis**~~ **COMPLETE (2026-05-29).** Added `scripts/analyze_moe_posthoc_quorum_gaps.py` and wrote `outputs/moe/stage0_7_posthoc_quorum_gap_analysis_ft028/summary.json` / `report.md`. The 3-forward quorum's held-out target wins over single-pass approximations are larger than approximation wins: packaged seeds **69.3 vs 36.0**, stricter single-seed thresholds **88.3 vs 36.7**, and one-seed quorum distillers **84.3 vs 29.7**. The lost rows are mainly support-preservation failures: TRUSTWORTHY rows demoted to ABSTAIN/DISPUTED, concentrated in `consistent_chain`, `multi_source_corroboration`, `quantitative_consensus`, `expert_consensus`, and `single_authoritative`.

51. ~~**MoE alpha release hardening**~~ **COMPLETE / PUBLISHED (2026-05-29).** Built `models/pyrrho-MoE-g3-alpha/` around the Stage 0.7 support-aggregation seeds plus packaged `trustworthy_quorum_2_of_3` verifier policy, then published it to Hugging Face at `yafitzdev/pyrrho-MoE-g3-alpha`. The release mirror and Hub repo contain copied seed checkpoints, per-seed verifiers, config, metadata, model card, LFS attributes, policy reports, and `PUBLISH_COMMANDS.md`. Held-out release policy remains **90.65% accuracy / 1.90% FT / 81.71% T recall**. CPU reload/inference smokes from the local release dir and a downloaded Hub snapshot passed. Do **not** restart Stage 0.10, verifier tuning, one-seed approximation work, modality work, or Qwen 4B-A0.4B scaling unless explicitly redirected.

52. ~~**Beta decision on alpha runtime shape**~~ **SUPERSEDED (2026-05-30).** The batch-profile evidence is still useful, but the user clarified that the goal is not to beta-harden the Stage 0.7 quorum alpha. Do not spend more default effort turning the quorum package into beta.

53. ~~**Current active next step: real MoE MVP audit + generative smoke path**~~ **COMPLETE / MECHANICALLY POSITIVE, QUALITY-NEGATIVE (2026-05-30).** Audited the existing Qwen-seeded 4B/A0.4B path and added `scripts/train_moe_qwen_sft.py`, a minimal generative SFT loop for the seed pack. Tiny smoke artifact: `outputs/moe/qwen_generative_sft_tiny_smoke/`. The path loads, trains, generates, parses, and reports, but after one LoRA step generation is still unparseable garbage (**0.00** JSON parse / **0.00** label parse). This confirms the next work should be format-acquisition training/runtime, not more classifier-head scaling or alpha-quorum packaging.

54. ~~**Qwen generative format-acquisition probe**~~ **COMPLETE / FORMAT-POSITIVE, QUALITY-NEGATIVE (2026-05-30).** `scripts/train_moe_qwen_sft.py` can make the Qwen-seeded 4B/A0.4B skeleton emit valid compact pyrrho JSON under broad LoRA. The 4-row overfit probe reached **100%** parse/classification/route/taxonomy on train rows, and the 64-train/16-heldout probe reached **100%** JSON+label parse. The first trusted balanced 256-train/64-heldout run predicted **TRUSTWORTHY for every eval row** (**32.8%** accuracy / **100.0%** FT). A 512-step asymmetric/classification-token weighted rerun flipped the safety failure to **0.0%** FT with **100%** parseability, but collapsed to **63/64 ABSTAIN-like** predictions and only **35.9%** accuracy. Do not scale this token-loss recipe as-is.

55. ~~**Tune explicit decision supervision for generative MoE**~~ **COMPLETE / PARTIAL POSITIVE (2026-05-30).** Added auxiliary prompt-state classification and decision-first target modes to `scripts/train_moe_qwen_sft.py`. Aux-head supervision was negative: trunk-coupled aux damaged generation, and detached aux collapsed to **64/64 DISPUTED**. Context length alone was negative: the 512-context weighted JSON run emitted **64/64 ABSTAIN**. The useful path is `label-json`: label-first JSON with mild safety weighting reached **51.6%** accuracy / **0.0%** FT with **100%** JSON+label parse on the 64-row heldout slice.

56. ~~**Scale the label-first JSON decision path, with calibration**~~ **COMPLETE / PARTIAL POSITIVE (2026-05-30).** `scripts/train_moe_qwen_sft.py` now saves/reloads Qwen-MoE LoRA adapters, label-score classification with a TRUSTWORTHY probability threshold is available, and prediction artifacts include `selected_output` JSON so the calibrated decision can be consumed separately from unsafe free-generation text. The 1k-train/256-eval bounded run improved selected classification from the earlier **51.6% / 0.0% FT** 64-row probe to **63.7% / 0.0% FT** on 256 rows, but free generation was unsafe (**68.4% / 12.9% FT**) and route/taxonomy remain weak.

57. ~~**Scale with tau 0.47, then reassess supervision**~~ **COMPLETE / POSITIVE (2026-05-30).** The 4k-train/512-eval run at `outputs/moe/qwen_generative_sft_label_json_mild_weighted_512ctx_4096x512_tau047/` reached **85.35%** selected accuracy / **5.26%** FT with **100%** JSON+label parse, **81.64%** route, and **62.30%** taxonomy. This is the first Qwen-seeded generative MoE result that clears the headline gates on a bounded eval slice, but FT is close to the **5.7%** bar and the slice is not full held-out evidence.

58. ~~**Validate the 4k adapter beyond the bounded slice**~~ **COMPLETE / POSITIVE (2026-05-30).** Added skip-generation scoring and ran full eval/test selected-label validation. Full eval selected **tau 0.50** as the best gate-passing threshold (**83.69%** accuracy / **4.29%** FT). Full held-out test at **tau 0.50** scored **82.39%** accuracy / **4.44%** FT. Both pass the headline gates for selected label-score classification.

59. ~~**MVP runtime around selected_output**~~ **COMPLETE / MINIMAL (2026-05-30).** Added `scripts/infer_moe_qwen_sft.py`, which loads the 4k adapter, scores label candidates at **tau 0.50**, emits `selected_output` as authoritative governance JSON, and keeps raw generation as audit/debug text. Full-generation and skip-generation smokes passed; override cases now replace the rationale with a label-consistent default when selected classification differs from raw generation.

60. ~~**Package the Qwen generative MVP artifact**~~ **COMPLETE / LOCAL PACKAGE (2026-05-30).** Built `models/pyrrho-MoE-g3-mvp/` with copied adapter/tokenizer files (~646 MB), metadata, full eval/test selected-label reports, bounded full-generation report, inference smokes, `manifest.json`, and `README.md`. The package references the 8 GB seed pack instead of duplicating it. Manifest JSON validates, and `scripts/infer_moe_qwen_sft.py --adapter-path models\pyrrho-MoE-g3-mvp\adapter --skip-generation` smoke passed.

61. ~~**Release hardening for the Qwen generative MVP package**~~ **COMPLETE / MIXED (2026-05-30).** Added `scripts/verify_moe_qwen_sft_package.py`; the initial adapter-only verifier passed and wrote `models/pyrrho-MoE-g3-mvp/release_verify_report.json` (then **28** files / **646,386,940** bytes; superseded by #63 for the current bundled-Q4 package). Packaged full-generation smoke on a seeded random 32-row test sample reached **100%** JSON parse, **87.50%** selected-output accuracy, **0.00%** selected FT; raw generation on the same rows was **84.38%** accuracy / **16.67%** FT. BF16 CPU smokes pass from the package: skip-generation **18.06s / 9.84 GiB** peak RSS, full-generation **323.56s / 10.16 GiB** peak RSS for 1 row. This proves CPU executability but not the intended ≤8 GiB Q4 runtime target.

62. ~~**Local bnb4 CPU runtime probe**~~ **COMPLETE / NEGATIVE (2026-05-30).** Added optional bitsandbytes quantized-load flags to `scripts/train_moe_qwen_sft.py` / `scripts/infer_moe_qwen_sft.py`. Direct CPU bnb4 base-model load succeeds (`outputs/moe/package_hardening/bnb4_cpu_load_probe.json`), but packaged-adapter selected-output inference timed out after **900s** with no prediction file (`outputs/moe/package_hardening/bnb4_cpu_adapter_skipgen_timeout.json`). Do not use local Transformers+bitsandbytes 4-bit as the release runtime.

63. ~~**Package the Q4 GGUF selected-output runtime**~~ **COMPLETE / LOCAL PUBLISH CANDIDATE (2026-05-30).** Bundled `models/pyrrho-MoE-g3-mvp/gguf/pyrrho-MoE-g3-mvp-merged-Q4_K_M.gguf` (**2,703,385,760** bytes) and `models/pyrrho-MoE-g3-mvp/patches/llama_cpp_qwen3moe_mlp_only_layers.patch`, added LFS hints, updated README/manifest to make full-sequence GGUF label scoring the authoritative low-memory CPU decision path, and hardened `scripts/verify_moe_qwen_sft_package.py` to parse GGUF evidence plus size/SHA256-check the bundled runtime files. Current package verifier passes with **34** files / **3,349,873,300** bytes and a 4-row adapter smoke.

64. ~~**Publish naming + upload/download smoke**~~ **COMPLETE / PUBLISHED (2026-05-30).** User chose the MVP name. Published [`yafitzdev/pyrrho-MoE-g3-mvp`](https://huggingface.co/yafitzdev/pyrrho-MoE-g3-mvp), final downloaded-artifact validation commit `6b44a30c531c286fcfc0d5b9b618c25c5c86441e`, **34** Hub sibling files, **3,348,978,530** bytes used storage. Final downloaded snapshot at `outputs/moe/hf_download_pyrrho_moe_g3_mvp_final/` verifies with **34** files / **3,349,869,920** bytes and passes the downloaded Q4 GGUF random-32 sequence-label smoke (**93.75% / 0.00% FT**, **4.220 GiB** peak RSS). Later metadata-only commits supersede this as the current HF head.

65. ~~**Clarify model-card architecture naming**~~ **COMPLETE / METADATA-ONLY (2026-05-31).** Updated the model card and manifest to distinguish **Project architecture: Pyrrho MoE MVP** from **Runtime / loader architecture: Qwen3MoE-compatible sparse MoE**, regenerated `release_verify_report.json`, and pushed HF commit `ff26a761ec6adf37cb99be8eaffe58142a0843ec`.

66. ~~**Add public MoE MVP run guide**~~ **COMPLETE / DOCS + HF CARD (2026-05-31).** Added `docs/PYRRHO_MOE_MVP_RUN_GUIDE.md`, linked it from `docs/INDEX.md` and the top-level `README.md`, added a quick start to the HF model card, reran the package verifier, and pushed HF commit `022825604abedec31d78f91f6706bbfd70000507`. The later LM Studio caveat refresh supersedes the HF head at commit `f637325ba5a49429952c122b14913dd4cf355411`.

67. ~~**Reset active goal to clean real MoE**~~ **COMPLETE / G4-REAL STARTED (2026-05-31).** Rewrote `docs/GOAL.md` around `pyrrho-MoE-g4-real`, froze `g3-mvp` as patched-runtime experimental evidence, added `configs/moe/pyrrho_moe_g4_real_stock_runtime.yaml`, extended the parameter counter for uneven semantic shard maps, added `scripts/audit_moe_runtime_shape.py`, and wrote `outputs/moe/g4_real_runtime_shape_gate.json`. The first candidate passes local budget/runtime-shape checks: **4.092512305B total / 0.412010545B active inclusive** with no dense-only FFN layers or `mlp_only_layers`.

68. ~~**Stock runtime carrier proof**~~ **COMPLETE / INTERNAL MIXTRAL PROBE ONLY (2026-05-31).** `MixtralForCausalLM` proved that a stock MoE carrier can convert/load without Qwen patches: tiny random structural checkpoint at `outputs/moe/g4_real_stock_runtime_carrier/mixtral_tiny_hf/`, converted with clean upstream llama.cpp at `outputs/moe/g4_real_stock_runtime_carrier/llama_cpp_stock_568aec82/convert_hf_to_gguf.py`, and loaded/generated with clean upstream `llama-cli.exe` at commit `568aec82d2fc48341c54cae565768ac75072a31d`. Report: `outputs/moe/g4_real_stock_runtime_carrier/stock_runtime_carrier_probe_report.json`. User rejected public Mixtral association, so this is only an internal proof, not the final public carrier.

69. ~~**Non-Mixtral stock carrier proof**~~ **COMPLETE / OLMOE TINY PROOF PASSED (2026-05-31).** `OlmoeForCausalLM` is the current public-carrier candidate. Tiny random checkpoint at `outputs/moe/g4_real_stock_runtime_carrier/olmoe_tiny_hf/`, GGUF at `outputs/moe/g4_real_stock_runtime_carrier/pyrrho_g4_real_olmoe_tiny_stock_converter_f16.gguf` (**15,013,248 bytes**, SHA256 `c20b55ef7818573c4d7b49153bea20a1b7a4e736b9ecc3483d53ca98c7c1a3dc`), and load report at `outputs/moe/g4_real_stock_runtime_carrier/olmoe_stock_runtime_carrier_probe_report.json`. It converted with the clean upstream llama.cpp converter and generated one token through clean upstream `llama-cli.exe`.

70. ~~**Full-size OLMoE structural load proof**~~ **COMPLETE / STOCK LLAMA.CPP + LM STUDIO PASS (2026-05-31).** Added `scripts/export_olmoe_structural_checkpoint.py`, exported `outputs/moe/g4_real_stock_runtime_carrier/olmoe_g4_real_full_random_hf/`, converted full F16 GGUF `outputs/moe/g4_real_stock_runtime_carrier/pyrrho_g4_real_olmoe_full_random_stock_converter_f16.gguf` (**7,942,296,864** bytes, SHA256 `5bc7c7e6a9f6ec4095477279937e34c95167af454148d489cfc3d242046d62da`), loaded/generated one token through clean upstream `llama-cli`, hard-link imported into LM Studio, and loaded via `lms load pyrrho-moe-g4-real-olmoe-structural --gpu off -c 128 --ttl 5 -y`. Full report: `outputs/moe/g4_real_stock_runtime_carrier/olmoe_full_stock_runtime_probe_report.json`.

71. ~~**Training/upcycling path for OLMoE carrier shape**~~ **COMPLETE / MECHANICAL SFT PASS, DONOR AUDIT DONE (2026-05-31).** Added `scripts/train_moe_olmoe_sft.py`, patched the SFT harness to compute token CE in float32, and ran the full 3.970B carrier one-step LoRA smoke. Output: `outputs/moe/olmoe_g4_real_sft_full_smoke_fp32loss/train_report.json`; adapter reload smoke: `outputs/moe/olmoe_g4_real_sft_full_smoke_fp32loss_reload/train_report.json`; donor audit: `outputs/moe/g4_real_olmoe_training_path/training_path_audit.json`; decision doc: `docs/OLMOE_TRAINING_PATH_2026-05-31.md`.

72. ~~**Donor/teacher initialization experiment**~~ **COMPLETE / FIRST OLMOE DONOR TRANSPLANT PASSED STOCK LOAD (2026-05-31).** Added `scripts/init_olmoe_from_donor.py`, downloaded `allenai/OLMoE-1B-7B-0924`, initialized the exact target OLMoE carrier at `outputs/moe/g4_real_olmoe_donor_init/olmoe_1b7b_block_resize_hf/`, converted `outputs/moe/g4_real_olmoe_donor_init/pyrrho_g4_real_olmoe_1b7b_block_resize_f16.gguf` (**7,942,296,800** bytes, SHA256 `e646c254f4bf7f11605ef6b9dc463994245ff104b6d4e711f635b05baacefaf4`), and passed clean llama.cpp plus LM Studio load as `pyrrho-moe-g4-real-olmoe-donor-init`. Runtime report: `outputs/moe/g4_real_olmoe_donor_init/donor_init_runtime_probe_report.json`. Tiny label-score baseline is quality-negative (**1/12** accuracy), so this is an initialization/runtime milestone only.

73. ~~**Bounded donor-seed SFT or teacher distillation**~~ **COMPLETE / ATTENTION-LORA PATH NEGATIVE (2026-05-31).** Bounded SFT from `outputs/moe/g4_real_olmoe_donor_init/olmoe_1b7b_block_resize_hf/` can teach some label text, but it does not produce useful governance behavior. Best small gated label-score probe reached only **39.06% accuracy / 0.00% FT**; the 1,024-row scale-up regressed to **33.59% / 0.58% FT**; generation remained malformed or collapsed; naive router/lm_head raw unfreeze went **NaN**. Summary: `outputs/moe/g4_real_olmoe_donor_init/sft_probe_summary_2026-05-31.json`.

74. ~~**Teacher distillation or stable router/expert adaptation setup**~~ **COMPLETE / MECHANICALLY POSITIVE, QUALITY-NEGATIVE (2026-05-31).** Added OLMoE raw expert/router adapter hooks to `scripts/train_moe_qwen_sft.py`: `--olmoe-expert-down-lora-r`, `--olmoe-expert-down-lora-alpha`, `--olmoe-router-lora-r`, and `--olmoe-router-lora-alpha`. BF16 low-LR raw router/lm_head unfreeze avoids the earlier NaN but is unsafe (**34.38% / 62.79% FT** raw; safe calibration collapses to **32.81%**). Expert-down LoRA rank 4 is stable and reaches **37.50% / 2.33% FT**, tying but not beating the earlier attention-LoRA small probe. Attention+expert LoRA collapses to **31.25% / 0.00% FT**. Summary: `outputs/moe/g4_real_olmoe_donor_init/stable_adaptation_probe_summary_2026-05-31.json`.

75. ~~**Teacher distillation on the stable expert-adapter surface**~~ **COMPLETE / QUALITY-NEGATIVE (2026-05-31).** Added teacher-logit KL distillation to `scripts/train_moe_qwen_sft.py` using `outputs/moe/teacher_logits/pyrrho_nano_g3_full_v8/`. The teacher signal itself is sane on the exact 64-row eval sample (**96.88% accuracy / 2.33% FT**, balanced 22/21/21 predictions), but bounded student adaptation did not absorb it. Best expert-down LoRA distillation tied the old small ceiling at **39.06% / 2.33% FT**; teacher weight 4 dropped to **37.50% / 0.00% FT** with no TRUSTWORTHY recall; label-only collapsed to all ABSTAIN; router+expert was worse/unsafe; BF16 low-LR raw router/lm_head stayed finite but collapsed mostly to DISPUTED. Summary: `outputs/moe/g4_real_olmoe_donor_init/teacher_distillation_probe_summary_2026-05-31.json`.

76. ~~**OLMoE gate/up expert training-surface probe**~~ **COMPLETE / MECHANICALLY POSITIVE, QUALITY-NEGATIVE (2026-05-31).** Added raw expert `gate_up_proj` LoRA hooks to `scripts/train_moe_qwen_sft.py` via `--olmoe-expert-gate-up-lora-r` / `--olmoe-expert-gate-up-lora-alpha`, alongside the existing expert-down hook. Smoke passed on the real donor checkpoint with **9.22M** trainable params. Rank-4 gate/up+down distillation trained **18.44M** params, but LR **1e-4** was unsafe at tau **0.50** (**35.94% / 48.84% FT**) and only became safe by collapsing to **34.38% / 2.33% FT** at tau **0.65**; LR **3e-5** collapsed further to **32.81% / 0.00% FT** at the best safe gate with no TRUSTWORTHY predictions. Summary: `outputs/moe/g4_real_olmoe_donor_init/gate_up_expert_surface_probe_summary_2026-05-31.json`.

77. ~~**OLMoE head-topnorm donor-resize probe**~~ **COMPLETE / MECHANICALLY POSITIVE, QUALITY-NEGATIVE (2026-05-31).** Added `--resize-strategy head-topnorm-slice` to `scripts/init_olmoe_from_donor.py` and built `outputs/moe/g4_real_olmoe_donor_init/olmoe_1b7b_head_topnorm_slice_hf/`, preserving the exact OLMoE carrier with **3,969,688,576 / 3,969,688,576** params copied and **0** missing. No-training label-score was unsafe at tau **0.50** (**35.94% / 46.51% FT**) and safe only by near-ABSTAIN collapse. The controlled expert-down distillation comparison tied the old accuracy ceiling at **39.06%**, but was less safe (**4.65% FT** vs old **2.33%**) with the same TRUSTWORTHY recall. Summary: `outputs/moe/g4_real_olmoe_donor_init/head_topnorm_slice_init_probe_summary_2026-05-31.json`.

78. ~~**External current-base controls for Qwen/LFM**~~ **COMPLETE / QUALITY-NEGATIVE (2026-05-31).** Downloaded and tested `Qwen/Qwen3.5-0.8B-Base` and `LiquidAI/LFM2.5-8B-A1B` on the same 64-row label-score control plus a 128-row LoRA+teacher probe. Raw Qwen tied the **39.06%** safe ceiling only at tau **0.90** with **0** DISPUTED predictions; Qwen LoRA/distill regressed to **35.94% / 2.33% FT**. Raw LFM had an unsafe **42.19% / 11.63% FT** tradeoff but safe calibration collapsed to **34.38% / 4.65% FT**; LFM LoRA/distill collapsed to **35.94% / 0.00% FT** with no TRUSTWORTHY predictions. Summary: `outputs/external_baselines/pyrrho_moe_external_baseline_control_summary_2026-05-31.json`.

79. ~~**fitz-sage enrichment-bus local-AI probe**~~ **COMPLETE / QWEN-POSITIVE, LFM/PYRRHO-DONOR NEGATIVE (2026-06-01).** Added `scripts/probe_fitz_sage_enrichment_bus.py`, which imports the real fitz-sage `KragEnricher` and checks the exact JSON-array enrichment contract. `Qwen/Qwen3.5-0.8B-Base` passes the small fixture with **1024** output tokens: **2/2** batch calls parse/count OK, **8/8** item shapes, **8/8** nonempty keyword lists, **3/8** nonempty entity lists, **3/8** temporal metadata, and **35/39** anchor hits. The 512-token cap failed by truncating JSON. `LiquidAI/LFM2.5-8B-A1B` returned empty HF generations in this setup, and the current LM Studio endpoint model `pyrrho-moe-g4-real-olmoe-donor-init` returned non-JSON repetition. Reports: `outputs/enrichment_bus_probe/`.

80. ~~**Qwen 0.8B Q4_K_M CPU-only enrichment runtime**~~ **COMPLETE / PASSES SMALL BUS FIXTURE (2026-06-01).** Downloaded LM Studio's `qwen/qwen3.5-0.8b` Q4_K_M GGUF variant (`Qwen3.5-0.8B-Q4_K_M.gguf`, **527,502,816** bytes plus BF16 mmproj) and loaded it with `lms load qwen/qwen3.5-0.8b --gpu off -c 2048 --identifier qwen35-08b-q4km-cpu`. Load time was **3.39s**, reported size **700.98 MiB**, `lms ps` showed **735.03 MB** at context **2048**, parallel **4**, TTL **5m**. Three enrichment-bus endpoint runs all passed parse/count and item-shape gates. Throughput was **38.54 / 39.49 / 39.77 completion tok/s**, mean **39.26 tok/s**, with **666** completion tokens per run over two batch calls. Quality on the fixture was stable: **8/8** nonempty keyword lists, **5/8** nonempty entity lists, **3/8** temporal metadata, **34/39** anchor hits. Real fitz-sage CLI timing on `rag_test_corpus/keyword_test` with the same CPU-only endpoint: cold-ish first `--source` query produced the answer at **63.9s** wall / **57.6s** pipeline and returned after background indexing at **117.4s** shell elapsed; second warm repeated `--source` query produced the answer at **30.4s** wall / **29.3s** pipeline and returned at **36.0s** shell elapsed. Warm pipeline split: query prep **5.3s**, retrieval **9.2s**, governance **0.6s**, generation **14.2s**. Reports: `outputs/enrichment_bus_probe/fitz_sage_enrichment_bus_qwen3_5_0_8b_q4_k_m_lmstudio_cpu_1024*.json`.

81. ~~**Qwen 0.8B Q2_K CPU-only enrichment control**~~ **COMPLETE / NEGATIVE (2026-06-01).** LM Studio's community alias does not ship Q2, so downloaded `bartowski/Qwen_Qwen3.5-0.8B-GGUF` file `Qwen_Qwen3.5-0.8B-Q2_K.gguf` (**464,231,520** bytes), hard-link imported it into LM Studio, and loaded it CPU-only as `qwen35-08b-q2k-cpu`. Load time was **1.64s**, reported size **442.73 MiB**, `lms ps` showed **464.23 MB** at context **4096**, parallel **4**. Default enrichment-bus batching failed: both 4-item calls parsed as JSON but returned only **1** object, so fitz-sage would fallback to empty enrichment; throughput was only **16.88 tok/s**. Batch size **1** parsed/count OK but still failed usefulness: **12.00 tok/s**, **8/8** keywords, **8/8** entities, **0/8** temporal, only **12/39** anchor hits. Reports: `outputs/enrichment_bus_probe/fitz_sage_enrichment_bus_qwen3_5_0_8b_q2_k_lmstudio_cpu*.json`. Conclusion: Q2 saves ~258 MiB vs Q4_K_M but is slower and not reliable for the enrichment bus.

82. **Current active next step: stronger OLMoE initialization/upcycling or broader training surface before scale-up.** Keep the exact stock-loadable OLMoE carrier shape unchanged, but stop scaling the tested tiny adapter SFT/distillation recipes, simple donor-resize variants, and 64-128 row external-base adapter controls. The next useful work must materially improve the seed/training surface enough that the strong teacher signal can move class behavior beyond the current **39.06%** ceiling, then rerun bounded quality checks before any full GGUF/LM Studio trained-weight gate.

83. ~~**fitz-sage retrieval-first cleanup plan**~~ **COMPLETE / HANDOFF WRITTEN (2026-06-01).** Wrote `C:/Users/yanfi/PycharmProjects/fitz-sage/docs/roadmap/retrieval-first-pivot.md` and updated fitz-sage's local roadmap index plus `HANDOFF.md`. Direction: fitz-sage should default to governed retrieval, not answer generation: query + source path in, ranked `EvidencePack` out. Synthesis becomes explicit/optional; no API key or local chat model should be required for default retrieval. The plan phases are `EvidencePack`, `fitz retrieve`, no-chat heuristic query planning, optional synthesis provider, background enrichment, and docs/tests.

## Release gates (the bar any pyrrho model must clear before shipping)

Measured mean across 3 seeds. For datasets with a held-out test split (V7+), checkpoint/threshold selection happens on validation and gates are applied to the held-out test report.

- **Overall accuracy ≥ 78.7%** — matches fitz-sage v0.11 sklearn baseline.
- **False-trustworthy rate ≤ 5.7%** — matches baseline; the production safety axis.

The originally-planned **tier0 95% sanity gate has been dropped** (see LOG 2026-05-14 afternoon). With 60 cases, run-to-run variance is ±3.5 pts and ~5 of the 60 cases have ambiguous gold labels. Tier0 is reported as a diagnostic in every model card, not a gate.

## Things NOT to do (already decided — don't relitigate)

- ❌ Don't propose Qwen 2.5 anything — stale (Nov 2024). Use Qwen 3.5+ family.
- ❌ Don't propose 35B-class MoE bases — violates the universal CPU-runnable constraint.
- ❌ Don't propose Llama-family bases — license is more restrictive than Apache-2.0.
- ~~❌ Don't start fitz-gov v6~~ fitz-gov V6.0.0 shipped 2026-05-20 (V5.1-enriched schema overlay). ~~Do not publish/train on V7 until QA passes~~ V7.0.1 shipped to Hugging Face on 2026-05-24 after blind-label, dedup/leakage, cross-label review, and schema-clean gates passed. Do not add new primary domains in V7; V8.0.1 is now the published target-50 expansion release with the unstructured modality field backfilled.
- ❌ Don't rebrand pyrrho — chosen after going through Doxa/Aegis/Sift/Minos/Themis.
- ❌ Don't generate emojis in code/docs unless explicitly asked.
- ❌ Don't propose running pyrrho as a remote endpoint / hosted service. It runs CPU-side via INT8 ONNX inside fitz-sage — that's the architectural commitment (no embeddings, no vector DB, no per-vendor providers in fitz-sage's hot path).
- ❌ Don't confuse `pyrrho-MoE-g3-alpha` with the active MoE MVP goal. The alpha is published and useful, but the current goal is the real ~4B/A0.4B CPU-runnable generative sparse MoE. Do not keep beta-hardening the quorum package unless explicitly requested.

## Where to look for more

| Need | Where |
|---|---|
| What happened, why, and when | [LOG.md](LOG.md) |
| Full vision + 10-release roadmap | [PROJECT.md](PROJECT.md) |
| Code modality axes and current gaps | [CODE_MODALITY_AXES.md](CODE_MODALITY_AXES.md) |
| End-to-end model-development pipeline | [METHODOLOGY.md](METHODOLOGY.md) |
| RTX 5090 / Blackwell / Windows specifics | [SETUP.md](SETUP.md) |
| Repository overview + quickstart | [../README.md](../README.md) |
| Persistent memory (user prefs, banned models, conventions) | `C:\Users\yanfi\.Codex\projects\C--Users-yanfi-PycharmProjects-pyrrho\memory\` |
